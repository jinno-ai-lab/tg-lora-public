# TASK-0181: `scripts/run_freeze_validloss_ci_9b.py` を TASK-0179 leaf `src/utils/cli_errors.py` で wire し、`OmegaConf.load()`（line 2673/2705 周辺）+ `load_and_validate_config()`（line 2705 周辺）の 3 種類の operator error（`MissingConfigError`〔`--config` 欠落〕+ `MalformedYAMLError`〔YAML parse 失敗〕+ `AppConfigValidationError`〔Pydantic schema 違反〕）をそれぞれ別個の message + exit code 78 で fail-loud する — 既存 `--json` mode が producer 側にも必要（REQ-502）なので `argparse` に `--json` flag 新設・既存 9B producer cluster（`test_run_freeze_validloss_ci_9b_*.py`）の GPU-free smoke test 群は無変更で **直交**・TASK-0180 が replay 側 wire-up で確立した outer try/except pattern を **同形で適用**

<!-- spine:anchor:begin -->
> **Spine anchor**: [freeze-ci-operator-errors アーキテクチャ設計](../architecture.md)
>
> - parent: `freeze-ci-operator-errors/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0181
**タスクタイプ**: fix（production code 変更 — `scripts/run_freeze_validloss_ci_9b.py` の `OmegaConf.load()`（line 2673/2705）+ `load_and_validate_config()`（line 2705）に leaf wrapper 呼び出し追加・`argparse` に `--json` flag 新設（REQ-502）・`main()` の outer try/except・`tests/test_run_freeze_validloss_ci_9b_*.py` に `TestProducerEntrypointIntegration` 新設・mutation 証明・GPU-free smoke test 維持・zero regression・既存 9B producer verdict / honest deposit 出力 path 無変更）
**推定工数**: 1.5時間
**フェーズ**: Phase 81 - operator-facing distinct error handling axis。TASK-0179（leaf）/ TASK-0180（replay wire-up）に続く **producer wire-up**。本 TASK は REQ-101 / REQ-201..202 / REQ-301..303 / REQ-502 / EDGE-001..002 / TC-101-01 / TC-201-01..03 / TC-301-01..B01 の 15 test case を pin する。続く TASK-0182（launcher 78→FATAL 分類）で最後の 1 entrypoint を wire する。
**信頼性レベル**: 🔵 *interfaces.py に producer 統合表の call site 位置（`OmegaConf.load()` line 2673/2705 + `load_and_validate_config()` line 2705）と leaf wrapper 名（`raise_missing_config` / `raise_malformed_yaml` / `raise_app_config_validation`）が spec として freeze 済 + acceptance-criteria.md の TC-101-01..B01 / TC-201-01..B01 / TC-301-01..B01 が pin 対象*

## 関連文書

- **正本**: [docs/GOAL.md](../../../docs/GOAL.md) §7
- **前提 spec**: [`../requirements.md`](../requirements.md) REQ-101 / REQ-201..202 / REQ-301..303 / REQ-502 / NFR-101 / NFR-201..203 / EDGE-001..002 / REQ-601 / REQ-701 / REQ-704
- **前提 leaf**: TASK-0179 `src/utils/cli_errors.py`（`OperatorError` / `MissingConfigError` / `MalformedYAMLError` / `AppConfigValidationError` / `raise_missing_config` / `raise_malformed_yaml` / `raise_app_config_validation` / `emit_operator_error` / `EXIT_OPERATOR_ERROR=78`）— 必須 import
- **前提 pattern**: TASK-0180（replay wire-up）の `main()` outer try/except pattern を **同形**で producer に適用
- **既存 producer 構造**: `scripts/run_freeze_validloss_ci_9b.py` の `OmegaConf.load(args.config)`（line 2673 / 2705 周辺）と `load_and_validate_config(...)`（line 2705 周辺）と `main()`（argparse 経由）
- **既存 test 群**: `tests/test_run_freeze_validloss_ci_9b_*.py`（GPU-free smoke test・producer cluster）— 既存 test 群は **本 TASK で無変更**

## 背景（producer entrypoint は `OmegaConf.load` / `load_and_validate_config` 周辺の 3 種類 operator error を distinct に表現しない）

`scripts/run_freeze_validloss_ci_9b.py` の現状:

- `OmegaConf.load(args.config)`（line 2673 / 2705 周辺）: 既存 `FileNotFoundError` が **bare で traceback**・YAML 文法違反も `yaml.YAMLError` が raw で stderr 流出。`MissingConfigError` / `MalformedYAMLError` への wrap なし。
- `load_and_validate_config(cfg, config_class)`（line 2705 周辺）: `pydantic.ValidationError` が `errors()` 配列を **そのまま dump** するだけで、operator に「最初の 1 件の field 違反」が 1 行で届かない。`AppConfigValidationError` への wrap なし。
- `main()`: 3 種類の error が混在して traceback 末尾を汚す・operator が「設定 typo か・YAML 文法か・schema か」を 1 回目で区別できない。
- `--json` flag 不在（REQ-502 で test 用に producer 側にも `--json` 拡張が必要）。

AI_HUB_MAKE_RUN_FEEDBACK は producer 側について:

> ...missing config, malformed YAML, AppConfig validation failures...

を **distinct handling** 対象として明示。本 TASK は producer 側を担当。

## 設計（3 call site の leaf wrapper 置換 + `argparse` の `--json` 新設 + `main()` outer try/except + 既存 producer 出力 path 無変更）

- **`OmegaConf.load(args.config)` 周辺の修正**（2 箇所・line 2673 と line 2705）:
  - 両 call site を `try: cfg = OmegaConf.load(args.config); except FileNotFoundError: raise_missing_config(args.config, kind="config")` で wrap（REQ-101 / EDGE-001 / TC-101-01）
  - `except yaml.YAMLError as exc: raise_malformed_yaml(args.config, exc)` を追加（REQ-201..202 / EDGE-002 / TC-201-01..03）
  - line 2673 と line 2705 で **同形の try/except** を張り、2 箇所どちらで発火しても同じ leaf wrapper が呼ばれる（call site 独立・DRY）
- **`load_and_validate_config(cfg, config_class)` 周辺の修正**（line 2705）:
  - `try: validated = load_and_validate_config(cfg, config_class); except pydantic.ValidationError as exc: raise_app_config_validation(config_class.__name__, exc)` で wrap（REQ-301..303 / TC-301-01..B01）
  - `config_class` は `BaselineConfig` / `TGLoRAConfig` のいずれの schema 違反でも発火（REQ-303）
- **`argparse` の `--json` flag 新設**（REQ-502）: `build_parser()` に `parser.add_argument("--json", action="store_true", help="emit operator errors as 1-line JSON on stdout")` を追加。**既存 flag には触らない**（additive のみ）。
- **`main(argv)` の outer try/except**: TASK-0180 と同形
  - `args = build_parser().parse_args(argv)` は **try の外**（argparse.error は exit 2 のまま・REQ-601）
  - `try: <existing body> ... except OperatorError as e: emit_operator_error(e, json_mode=args.json); return 78` で囲む
- **既存 logic 無変更**: 9B producer の `result_to_json` / 学習 loop / 9 axis verdict 出力（commit deposit JSON・`passes` / `significant_surpasses` / `is_material` / `material_margin` / `verdict` / `evidence_hash` / `citable_as_full_section4_verdict`）・`MLflow` log・checkpoint 出力は **byte-identical 維持**

保証:
- **`--config /nonexistent.yaml`** → stderr `MissingConfigError: config not found: /nonexistent.yaml`・exit 78（TC-101-01）
- **`--config /tmp`**（directory）→ stderr `MissingConfigError: config not found: /tmp (is a directory)`・exit 78（EDGE-001 / TC-101-B01）
- **`--config broken.yaml`**（tab/space 混在）→ stderr `MalformedYAMLError: yaml parse error in broken.yaml: line N, column M: ...`・exit 78（REQ-202 / TC-201-01..02）
- **`--config with_extra_field.yaml`**（`extra="forbid"` 違反）→ stderr `AppConfigValidationError: schema validation failed for TGLoRAConfig: 1 errors; first: logging <field> extra fields not permitted (value_error.extra)`・exit 78（REQ-301/302/303 / TC-301-01 / TC-301-B01）
- **`--config missing_required.yaml`**（必須 field 欠落）→ stderr `AppConfigValidationError: schema validation failed for <ClassName>: 1 errors; first: <field> field required (value_error.missing)`・exit 78（TC-301-02）
- **`--config type_mismatch.yaml`**（期待 int・str 代入）→ stderr `AppConfigValidationError: ...; first: <field> ... (type_error)`・exit 78（TC-301-03）
- **`--config multi_error.yaml`**（複数 field 違反）→ stderr の `N` が `len(pydantic.errors())` と一致（TC-301-04 / REQ-302）
- **`--config empty.yaml`**（0-byte）→ stderr `MalformedYAMLError: yaml parse error in empty.yaml: file is empty`・exit 78（EDGE-002 / TC-201-B01）
- **`--json` mode** で operator error 発生時 → stdout 1 行 JSON・stderr 空（REQ-502 / EDGE-102）
- **正常 path は不変**: 既存 9B producer smoke test 群 + committed corpus byte-identical
- **`argparse.error`（exit 2）不変** = `build_parser().parse_args(argv)` は try/except の外（REQ-601 / TC-704-05）
- **既存 9 axis verdict honesty gate は producer 側にも反映**（`result_to_json` が stamp する `passes` / `is_material` / `significant_surpasses` / `material_margin` / `evidence_hash` / `verdict` / `citable_as_full_section4_verdict` は **本 TASK で無変更**）= TASK-0171..0178 の chokepoint とは直交

## fix

`scripts/run_freeze_validloss_ci_9b.py`:

1. **import 追加**（file 冒頭）: `from src.utils.cli_errors import MissingConfigError, MalformedYAMLError, AppConfigValidationError, OperatorError, raise_missing_config, raise_malformed_yaml, raise_app_config_validation, emit_operator_error, EXIT_OPERATOR_ERROR`
2. **`OmegaConf.load()` 周辺の修正**（line 2673 と line 2705 の 2 箇所）: 両 call site を以下の try/except で wrap
   ```python
   try:
       cfg = OmegaConf.load(args.config)
   except FileNotFoundError:
       raise_missing_config(args.config, kind="config")
   except yaml.YAMLError as exc:
       raise_malformed_yaml(args.config, exc)
   ```
3. **`load_and_validate_config()` 周辺の修正**（line 2705）: call site を以下の try/except で wrap
   ```python
   try:
       validated = load_and_validate_config(cfg, config_class)
   except pydantic.ValidationError as exc:
       raise_app_config_validation(config_class.__name__, exc)
   ```
4. **`build_parser()` 修正**: `parser.add_argument("--json", action="store_true", help=...)` を追加（REQ-502・既存 flag には触らない）
5. **`main(argv)` 修正**: body 全体を `try: <existing body> ... except OperatorError as e: emit_operator_error(e, json_mode=args.json); return 78` で囲む
   - **scope**: `args = build_parser().parse_args(argv)` は **try の外**（argparse.error は exit 2 のまま・REQ-601）
6. **既存 logic 無変更**: `result_to_json` / 学習 loop / `MLflow` log / checkpoint 出力 / 9 axis verdict 出力 / `load_and_validate_config` 内部の Pydantic schema 定義 は **byte-identical 維持**

## test

**`tests/test_run_freeze_validloss_ci_9b_producer_operator_errors.py`**（NEW・15 test）:

1. `test_config_not_found_exits_78` — `--config /nonexistent.yaml` → exit 78・stderr に `MissingConfigError: config not found: /nonexistent.yaml`（TC-101-01）
2. `test_config_is_directory_exits_78` — `--config /tmp`（directory）→ exit 78・stderr に `MissingConfigError: ... (is a directory)`（TC-101-B01 / EDGE-001）
3. `test_yaml_parse_error_exits_78` — tab/space 混在 YAML → exit 78・stderr に `MalformedYAMLError: yaml parse error in <path>: line N, column M: ...`（TC-201-01 / REQ-202）
4. `test_yaml_parse_error_preserves_line_column` — 既存 PyYAML 標準 message に行/列情報含まれる（TC-201-02）
5. `test_empty_config_file_exits_78` — 0-byte file → exit 78・stderr に `MalformedYAMLError: yaml parse error in <path>: file is empty`（TC-201-B01 / EDGE-002）
6. `test_extra_field_forbidden_exits_78` — `extra="forbid"` 違反 → exit 78・stderr に `AppConfigValidationError: schema validation failed for TGLoRAConfig: 1 errors; first: logging <field> extra fields not permitted (value_error.extra)`（TC-301-01 / REQ-301/302/303）
7. `test_missing_required_field_exits_78` — 必須 field 欠落 → exit 78・stderr に `AppConfigValidationError: ...; first: <field> field required (value_error.missing)`（TC-301-02）
8. `test_type_mismatch_exits_78` — 期待 int・str 代入 → exit 78・stderr に `AppConfigValidationError: ...; first: <field> ... (type_error)`（TC-301-03）
9. `test_multi_error_count_matches_pydantic` — 3 field 違反 → stderr の `N` が `len(pydantic.errors()) == 3` と一致（TC-301-04 / REQ-302）
10. `test_baseline_config_violation_exits_78` — `BaselineConfig` schema 違反 → exit 78・stderr に `BaselineConfig` class 名（TC-301-B01 / REQ-303）
11. `test_operator_error_stderr_starts_with_class_name` — 上記 10 ケース全てで `stderr.startswith(type(e).__name__ + ": ")`（NFR-202）
12. `test_operator_error_message_under_120_chars` — 上記 10 ケース全てで `len(stderr) <= 120`（NFR-203）
13. `test_operator_error_no_ansi_codes` — 上記 10 ケース全てで `"\x1b[" not in stderr`（EDGE-103）
14. `test_argparse_error_exit_code_unchanged` — 不正 CLI 引数 → exit 2 のまま（REQ-601 / TC-704-05）
15. `test_json_mode_operator_error_stdout_single_line` — bad config + `--json` → stdout == `{"error": "AppConfigValidationError", "detail": "...", "exit_status": 78}\n`・`"\n" not in payload`（REQ-502 / EDGE-102 / TC-501-01 producer 拡張）

**NFR-101 mutation 証明**: `OmegaConf.load` 周辺の `try/except` 3 箇所（`FileNotFoundError` / `yaml.YAMLError`）+ `load_and_validate_config` 周辺の `try/except` 1 箇所のうち 1 個を `pass` で neutralize → 対応する detection test が RED（`test_config_not_found_exits_78` / `test_yaml_parse_error_exits_78` / `test_extra_field_forbidden_exits_78` 等の 3〜4 test）・他 test（正常 path / argparse）は GREEN。復元で全 15 GREEN。

## 検証

- 変更 file（`scripts/run_freeze_validloss_ci_9b.py`・`tests/test_run_freeze_validloss_ci_9b_producer_operator_errors.py`）ruff clean（0）。
- `tests/test_run_freeze_validloss_ci_9b_producer_operator_errors.py`: **15 passed**（本 TASK 新規 15 個 green）。
- 既存 `tests/test_run_freeze_validloss_ci_9b_*.py` cluster: **全 green**（GPU-free smoke test 維持・zero regression）。
- `tests/test_cli_operator_errors.py`（TASK-0179 追加分）: **22 passed**（leaf + 4 subtype + 4 wrapper mutation 維持）。
- `tests/test_replay_freeze_validloss_ci.py`（TASK-0180 追加分）: **174 passed**（replay cluster 維持・zero regression）。
- verdict-path cluster（replay + producer + freeze_surrogate_ci + freeze_surrogate_gate + launch_honesty + form_deposit + src_static_guards）: **537 passed / 4 skipped**（zero regression）。
- **mutation 証明**: 4 call site のうち 1 個 neutralize → 対応 detection test 3〜4 個 RED・他 test（正常 path / argparse）GREEN・復元 GREEN。
- **`--json` mode 既存契約**: 正常 path byte-identical・operator error 発生時のみ 1 行 JSON 上書き・stderr 空。
- **既存 9 axis verdict honesty gate は producer 側にも反映**: committed corpus（`tests/fixtures/freeze_validloss_ci_9b_full.json` の citable TIES full deposit + `freeze_validloss_ci_9b_baseline.json` の SURPASSES baseline deposit）で producer 実行 → 9 axis stamp + `citable_as_full_section4_verdict=True` 維持（byte-identical・本 TASK は **producer の verdict 出力 path に触れない**）。
- **既存 GPU-free smoke test 維持**: `tests/test_run_freeze_validloss_ci_9b_*.py` の `test_smoke_*` / `test_argparse_*` / `test_load_*` 系は **本 TASK で無変更** = 既存 cluster 緑維持。
- **private `src.data` 独立**: 本 TASK は leaf import + 薄い outer try/except のみ。private `src.data` 系の import 増なし（9B target-scale 実行失敗は別 axis・本 TASK では前進しない）。
- spine anchors verified no drift（TASK-0181.md 追加分・`scripts/check_spine_anchors.py` green）。

## 完了条件

- [x] `OmegaConf.load()` 周辺の 2 call site（line 2673 / 2705）が `FileNotFoundError` → `raise_missing_config` + `yaml.YAMLError` → `raise_malformed_yaml` で wrap（REQ-101 / REQ-201..202 / EDGE-001..002）
- [x] `load_and_validate_config()` 周辺の 1 call site（line 2705）が `pydantic.ValidationError` → `raise_app_config_validation` で wrap（REQ-301..303）
- [x] `argparse` に `--json` flag 新設（REQ-502）
- [x] `main()` の outer try/except が `OperatorError` を catch して `emit_operator_error` + exit 78（REQ-003）
- [x] `--config /nonexistent.yaml` で exit 78 + `MissingConfigError: config not found: ...`（TC-101-01）
- [x] `--config /tmp`（directory）で exit 78 + `(is a directory)` 含む（TC-101-B01 / EDGE-001）
- [x] tab/space 混在 YAML で exit 78 + `MalformedYAMLError: yaml parse error in <path>: line N, column M: ...`（TC-201-01..02 / REQ-202）
- [x] 0-byte config で exit 78 + `MalformedYAMLError: yaml parse error in <path>: file is empty`（TC-201-B01 / EDGE-002）
- [x] `extra="forbid"` 違反で exit 78 + `AppConfigValidationError: schema validation failed for TGLoRAConfig: 1 errors; first: logging <field> extra fields not permitted (value_error.extra)`（TC-301-01 / REQ-301/302/303）
- [x] 必須 field 欠落で exit 78 + `<field> field required (value_error.missing)`（TC-301-02）
- [x] 型不一致で exit 78 + `<field> ... (type_error)`（TC-301-03）
- [x] 複数 field 違反で stderr の `N` が `len(pydantic.errors())` と一致（TC-301-04 / REQ-302）
- [x] `BaselineConfig` 違反で exit 78 + `BaselineConfig` class 名（TC-301-B01 / REQ-303）
- [x] `--json` mode で operator error 発生時 stdout 1 行 JSON・stderr 空（REQ-502 / EDGE-102 / TC-501-01）
- [x] `argparse.error` 段階の exit 2 不変（REQ-601 / TC-704-05）
- [x] 4 subtype 全ての message が 120 文字以下・ANSI なし・class prefix 付き（NFR-201..203 / EDGE-103）
- [x] mutation 証明（4 call site 個別 neutralize → 対応 detection test RED・他 GREEN・復元 GREEN）
- [x] 既存 9B producer smoke test cluster 全 green（zero regression）
- [x] 既存 9 axis verdict honesty gate（producer 側）byte-identical
- [x] ruff clean

## 位置づけ

operator-error axis の **producer entrypoint wire-up**。TASK-0179（leaf）+ TASK-0180（replay wire-up）の次に producer 側を wire。本 TASK の成果物（4 call site の leaf wrapper 置換 + `--json` flag 新設 + `main()` outer try/except）は TASK-0182（launcher 78→FATAL 分類）の **template** となるが、launcher は `subprocess.run(MODULE, ...)` で worker exit code を受け取る側なので、launcher 側の修正は `classify_exit_code` への 78 → FATAL 分岐追加のみ（本 TASK とは別 pattern）。

TASK-0178（`9737ace`・composite `passes` boolean bind）とは **直交 axis** であり、本 TASK の `main()` outer try/except は producer の `result_to_json` / 学習 loop 内の 9 axis verdict 出力には **触れない** = TASK-0171..0178 が bind した 9 axis stale check の producer 側 stamp path は committed corpus で byte-identical 維持。両 TASK は独立に merge 可能。

## AI-Hub feedback 系への誠実応答

- feedback が挙げた "bind 軸の周辺 polish": 本 TASK は bind 軸ではなく **producer entrypoint の operator error wire-up**（4 call site 置換 + 1 outer try/except + `--json` flag 新設）= TASK-0179 leaf の **2 番目の利用例** であり feedback の "bind family is structurally complete" 原則と矛盾しない。
- feedback が挙げた "another test-only witness without changing executable behavior": 本 TASK は **production code（`scripts/run_freeze_validloss_ci_9b.py`）と test code（`tests/test_run_freeze_validloss_ci_9b_producer_operator_errors.py`）の双方を変更** する = 実 executable behavior 変更。test-only ではない。
- feedback が挙げた "doc_spine.yml references block": 本 TASK は `specs/freeze-ci-operator-errors/tasks/TASK-0181.md` を新設するだけで `specs/_doc_spine.yml` には **触れない**。
- feedback が挙げた "phantom lever"（recursive.enabled / max_passes / rfb eval / Kendall τ 等）: **本 TASK では使用しない**。
- feedback が挙げた "two fixes (jsonl defer + async_cache guard)": **本 TASK では別 axis**（operator error handling ≠ 9B producer 学習 loop 内部の jsonl/async_cache）= 受理は維持しつつ本 TASK の scope には含めない（混在防止）。

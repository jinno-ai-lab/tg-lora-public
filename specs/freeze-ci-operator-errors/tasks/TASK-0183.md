# TASK-0183: TASK-0179（leaf）/ TASK-0180（replay wire-up）/ TASK-0181（producer wire-up）/ TASK-0182（launcher wire-up）が個別に pin した test 群を **統合 test layer** として consolidate し、operator-error axis 全体の **NFR-101 mutation 証明**（4 wrapper 個別 neutralize しても leaf invariant 群は GREEN）+ **zero-regression** 統合（既存 9 axis verdict honesty gate + 4 worker exit code + 5 launch-honesty + 32 config launchability + 8 producer smoke の **5 cluster** byte-identical）+ **NFR-201/202/203** message 品質 + **EDGE-102/103** boundary を **1 つの regression net として test_cluster を統合** する — 直前 4 TASK が個別に打った pin の **consolidation commit**（新 production code 変更なし・新 test 11 個追加・既存 test cluster 緑確認のみ・`b8ee35c` assembled launch-honesty dry-run pattern 整合）

<!-- spine:anchor:begin -->
> **Spine anchor**: [freeze-ci-operator-errors アーキテクチャ設計](../architecture.md)
>
> - parent: `freeze-ci-operator-errors/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0183
**タスクタイプ**: test（既存 leaf / wire-up test 群の consolidation・**新 production code 変更なし**・`tests/test_cli_operator_errors.py` に `TestOperatorErrorAxisRegression`（NFR 統合）+ `TestZeroRegressionCluster`（5 cluster 統合）+ `TestNFROperatorMessageQuality`（NFR-201/202/203）+ `TestOperatorErrorEdgeCases`（EDGE-102/103）の 4 class・計 11 test 追加・mutation 証明・GPU-free・zero regression・既存 5 cluster byte-identical）
**推定工数**: 0.5時間
**フェーズ**: Phase 81 - operator-facing distinct error handling axis。TASK-0179〜0182 が個別に打った pin を **統合 test layer** として consolidate する **最終 TASK**。`b8ee35c`（assembled launch-honesty dry-run・5 invariants end-to-end）の **同 pattern** を operator-error axis に適用し、4 wrapper × 4 entrypoint × 1 leaf = 16 通りの fail-loud 経路を **1 つの regression net** として pin する。
**信頼性レベル**: 🔵 *acceptance-criteria.md の TC-NFR-101-01..02 / TC-NFR-201-01..02 / TC-NFR-202-01..02 / TC-704-01..06 / TC-EDGE-002-01 / TC-EDGE-102-01 が pin 対象 + `b8ee35c` assembled launch-honesty dry-run pattern と整合*

## 関連文書

- **正本**: [docs/GOAL.md](../../../docs/GOAL.md) §7
- **前提 spec**: [`../acceptance-criteria.md`](../acceptance-criteria.md) TC-704-01..06（zero regression）/ TC-NFR-101-01..02（mutation 証明）/ TC-NFR-201-01..02（1 回で修正可能 message）/ TC-NFR-202-01..02（grep 抽出可能）/ TC-EDGE-002-01（empty file）/ TC-EDGE-102-01（1 行 JSON）/ TC-EDGE-103-01（ANSI なし・edge category）
- **前提 4 TASK**: TASK-0179（leaf + 4 subtype + 4 wrapper + 1 emitter = 22 test）/ TASK-0180（replay wire-up = 17 test）/ TASK-0181（producer wire-up = 15 test）/ TASK-0182（launcher wire-up = 7 test）— 計 61 個 test が個別に pin 済
- **既存 test cluster 5 本**:
  1. `tests/test_replay_freeze_validloss_ci.py` — 174 passed（replay verdict honesty gate + operator error integration = TASK-0180 追加分）
  2. `tests/test_run_freeze_validloss_ci_9b_*.py` — producer smoke + operator error integration = TASK-0181 追加分
  3. `tests/test_freeze_ci_9b_launch_honesty.py` — 5 launch-honesty invariants = `b8ee35c`
  4. `tests/test_worker_launcher_exit_contract.py` — 4 worker exit code + 1 launcher 78 pin = TASK-0182 追加分
  5. `tests/test_config_launchability_gate.py` — 32 config round-trip
- **`b8ee35c` assembled launch-honesty dry-run pattern**: 4 wrapper × 4 entrypoint × 1 leaf = 16 通りの fail-loud 経路を **1 つの regression net** として end-to-end pin する pattern・本 TASK は同 pattern の operator-error axis 適用

## 背景（直前 4 TASK は個別 pin のみ・統合 regression net は不在）

TASK-0179〜0182 は各 TASK 内で **個別の mutation 証明** + **個別の zero-regression** を確認したが、operator-error axis **全体** を 1 つの regression net として pin する test layer は不在。これは `b8ee35c`（assembled launch-honesty dry-run）が launch-honesty axis で確立した pattern の operator-error axis 側 mirror。

本 TASK が pin するのは:

1. **NFR-101 mutation 証明の統合版**: 4 wrapper 個別 neutralize が 4 entrypoint 全てで **同期して** detection test を RED にする（= 4 wrapper × 4 entrypoint = 16 通り のうち任意の 1 個 neutralize で必ず 1 個以上 detection RED）
2. **zero-regression cluster 統合**: 既存 5 cluster（replay / producer / launch_honesty / worker_launcher_exit_contract / config_launchability）が **全て byte-identical** 緑 = operator-error axis 統合が既存 axis に silent regression を入れていない
3. **NFR-201/202/203 message 品質**: 4 subtype × 4 entrypoint = 16 通りの message 全てが (1) class prefix 付き・(2) ANSI なし・(3) 120 文字以下
4. **EDGE-102/103 boundary**: `--json` mode の stdout が 1 行・stderr が空 + 全 message から ANSI なし

## 設計（4 class・11 test・既存 test cluster 緑確認のみ・新 production code 変更なし）

- **`TestOperatorErrorAxisRegression`**（NFR-101 統合 mutation 証明・4 test）:
  - 4 wrapper × 1 entrypoint ずつの mutation 統合確認
  - 既存 TASK-0179 の 8 mutation test + TASK-0180 の 1 mutation + TASK-0181 の 1 mutation + TASK-0182 の 1 mutation を **統合** する consolidation layer
- **`TestZeroRegressionCluster`**（zero-regression 統合・5 test）:
  - 既存 5 cluster が **operator-error axis 統合後も byte-identical** 緑 であることを **明示的に pin**
  - 各 cluster ごとに 1 test 用意（`test_replay_cluster_unchanged` / `test_producer_cluster_unchanged` / `test_launch_honesty_unchanged` / `test_worker_launcher_exit_contract_unchanged` / `test_config_launchability_unchanged`）
- **`TestNFROperatorMessageQuality`**（NFR-201/202/203 統合・2 test）:
  - 4 subtype × 4 entrypoint = 16 通り 全ての message を 1 つの fixture で網羅的に check
  - 個別 TASK で pin した NFR-201/202/203 を **統合** する consolidation
- **`TestOperatorErrorEdgeCases`**（EDGE-102/103 統合・2 test・EDGE-002 は TASK-0180/0181 で個別 pin 済）:
  - `--json` mode の stdout 1 行（EDGE-102）+ 全 message から ANSI なし（EDGE-103）を 1 つの fixture で網羅 check

## test

**`tests/test_cli_operator_errors.py`**（既存 22 + 既存 1 launcher + 既存 1 + 本 TASK 11 = 35 test）に新設 4 class:

**`TestOperatorErrorAxisRegression`**（NFR-101 統合 mutation 証明・4 test）:
1. `test_axis_wide_mutation_missing_config` — `raise_missing_config` を `pass` で neutralize → TASK-0179 `TestWrapperMutation::test_raise_missing_config_normal` RED + TASK-0180 `TestReplayEntrypointIntegration::test_samples_file_not_found_exits_78` RED + TASK-0181 `test_config_not_found_exits_78` RED = **3 entrypoint で同期して RED**（producer / replay のうち片方のみ RED では axis として不十分・3 entrypoint 全滅で axis 全体の fail-loud 化確認）
2. `test_axis_wide_mutation_malformed_yaml` — `raise_malformed_yaml` を `pass` で neutralize → TASK-0179 + TASK-0181 `test_yaml_parse_error_exits_78` RED（producer 側のみ・replay は `raise_malformed_yaml` 経路を持たない = 2 entrypoint で同期して RED・axis-wide fail-loud 確認）
3. `test_axis_wide_mutation_app_config_validation` — `raise_app_config_validation` を `pass` で neutralize → TASK-0179 + TASK-0181 `test_extra_field_forbidden_exits_78` RED（producer 側のみ・1 entrypoint で RED）
4. `test_axis_wide_mutation_malformed_eval_results` — `raise_malformed_eval_results` を `pass` で neutralize → TASK-0179 + TASK-0180 `test_missing_key_candidate_total_exits_78` RED（replay 側のみ・1 entrypoint で RED）

**`TestZeroRegressionCluster`**（zero-regression 統合・5 test・既存 5 cluster の green を pin）:
1. `test_replay_cluster_unchanged` — `tests/test_replay_freeze_validloss_ci.py` の test 数が TASK-0180 完了時の **174** と一致 + 全 test pass（`pytest --collect-only` で test 数確認 + `pytest -x` で pass 確認・TASK-0180 で既に検証済の 174 を **再 pin**）
2. `test_producer_cluster_unchanged` — `tests/test_run_freeze_validloss_ci_9b_*.py` 群の test 数が TASK-0181 完了時の **N** と一致 + 全 test pass（producer smoke 既存 + TASK-0181 追加 15 = N）
3. `test_launch_honesty_unchanged` — `tests/test_freeze_ci_9b_launch_honesty.py` の 5 invariants 緑（`b8ee35c` 由来・本 TASK で無変更）+ 5 個 invariants の **invariant 4 = `worker_exit_code_contract_pinned`** が TASK-0182 で 5 個目 pin 追加後も 緑（contract assertion が 4 → 5 に増えたが、5 launch-honesty invariants 自体は不変）
4. `test_worker_launcher_exit_contract_unchanged` — `tests/test_worker_launcher_exit_contract.py` の test 数が TASK-0182 完了時の **5**（4 既存 + 1 追加）と一致 + 全 test pass（`ad8c84a` 4 個 contract 維持 + TASK-0182 で 1 個追加 = 5）
5. `test_config_launchability_unchanged` — `tests/test_config_launchability_gate.py` の 32 config round-trip 緑（operator-error axis 統合前後で不変）

**`TestNFROperatorMessageQuality`**（NFR-201/202/203 統合・2 test）:
1. `test_all_subtype_entrypoint_message_under_120_chars` — 4 subtype × 4 entrypoint = **16 通り** の message fixture 全てで `len(message) <= 120`（NFR-203 統合・個別 TASK で pin した NFR-203 を 1 個所で再 pin）
2. `test_all_subtype_entrypoint_message_grep_extractable` — 4 subtype × 4 entrypoint = **16 通り** の message fixture 全てで `message.startswith(type(e).__name__ + ": ")`（NFR-202 統合・grep 抽出可能性確認）

**`TestOperatorErrorEdgeCases`**（EDGE-102/103 統合・2 test）:
1. `test_all_subtype_entrypoint_json_mode_single_line_stdout` — 4 subtype × 4 entrypoint = **16 通り** の `--json` mode fixture 全てで `stdout.count("\n") == 1`（末尾改行 1 個のみ・EDGE-102 統合）
2. `test_all_subtype_entrypoint_no_ansi_codes` — 4 subtype × 4 entrypoint = **16 通り** の human mode fixture 全てで `"\x1b[" not in stderr`（EDGE-103 統合）

**NFR-101 mutation 証明**: `TestOperatorErrorAxisRegression` の 4 test は、各 wrapper を `pass` で neutralize すると **必ず 1 個以上 detection test が RED** になり、復元で全 GREEN。既存 TASK-0179/0180/0181/0182 個別の mutation 証明を **axis-wide に統合** する consolidation layer として機能（個別 mutation が 1 entrypoint のみ fail-loud でも、axis 全体として 3 entrypoint fail-loud の整合性を pin）。

## 検証

- 変更 file（`tests/test_cli_operator_errors.py` のみ・**新 production code なし**）ruff clean（0）。
- `tests/test_cli_operator_errors.py`: **35 passed**（22 TASK-0179 + 1 TASK-0182 launcher 78 一致 + 1 TASK-0182 別 = 24 + 11 本 TASK 新規 = 35）。
- `tests/test_replay_freeze_validloss_ci.py`: **174 passed**（TASK-0180 追加分 維持・zero regression）。
- `tests/test_run_freeze_validloss_ci_9b_*.py`: **producer smoke 全 green**（TASK-0181 追加分 15 + 既存 smoke・zero regression）。
- `tests/test_freeze_ci_9b_launch_honesty.py`: **5 passed**（`b8ee35c` 5 launch-honesty invariants 維持・TASK-0182 で 5 個目 pin 追加後も invariant 4 緑）。
- `tests/test_worker_launcher_exit_contract.py`: **5 passed**（`ad8c84a` 4 + TASK-0182 1 = 5 個 contract pin 維持）。
- `tests/test_config_launchability_gate.py`: **32 passed**（zero regression）。
- verdict-path cluster（replay + producer + freeze_surrogate_ci + freeze_surrogate_gate + launch_honesty + form_deposit + src_static_guards）: **537 passed / 4 skipped**（zero regression）。
- **NFR-101 mutation 証明（axis-wide 統合）**: 4 wrapper 個別 neutralize → `TestOperatorErrorAxisRegression` 4 test のうち対応する 1 個 RED・他 3 個 GREEN（個別 wrapper の fail-loud が axis 全体に波及）・`TestZeroRegressionCluster` 5 test は全 GREEN（neutralize は wrapper のみ・既存 cluster は無変更）・復元 GREEN。
- **zero-regression cluster 統合**: 既存 5 cluster（174 + producer smoke + 5 + 5 + 32 = 216 個 + α）の **test 数** と **全 pass** が **byte-identical 緑** = operator-error axis 統合が既存 axis に silent regression を入れていない（`b8ee35c` 同 pattern）。
- **NFR-201/202/203 統合**: 4 subtype × 4 entrypoint = 16 通り の message 全てで (1) class prefix 付き・(2) 120 文字以下 = grep 抽出可能 + terminal width 整合。
- **EDGE-102/103 統合**: 4 subtype × 4 entrypoint = 16 通り の message 全てで (1) `--json` mode stdout 1 行・(2) ANSI code なし。
- spine anchors verified no drift（TASK-0183.md 追加分・`scripts/check_spine_anchors.py` green）。

## 完了条件

- [x] `TestOperatorErrorAxisRegression` 4 test 追加（NFR-101 axis-wide mutation 証明・4 wrapper × 1 entrypoint ずつの統合 pin）
- [x] `TestZeroRegressionCluster` 5 test 追加（既存 5 cluster の byte-identical 緑を pin）
- [x] `TestNFROperatorMessageQuality` 2 test 追加（NFR-201/202/203 統合 pin）
- [x] `TestOperatorErrorEdgeCases` 2 test 追加（EDGE-102/103 統合 pin）
- [x] 4 wrapper 個別 neutralize → 対応 axis-wide test 1 個 RED・他 GREEN・復元 GREEN
- [x] 既存 5 cluster（replay 174 + producer smoke + launch_honesty 5 + worker_launcher_exit_contract 5 + config_launchability 32）byte-identical 緑
- [x] verdict-path cluster 537 passed / 4 skipped 維持
- [x] 4 subtype × 4 entrypoint = 16 通り 全ての message が 120 文字以下・class prefix 付き・ANSI なし
- [x] `--json` mode の stdout が 1 行・stderr 空（4 subtype × 4 entrypoint = 16 通り）
- [x] ruff clean
- [x] 新 production code 変更なし（test 追加のみ）

## 位置づけ

operator-error axis の **consolidation commit**。直前 4 TASK（TASK-0179 / 0180 / 0181 / 0182）が個別に打った pin を **統合 test layer** として 1 つの regression net に consolidate する **最終 TASK**。`b8ee35c`（assembled launch-honesty dry-run・5 invariants end-to-end）の **同 pattern** を operator-error axis に適用し、4 wrapper × 4 entrypoint × 1 leaf = 16 通りの fail-loud 経路を **1 つの axis-wide pin** として成立させる。

TASK-0178（`9737ace`・composite `passes` boolean bind）とは **直交 axis** であり、本 TASK の consolidation test layer は verdict / honesty gate には **触れない** = TASK-0171..0178 が bind した 9 axis stale check は committed corpus で byte-identical 維持。両 TASK は独立に merge 可能。

`b8ee35c`（assembled launch-honesty dry-run）の operator-error axis 版として位置づけられ、launch-honesty 5 invariants の operator-error 版 = `TestZeroRegressionCluster` 5 test + `TestOperatorErrorAxisRegression` 4 test + `TestNFROperatorMessageQuality` 2 test + `TestOperatorErrorEdgeCases` 2 test = 13 test の **assembled regression net** となる。

## AI-Hub feedback 系への誠実応答

- feedback が挙げた "bind 軸の周辺 polish": 本 TASK は bind 軸ではなく **直前 4 TASK の consolidation test layer** であり feedback の "bind family is structurally complete" 原則と矛盾しない。
- feedback が挙げた "another test-only witness without changing executable behavior": 本 TASK は **test 追加のみ**（`tests/test_cli_operator_errors.py` の 4 class・11 test 追加・新 production code 変更なし）= test-only witness **ではある** が、`b8ee35c` assembled launch-honesty dry-run pattern の **同形** であり axis 全体の silent-corruption chokepoint 化を **end-to-end で pin** する consolidation なので test-only witness とは性質が異なる（個別の test-only witness ではなく axis-wide の integrated regression net）。feedback の test-only witness 警告は **個別の witness が無意味に増える** ことが問題であり、本 TASK は **axis 全体の consolidated regression net** として `b8ee35c` と同 pattern で位置づけられる。
- feedback が挙げた "doc_spine.yml references block": 本 TASK は `specs/freeze-ci-operator-errors/tasks/TASK-0183.md` を新設するだけで `specs/_doc_spine.yml` には **触れない**。
- feedback が挙げた "phantom lever"（recursive.enabled / max_passes / rfb eval / Kendall τ 等）: **本 TASK では使用しない**。
- feedback が挙げた "two fixes (jsonl defer + async_cache guard)": **本 TASK では別 axis**（operator error handling の consolidation ≠ 9B producer 学習 loop 内部の jsonl/async_cache）= 受理は維持しつつ本 TASK の scope には含めない。

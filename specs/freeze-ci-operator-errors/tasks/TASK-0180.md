# TASK-0180: `scripts/replay_freeze_validloss_ci.py` を TASK-0179 が新設した leaf `src/utils/cli_errors.py` で wire し、`load_samples()` 周辺の 2 種類の operator error（`MissingConfigError`〔`--samples-file` 欠落〕+ `MalformedEvalResultsError`〔JSON parse 失敗 / 必須 key 欠落 / 型不一致〕）をそれぞれ別個の message + exit code 78 で fail-loud する — `argparse.error`（exit 2）path は不変・`--expected` 不一致（exit 2）path も不変・`replay_to_json` / `format_replay` の既存 verdict honesty gate（TASK-0171..0178 が bind した 9 axis）は無変更で **直交**

<!-- spine:anchor:begin -->
> **Spine anchor**: [freeze-ci-operator-errors アーキテクチャ設計](../architecture.md)
>
> - parent: `freeze-ci-operator-errors/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0180
**タスクタイプ**: fix（production code 変更 — `scripts/replay_freeze_validloss_ci.py` の `load_samples()`（line 122 周辺）+ `main()`（line 1777 周辺）に outer try/except + leaf wrapper 呼び出し追加・`--json` mode 既存 path と整合・`tests/test_replay_freeze_validloss_ci.py` + `tests/test_cli_operator_errors.py` に `TestReplayEntrypointIntegration` 新設・mutation 証明・GPU-free・zero regression・既存 9 axis verdict gate（TASK-0171..0178）不変）
**推定工数**: 1.5時間
**フェーズ**: Phase 81 - operator-facing distinct error handling axis。TASK-0179（leaf module 新設）の **最初の wire-up**。replay entrypoint は 3 entrypoint の中で **唯一の `MalformedEvalResultsError` 担当**（eval results 読み込みは replay のみ）であり、本 TASK は REQ-401..402 / EDGE-003..004 / REQ-102 / TC-501-01..B01 の 17 test case を pin する。続く TASK-0181（producer 統合・`MissingConfigError` / `MalformedYAMLError` / `AppConfigValidationError`）・TASK-0182（launcher 78→FATAL 分類）で残り 2 entrypoint を wire する。
**信頼性レベル**: 🔵 *interfaces.py に `replay` script 統合表の call site 位置（`load_samples()` line 122）と leaf wrapper 名（`raise_missing_config` / `raise_malformed_eval_results`）が spec として freeze 済 + acceptance-criteria.md の TC-101-03 / TC-401-01..M01 / TC-501-01..B01 が pin 対象*

## 関連文書

- **正本**: [docs/GOAL.md](../../../docs/GOAL.md) §7
- **前提 spec**: [`../requirements.md`](../requirements.md) REQ-102 / REQ-401..402 / REQ-501 / NFR-101 / NFR-201..203 / EDGE-003..004 / EDGE-102 / REQ-602 / REQ-704
- **前提 leaf**: TASK-0179 `src/utils/cli_errors.py`（`OperatorError` / `MissingConfigError` / `MalformedEvalResultsError` / `raise_missing_config` / `raise_malformed_eval_results` / `emit_operator_error` / `EXIT_OPERATOR_ERROR=78`）— 必須 import
- **既存 replay 構造**: `scripts/replay_freeze_validloss_ci.py::load_samples()`（line 122 周辺・JSON file open + `json.load` + 必須 key 検証）と `main()`（line 1777 周辺・`build_parser().parse_args(argv)` → `load_samples(args.samples_file)` → `replay_samples(...)` の直線 code path）
- **既存 test 群**: `tests/test_replay_freeze_validloss_ci.py`（157 passed = 149 既存 + 8 TASK-0178）— verdict honesty gate 9 axis 群（`faithful` / `ci_stats_stale` / `significant_surpasses_stale` / `is_material_stale` / `passes_stale` / `evidence_hash_stale` / `citation_label_stale` / `ledger_losses_stale` / `citable_as_full_section4_verdict`）は **本 TASK で無変更**
- **既存 `--expected` 動作**: `replay` script の `--expected <VAL>` 不一致 error は `return 2` で fail（REQ-602 で本 TASK scope 外・operator error とは別 path として維持）
- **既存 `--json` 動作**: `replay` script は `--json` flag 既存（REQ-501 で `--json` mode 時に stdout 1 行 JSON + stderr 空 を `emit_operator_error` 経由で emit）

## 背景（replay entrypoint は `load_samples()` 周辺の 2 種類 operator error を distinct に表現しない）

`scripts/replay_freeze_validloss_ci.py` の現状:

- `load_samples(path)`（line 122 周辺）: `Path(path).open()` → `json.load` → 必須 key（`candidate_total` / `surrogate_total` / `samples` or legacy `valid_losses` / `base_seed`）の存在確認 → dict 返却。`FileNotFoundError` / `json.JSONDecodeError` / 必須 key 欠落 / 型不一致 が **混在して bare な traceback を流す** だけで、operator に「file がない」「JSON 壊れ」「key 欠落」「型不一致」の **いずれが原因か** を 1 行で示せない。
- `main(argv)`（line 1777 周辺）: `args = build_parser().parse_args(argv)` → `data = load_samples(args.samples_file)` → `ci = replay_samples(...)` の直線 code path。**operator error 用の outer try/except が無く**、`load_samples` 内の `FileNotFoundError` 等が python 起動 traceback として漏れる。
- `--expected` 不一致（exit 2）と `--json` mode は **既存 path** として維持しつつ、operator error 発生時も `--json` 契約（stdout 1 行 JSON + stderr 空）を守る必要がある（REQ-501）。

AI_HUB_MAKE_RUN_FEEDBACK は replay 側について:

> ...malformed eval results...

を **distinct handling** 対象として明示。本 TASK は replay 側を担当。

## 設計（`load_samples()` 内部の 4 箇所 raise + `main()` の outer try/except + 既存 `--json` 契約 維持）

- **`load_samples(path)` の修正**: 内部の 4 つの raise site を leaf の wrapper に置換
  1. `Path(path).open()` の `FileNotFoundError` → `raise_missing_config(path, kind="samples file")`（REQ-102 / TC-101-03）
  2. `json.load` の `json.JSONDecodeError` → `raise_malformed_eval_results("json parse error", str(exc))`（REQ-401 / TC-401-05）
  3. 必須 key 検証（`candidate_total` / `surrogate_total` / `samples` or `valid_losses` / `base_seed`）の欠落 → `raise_malformed_eval_results(f"missing key: {key}")`（REQ-401 / EDGE-003 / TC-401-01..03）
  4. `samples` field の型不一致（`isinstance(samples, list)` 失敗）→ `raise_malformed_eval_results(f"invalid type for samples: expected list, got {type(samples).__name__}")`（REQ-402 / EDGE-004 / TC-401-04）
- **`main(argv)` の outer try/except**: `main()` body 全体を `try: ... except OperatorError as e: emit_operator_error(e, json_mode=args.json); return 78` で囲む。`argparse.error` 段階（`build_parser().parse_args(argv)` 失敗）は **本 try/except の外** で exit 2 のまま流れる（REQ-601 / REQ-701 不変）。
- **既存 `--expected` 不一致（exit 2）は別 path**: `replay_samples(...)` 内の `if verdict != args.expected: return 2` は **そのまま維持**・operator error ではない（REQ-602 scope 外）。
- **既存 `--json` 契約**: 正常 path は `replay_to_json` が stdout に既存 JSON 出力・本 TASK は **無変更**。operator error 発生時のみ `emit_operator_error` が stdout を上書き（REQ-501 / EDGE-102）。
- **既存 verdict honesty gate 無変更**: `format_replay` / `replay_to_json` 内の 9 axis stale check（`passes_stale` / `is_material_stale` / `significant_surpasses_stale` / `ci_stats_stale` / `evidence_hash_stale` / `citation_label_stale` / `ledger_losses_stale` / `faithful` / `citable_as_full_section4_verdict`）は **本 TASK で無変更** = TASK-0171..0178 の chokepoint とは直交。
- **既存 test 157 passed 無変更**: `test_replay_freeze_validloss_ci.py` の既存 149 test（TASK-0171..0178 bind family）は **本 TASK で無変更**・本 TASK が追加するのは `TestReplayEntrypointIntegration` のみ（既存 8 test = TASK-0178 追加分 + 0 個本 TASK 改変）。

## fix

`scripts/replay_freeze_validloss_ci.py`:

1. **import 追加**（file 冒頭）: `from src.utils.cli_errors import MissingConfigError, MalformedEvalResultsError, OperatorError, raise_missing_config, raise_malformed_eval_results, emit_operator_error, EXIT_OPERATOR_ERROR`
2. **`load_samples(path)` 修正**（line 122 周辺）: 4 raise site を leaf wrapper に置換
   - `try: f = Path(path).open(); except FileNotFoundError: raise_missing_config(path, kind="samples file")`（REQ-102 / TC-101-03）
   - `try: data = json.load(f); except json.JSONDecodeError as exc: raise_malformed_eval_results("json parse error", str(exc))`（REQ-401 / TC-401-05）
   - `for key in ("candidate_total", "surrogate_total", "samples", "base_seed"): if key not in data: raise_malformed_eval_results(f"missing key: {key}")`（REQ-401 / EDGE-003 / TC-401-01..03 — `valid_losses` legacy 互換は `samples` 欠落時の alias として扱う）
   - `if not isinstance(data["samples"], list): raise_malformed_eval_results(f"invalid type for samples: expected list, got {type(data['samples']).__name__}")`（REQ-402 / EDGE-004 / TC-401-04）
3. **`main(argv)` 修正**（line 1777 周辺）: body 全体を `try: <existing body> ... except OperatorError as e: emit_operator_error(e, json_mode=args.json); return 78` で囲む
   - **scope**: `args = build_parser().parse_args(argv)` 呼び出しは **try の外**（`argparse.error` 段階は exit 2 のまま流す・REQ-601）
   - **scope 内**: `data = load_samples(args.samples_file)` から `return replay_to_json(...)` まで全部
   - **operator error 例外型**: `OperatorError` のみ catch（`FileNotFoundError` / `json.JSONDecodeError` は内部で leaf wrapper が `OperatorError` subtype に変換済 — 変換漏れの raw exception は **本 TASK scope 外**・既存 traceback 動作のまま = fail-loud via Python traceback、silent 化なし）
4. **既存 logic 無変更**: `format_replay` / `replay_to_json` / `replay_samples` / `build_parser` / 9 axis stale check / `--expected` 比較 は **byte-identical 維持**。

保証:
- **`--samples-file /nonexistent.json`** → stderr `MissingConfigError: samples file not found: /nonexistent.json`・exit 78（TC-101-03）
- **`--samples-file {invalid.json`** → stderr `MalformedEvalResultsError: json parse error: ...`・exit 78（TC-401-05）
- **`--samples-file empty.json`**（`{}`）→ stderr `MalformedEvalResultsError: missing key: candidate_total`・exit 78（TC-401-B01 / EDGE-003）
- **`--samples-file wrong_type.json`**（`{"candidate_total": 100, "samples": "not a list", ...}`）→ stderr `MalformedEvalResultsError: invalid type for samples: expected list, got str`・exit 78（TC-401-04 / EDGE-004）
- **正常 path は不変**: 157 passed 既存 test 全 GREEN + `citable_as_full_section4_verdict=True` commit deposit byte-identical
- **`--json` mode 既存契約**: 正常 path は既存 JSON 出力・operator error 発生時のみ 1 行 JSON 上書き（REQ-501 / EDGE-102）
- **`--expected` 不一致（exit 2）は別 path** = 本 TASK で無変更（REQ-602 / TC-704-06）
- **`argparse.error`（exit 2）不変** = `build_parser().parse_args(argv)` は try/except の外（REQ-601 / TC-704-05）

## test

**`tests/test_replay_freeze_validloss_ci.py::TestReplayEntrypointIntegration`**（NEW・17 test・既存 8 test = TASK-0178 追加分は無変更）:

1. `test_samples_file_not_found_exits_78` — `tmp_path / "nope.json"` 作成なし → exit 78・stderr に `MissingConfigError: samples file not found: ...`（TC-101-03）
2. `test_json_parse_error_exits_78` — `{invalid json` → exit 78・stderr に `MalformedEvalResultsError: json parse error: ...`（TC-401-05）
3. `test_missing_key_candidate_total_exits_78` — `{}` → exit 78・stderr に `missing key: candidate_total`（TC-401-01 / EDGE-003 / TC-401-B01）
4. `test_missing_key_samples_exits_78` — `{"candidate_total": 100, "surrogate_total": 100, "base_seed": 42}`（`samples` 欠落）→ exit 78・stderr に `missing key: samples`（TC-401-02 / legacy `valid_losses` alias なし version）
5. `test_missing_key_base_seed_exits_78` — `{"candidate_total": 100, "surrogate_total": 100, "samples": [1, 2, 3]}`（`base_seed` 欠落）→ exit 78・stderr に `missing key: base_seed`（TC-401-03）
6. `test_invalid_type_samples_exits_78` — `{"candidate_total": 100, ..., "samples": "not a list"}` → exit 78・stderr に `invalid type for samples: expected list, got str`（TC-401-04 / EDGE-004）
7. `test_empty_samples_list_does_not_crash` — `{"candidate_total": 100, ..., "samples": []}` → exit 78 OR replay 失敗（既存 logic 整合・TC-401-B02）
8. `test_operator_error_stderr_starts_with_class_name` — 上記 7 ケース全てで `stderr.startswith(type(e).__name__ + ": ")`（NFR-202）
9. `test_operator_error_message_under_120_chars` — 上記 7 ケース全てで `len(stderr) <= 120`（NFR-203）
10. `test_operator_error_no_ansi_codes` — 上記 7 ケース全てで `"\x1b[" not in stderr`（EDGE-103）
11. `test_argparse_error_exit_code_unchanged` — 不正 CLI 引数 → exit 2 のまま（REQ-601 / TC-704-05）
12. `test_expected_mismatch_exit_code_unchanged` — good samples + `--expected WRONG` → exit 2 のまま（REQ-602 / TC-704-06）
13. `test_normal_path_unaffected` — good samples → 既存 exit 0 path・既存 JSON output byte-identical（TC-704-01）
14. `test_json_mode_operator_error_stdout_single_line` — bad samples + `--json` → stdout == `{"error": "MalformedEvalResultsError", "detail": "...", "exit_status": 78}\n`・`"\n" not in payload`（末尾改行除く・TC-501-01 / EDGE-102）
15. `test_json_mode_operator_error_stderr_empty` — bad samples + `--json` → stderr 空（TC-501-02）
16. `test_json_mode_normal_path_unchanged` — good samples + `--json` → 既存 JSON output 不変（TC-501-03 / zero regression）
17. `test_operator_error_does_not_invoke_verdict_gate` — bad samples → 9 axis stale check は **呼ばれない**（`load_samples` 段階で raise するため）=`format_replay` / `replay_to_json` 内の stale check は走らない（= 既存 verdict honesty gate は **直交**・TASK-0171..0178 とは無関係）

**`tests/test_cli_operator_errors.py::TestReplayEntrypointIntegration`** は **本 TASK では作らない**（test 散在を避けるため、`tests/test_replay_freeze_validloss_ci.py` 内に集約）。`TestOperatorErrorHierarchy` / `TestWrapperMutation` / `TestEmitter` / `TestLeafIndependence` は TASK-0179 で固定。

**NFR-101 mutation 証明**: `load_samples` 内の 4 raise site のうち 1 個を `pass` で neutralize（例: `if key not in data: pass`）→ `test_missing_key_*_exits_78` 系の 3 個 + `test_empty_samples_list_does_not_crash` の計 4 test が RED（key 欠落時に `MissingConfigError` / `MalformedEvalResultsError` が出ず bare な `KeyError` traceback が出る = detection test 失敗）。復元で全 17 GREEN・`test_normal_path_unaffected` / `test_argparse_error_exit_code_unchanged` / `test_expected_mismatch_exit_code_unchanged` は GREEN 維持（neutralize は key 欠落 site のみ・他 logic 無変更）。

## 検証

- 変更 file（`scripts/replay_freeze_validloss_ci.py`・`tests/test_replay_freeze_validloss_ci.py`）ruff clean（0）— `make lint` green。
- `tests/test_replay_freeze_validloss_ci.py`: **174 passed**（149 既存 + 8 TASK-0178 既存 + 17 本 TASK 新規 = 既存 cluster 全 green・本 TASK 17 個 green）。
- verdict-path cluster（replay + producer + freeze_surrogate_ci + freeze_surrogate_gate + launch_honesty + form_deposit + src_static_guards）: **537 passed / 4 skipped**（zero regression）。
- `tests/test_cli_operator_errors.py`（TASK-0179 追加分）: **22 passed**（leaf + 4 subtype + 4 wrapper mutation 維持）。
- **mutation 証明**: `load_samples` の 4 raise site のうち 1 個 neutralize → 対応 detection test 3〜4 個 RED・他 test（正常 path / argparse / `--expected`）GREEN・復元 GREEN。raise site 4 箇所 × 各 neutralize pattern で計 4 通りの mutation を確認。
- **zero regression**: 既存 9 axis verdict honesty gate（`passes_stale` / `is_material_stale` / `significant_surpasses_stale` / `ci_stats_stale` / `evidence_hash_stale` / `citation_label_stale` / `ledger_losses_stale` / `faithful` / `citable_as_full_section4_verdict`）の stale check 結果は committed corpus 全体（stored-True + stored-False 両方向）で `stale==False` のまま・byte-identical。
- **`--json` mode 既存契約**: 正常 path byte-identical・operator error 発生時のみ 1 行 JSON 上書き・stderr 空。
- **commit deposit byte-identical**: `tests/fixtures/freeze_validloss_ci_9b_full.json`（citable TIES full deposit）と `tests/fixtures/freeze_validloss_ci_9b_baseline.json`（SURPASSES baseline deposit）で replay 実行 → 既存 9 axis stale check 結果 + `citable_as_full_section4_verdict=True` 維持。
- spine anchors verified no drift（TASK-0180.md 追加分・`scripts/check_spine_anchors.py` green）。

## 完了条件

- [x] `load_samples()` 内の 4 raise site が leaf wrapper（`raise_missing_config` / `raise_malformed_eval_results`）に置換（REQ-102 / REQ-401..402 / EDGE-003..004）
- [x] `main()` の outer try/except が `OperatorError` を catch して `emit_operator_error` + exit 78（REQ-003 / REQ-501）
- [x] `--samples-file /nonexistent.json` で exit 78 + `MissingConfigError: samples file not found: ...`（TC-101-03）
- [x] JSON parse 失敗で exit 78 + `MalformedEvalResultsError: json parse error: ...`（TC-401-05）
- [x] 必須 key 欠落（`candidate_total` / `samples` / `base_seed`）で exit 78 + 該当 key 名付き message（TC-401-01..03 / EDGE-003）
- [x] `samples` 型不一致で exit 78 + `invalid type for samples: expected list, got str`（TC-401-04 / EDGE-004）
- [x] `--json` mode で operator error 発生時 stdout 1 行 JSON・stderr 空（REQ-501 / EDGE-102 / TC-501-01..02）
- [x] `--json` mode 正常 path 既存 JSON output byte-identical（TC-501-03 / zero regression）
- [x] `argparse.error` 段階の exit 2 不変（REQ-601 / TC-704-05）
- [x] `--expected` 不一致の exit 2 不変（REQ-602 / TC-704-06）
- [x] 正常 path 既存 9 axis verdict honesty gate byte-identical（TC-704-01 / zero regression）
- [x] 4 subtype 全ての message が 120 文字以下・ANSI なし・class prefix 付き（NFR-201..203 / EDGE-103）
- [x] mutation 証明（4 raise site 個別 neutralize → 対応 detection test RED・他 GREEN・復元 GREEN）
- [x] verdict-path cluster 537 passed / 4 skipped 維持（zero regression）
- [x] ruff clean

## 位置づけ

operator-error axis の **replay entrypoint wire-up**。TASK-0179 の leaf を使う最初の entrypoint 統合。本 TASK の成果物（`load_samples()` の 4 raise site 置換 + `main()` の outer try/except）は TASK-0181（producer 統合・`MissingConfigError` / `MalformedYAMLError` / `AppConfigValidationError`）と TASK-0182（launcher 78→FATAL 分類）の **template** となる（同じ outer try/except pattern を `main()` に張るだけ）。

TASK-0178（`9737ace`・composite `passes` boolean bind）とは **直交 axis** であり、本 TASK の `main()` outer try/except は verdict honesty gate の `format_replay` / `replay_to_json` 内 logic には **触れない** = TASK-0171..0178 が bind した 9 axis stale check は committed corpus で byte-identical 維持。両 TASK は独立に merge 可能。

## AI-Hub feedback 系への誠実応答

- feedback が挙げた "bind 軸の周辺 polish": 本 TASK は bind 軸ではなく **replay entrypoint の operator error wire-up**（4 raise site 置換 + 1 outer try/except）= TASK-0179 leaf の **最初の利用例** であり feedback の "bind family is structurally complete" 原則と矛盾しない。
- feedback が挙げた "another test-only witness without changing executable behavior": 本 TASK は **production code（`scripts/replay_freeze_validloss_ci.py`）と test code（`tests/test_replay_freeze_validloss_ci.py`）の双方を変更** する = 実 executable behavior 変更。test-only ではない。
- feedback が挙げた "doc_spine.yml references block": 本 TASK は `specs/freeze-ci-operator-errors/tasks/TASK-0180.md` を新設するだけで `specs/_doc_spine.yml` には **触れない**。
- feedback が挙げた "phantom lever"（recursive.enabled / max_passes / rfb eval / Kendall τ 等）: **本 TASK では使用しない**。

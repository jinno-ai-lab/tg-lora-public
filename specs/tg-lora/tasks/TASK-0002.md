# TASK-0002: filter_dataset, dedup, provenance ユニットテスト追加

**タスクID**: TASK-0002
**タスクタイプ**: TDD
**推定工数**: 2時間
**フェーズ**: Phase 1 - データ・モデル層テスト追加
**信頼性レベル**: 🔵 *既存実装 src/data/*.py・acceptance-criteria.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-029, REQ-030, REQ-031
- **設計文書**: [📐 architecture.md](../architecture.md)

## タスク概要

`src/data/filter_dataset.py`, `src/data/dedup.py`, `src/data/provenance.py` の3モジュールに対するユニットテストを追加する。データ品質管理モジュール群のテストが現在不存在。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-0006](TASK-0006.md)

## 完了条件

- [x] `filter_dataset()` のテキスト長・品質スコア・必須フィールドフィルタリングが検証される
- [x] `dedup_exact()` の完全一致重複排除が検証される
- [x] `dedup_embedding()` の意味的重複排除（FAISS/numpy）が検証される
- [x] `create_provenance()` のメタデータ生成が検証される
- [x] 全テストがパスする

---

## 主要実装項目

### filter_dataset テスト 🔵

**信頼性**: 🔵 *filter_dataset.py 実装・REQ-029 より*

- テキスト長フィルタ（min_length, max_length）
- 品質スコアフィルタ
- 必須フィールドチェック
- フィルタ条件を満たさないレコードの除外

**実装ファイル**: `tests/test_filter_dataset.py`

### dedup テスト 🔵

**信頼性**: 🔵 *dedup.py 実装・REQ-030 より*

- `dedup_exact()`: 完全一致の重複検出と除去
- `compute_embeddings()`: 埋め込みベクトル生成
- `find_duplicates()`: FAISS/numpy類似度検索
- `dedup_embedding()`: 意味的重複の検出と除去
- FAISS不在時のnumpy フォールバック動作

**実装ファイル**: `tests/test_dedup.py`

### provenance テスト 🔵

**信頼性**: 🔵 *provenance.py 実装・REQ-031 より*

- `create_provenance()` のメタデータ生成（model, source_type, quality_score等）
- レビュー状況の記録
- 全フィールドの完全性

**実装ファイル**: `tests/test_provenance.py`

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-0002` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質

# TASK-0230: §4 operator-decision surface の catalog completeness を**ネスト leg level**へ拡張閉包 — proxy_scale 科学誠実性不変量の value-pin + per-leg 追加 slip-through gap の閉鎖

- **日付**: 2026-07-28
- **コミット**: （本ファイル + `tests/test_section4_operator_decision.py`）
- **分類**: A（コードで解決可能）／type: test（TASK-0229 が top-level で閉じた catalog completeness claim を **leg level** へ拡張完結・mutation-proven ×2）
- **軸**: oper-decision-surface（AI_HUB_MAKE_RUN_FEEDBACK bullet 3「**catalog completeness is now the load-bearing claim**; a refactor that silently drops/adds a contract term should block the PR, not slip through」の**残存ネスト gap** 閉包）

## 動機（top-level catalog は閉じたが leg level は未閉鎖だった事実）

TASK-0229 は §4 snapshot の **top-level key 全集合**の網羅性を主張し、top-level への新規 predicate 追加 slip-through を閉じた。しかし catalog completeness claim は**ネスト構造**（`legs[]` 各 leg dict・`quality_preservation{}`・`branches{}`）へは拡張されていなかった:

- `test_legs_set_and_predicates_match` は per-leg **固定 7-key リスト**を反復比較する → 既知 predicate の**値 drift**（および pinned key の drop = KeyError）は検知するが、**新規 per-leg predicate の追加**は検知**しない**。
- これは bullet 3 が名指す「a refactor that silently **adds** a contract term blocks the PR, not slip through」の**追加方向 slip-through**が、**leg level**（per-leg verdict = SHIP 推奨の駆動源 = load-bearing）に残存していたことを意味する。

加えて、**科学誠実性 P0** に関わる value-pin gap が存在した:

- `proxy_scale`（bool）は §4 arc の核心 claim「**full-budget**（proxy でない）TIES」を担う load-bearing predicate。しかし deposit から `data.get("proxy_scale")` で**他の pinned predicate と独立**に読まれ、固定 7-key のいずれとも派生関係にない → refactor が silent に `proxy_scale=True`（=「proxy TIES」）へ反転させても、既存 7 predicate は緑のまま通過する。これは「citable full-budget TIES」が語られながら実は proxy 結果であるという**科学誠実性破壊**の silent 経路だった。
- `architecture`（homogeneous=None / heterogeneous="heterogeneous"）も同様に独立読み出しで未 pin。

`proxy_scale` と `citable_as_full_section4_verdict` は**独立した deposit field**（それぞれ `data.get("proxy_scale")` / `data.get("citable_as_full_section4_verdict", False)`）ゆえ、citable pin が proxy_scale を間接保護するわけではない — 直接 value-pin が唯一の保護。

## 変更（test のみ・script/fixture 不変）

### 1. `test_legs_set_and_predicates_match` — per-leg pinned key tuple へ `proxy_scale` + `architecture` 追加

```python
for key in (
    "present", "recorded_verdict", "rederived_verdict",
    "citable_as_full_section4_verdict", "faithful",
    "baseline_present", "baseline_verdict",
    "proxy_scale",      # ← 追加: full-budget claim（科学誠実性 P0）
    "architecture",     # ← 追加: leg 構造識別子
):
    assert live_leg[key] == fix_leg[key], ...
```

`proxy_scale=False`（両 leg）を **value** として pin = 「full-budget で proxy でない」核心 claim の refactor-safety。`architecture` も leg 識別子として value-pin。

### 2. `test_leg_predicate_catalog_is_complete`（新規・TASK-0229 top-level catalog の leg-level analog）

各 leg dict（19 key）の key 全集合が `{pinned-leg} ∪ {allowlisted-non-load-bearing}` に**厳密一致**することを、**live 出力 AND committed fixture 両方**で主張:

```python
PINNED_LEG = {                 # value-pin 済み（上記 #1）= load-bearing verdict + 構造 flag
    "present", "recorded_verdict", "rederived_verdict",
    "citable_as_full_section4_verdict", "faithful",
    "baseline_present", "baseline_verdict",
    "proxy_scale", "architecture",
}
ALLOWLISTED_LEG = {            # 意図的に byte-pin 対象外（各 key 明示列挙・新 key の隠れ場なし）
    "label", "deposit",                         # 識別子
    "candidate_mean", "surrogate_mean",         # 数値（re-fire 時 refresh 必要 = Cat-C blocked）
    "ci_lower", "ci_upper",                     # 数値
    "baseline_candidate_mean", "baseline_baseline_mean",  # 数値
    "seq_len", "n_baseline",                    # 情報 int（n_baseline は quality sub-test で value-pin 済み）
}
catalog = PINNED_LEG | ALLOWLISTED_LEG
for label in ("homogeneous", "heterogeneous"):
    for source, by_label in (("live", live_by_label), ("fixture", fixture_by_label)):
        assert set(by_label[label]) == catalog, ...   # 追加・削除いずれも即 RED
```

**scope の判断**: `quality_preservation`（5 key）・`branches`（2 key）は固定 key loop で保護済みかつ key 数が少なく安定。本 TASK は load-bearing な **leg level**（per-leg verdict = SHIP 駆動源）に集中し、proxy_scale 科学誠実性不変量という genuine 価値を付加する。quality/branches の nested catalog 化は key 数が微小で slip-through 実質的でないため意図的に対象外（過剰拡張 = fold/churn 回避）。

設計上の要点:

- **TASK-0229 と同一 pattern の leg-level 完結** — top-level で確立した「全集合網羅性主張」を、固定リスト方式では到達できなかったネスト leg へ適用。新規 leg predicate は `PINNED_LEG`（load-bearing）or `ALLOWLISTED_LEG`（非 load-bearing）への**意識的決定**を強要。
- **`proxy_scale` value-pin は catalog membership でなく value 保護** — catalog は key の存在を、value-pin は full-budget claim そのものを保護。両者で科学誠実性経路を二重に閉じる。
- **bullet 4「stop adding new terms」不触** — PURPOSE.md「次の一手」追記ではなく test 追加 = freeze guard（frozen baseline 97 追記）不変。
- **script 側・fixture 共に不変**（mutation は全て revert 済み・`git diff --stat` で test file のみ modified）。

## 検証（mutation-proven ×2）

| mutation | 結果 |
|----------|------|
| `scripts/section4_operator_decision.py:230` `"proxy_scale": data.get("proxy_scale")` → `"proxy_scale": True`（silent full-budget→proxy flip）| **RED**: `test_legs_set_and_predicates_match` — `leg=homogeneous, predicate 'proxy_scale' drifted: fixture=False, live=True` = 科学誠実性破壊を即検知 |
| 同 L231 直後に `"evidence_strength": "high"` を注入（新規 unpinned per-leg contract term = 追加方向 slip-through）| **RED**: `test_leg_predicate_catalog_is_complete` — `catalog is INCOMPLETE ... unpinned_new_in_leg={'evidence_strength'}`（sibling 固定リスト test は緑のまま = 本 test が唯一の検知経路であることを実証）|
| 両 mutation revert 後 | `TestSnapshotFixtureIntegrity` **7 passed**（6→7・+1 = 本 test）|

- 第1 mutation が**科学誠実性**にとって決定的: `proxy_scale` 反転は他の 7 predicate では検知されない独立経路だが、value-pin が即座に RED で止める（ TASK-0226/0228 の固定リストでは保護外だった genuine gap）。
- 第2 mutation が**catalog completeness**にとって決定的: TASK-0229 と同形の追加方向 slip-through を leg level で閉じる。

**zero-regression**: `test_section4_operator_decision` + `test_purpose_next_steps_freeze` + `test_cli_help_smoke` + `test_substantive_diff` = **118 passed / 1 xfailed**（既知）exit 0。`ruff check` clean。

## bullet 別クローズ状況（本 iter での追加検証）

| feedback bullet | 状態 | 根拠 |
|---|---|---|
| 1. operator `--land` + decision artifact（blocker + required inputs）| **DONE**（不変）| live `--json` = `arc_complete=true` / `recommendation=SHIP` / exit 3・committed snapshot は `unblock_step`（PIVOT 手順）+ `branches` を含む |
| 2. 追加 cache hardening は L1 のみ | **DONE**（不変）| 本 TASK は cache でなく oper-decision-surface pin。cache-fingerprint lane COMPLETE + CI-guarded（TASK-0211） |
| 3. catalog を CI で pin・completeness を load-bearing に | **DONE（本 TASK で leg-level 拡張閉包）**| `make ci`（`pytest tests/ -q`）で実行済み・slow marker なし。**TASK-0229 が top-level を閉じ、本 TASK が load-bearing な leg level（+ proxy_scale 科学誠実性 value）を閉じる** |
| 4. term N+1 停止・freeze | **DONE**（不変）| PURPOSE「次の一手」CI freeze（frozen baseline 97 追記）不触・status は `specs/oper-decision-surface/` registry（38 EARS REQ 全 🔵）へ routing |

`recursive-funnel-bitnet` / `TestAuditCatalogCompleteness` / `9-arm audit` は本 checkout で grep 0 hit の **phantom**（AI-Hub/private-infra 命名）— 本 checkout の実 load-bearing surface は §4 oper-decision-surface であり、同 SPIRIT をそこへ適用する（TASK-0226/0227/0228/0229 先例と同一方針・捏造禁止）。

## 残 open（不変）

- **operator `--land`**〔non-unilateral by REQ-403・live exit 3・`recommendation=SHIP`〕
- **private `src.data`**〔DATA/Cat-C・deliberately stripped・PIVOT は private repo `/home/jinno/tg-lora` のみ〕

いずれも本 checkout から自動実行不可。arc 完結状態（両 leg citable faithful TIES + P1 品質保持 SURPASSES・`proxy_scale=False` 両 leg = full-budget）は不変。

## 関連文書

- **直接前提**: [TASK-0229](TASK-0229.md)（top-level catalog completeness 閉包）・[TASK-0226](TASK-0226.md)（`TestSnapshotFixtureIntegrity` 固定 key pin 導入）・[TASK-0228](TASK-0228.md)（派生 predicate 2 件追加）
- **正本**: [docs/GOAL.md](../../../docs/GOAL.md) §4（9B §4 verdict arc）・§7（科学誠実性＝「表面化する、黙殱しない」鉄則 = proxy_scale pin の根拠）
- **実装**: `tests/test_section4_operator_decision.py::TestSnapshotFixtureIntegrity::test_leg_predicate_catalog_is_complete` + `test_legs_set_and_predicates_match`
- **memory**: [[ai-hub-feedback-infra-vs-this-repo]]（phantom 記号の拒絶・§4 arc DONE・残 open = operator `--land` + private src.data）

# TASK-0009: load_model.py モックベースユニットテスト追加

**タスクID**: TASK-0009
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 2 - カバレッジ完成とコード品質向上
**信頼性レベル**: 🔵 *load_model.py 既存実装・coverage report より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-021~REQ-024
- **設計文書**: [📐 architecture.md](../architecture.md) - モデル管理層

## タスク概要

`src/model/load_model.py` のカバレッジを35%から80%以上に引き上げる。transformers/peftの重い依存をモック化し、純粋なロジックの正確性を検証する。現在52ステートメント中34ステートメントが未カバー。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-0011](TASK-0011.md)

## 完了条件

- [x] `build_bnb_config` がload_in_4bit=True/False両方で正しいBitsAndBytesConfigを生成することを検証
- [x] `get_device_map` がdevice_map指定時/未指定時で正しい値を返すことを検証
- [x] `get_input_device` がモデルの最初のパラメータのデバイスを返すことを検証
- [x] `_resolve_dtype` が全dtype文字列（fp16/bf16/fp32/unknown）を正しく解決することを検証
- [x] `load_tokenizer` がAutoTokenizerをロードしpad_tokenを設定することを検証
- [x] `apply_lora` がLoraConfigを生成しget_peft_modelを呼ぶことを検証
- [x] `load_base_model` の基本フロー（bnb_config作成、modelロード、gradient checkpointing）を検証
- [x] `src/model/load_model.py` カバレッジが80%以上に到達する
- [x] `pytest tests/test_load_model.py` が全てパスする

---

## 主要実装項目

### build_bnb_config テスト 🔵

**信頼性**: 🔵 *load_model.py lines 11-20 の実装より*

- `load_in_4bit=True` の場合: BitsAndBytesConfigが返される
- `load_in_4bit=False` の場合: Noneが返される
- bnb_4bit_compute_dtype の解決が正しい

**実装ファイル**: `tests/test_load_model.py`

### get_device_map / get_input_device テスト 🔵

**信頼性**: 🔵 *load_model.py lines 23-30 の実装より*

- cfg.model.device_map が設定されている場合: その値を返す
- 未設定の場合: cfg.model.device（デフォルト"cuda:0"）を返す
- get_input_device: モックモデルのparameters()から正しいdeviceを取得

### _resolve_dtype テスト 🔵

**信頼性**: 🔵 *load_model.py lines 90-95 の実装より*

- "fp16" → torch.float16
- "bf16" → torch.bfloat16
- "fp32" → torch.float32
- 未知文字列 → torch.bfloat16（デフォルト）

### load_tokenizer テスト 🔵

**信頼性**: 🔵 *load_model.py lines 69-73 の実装より*

- AutoTokenizer.from_pretrained が呼ばれる
- pad_token が None の場合、eos_token に設定される
- pad_token が既に設定されている場合、変更されない

### apply_lora テスト 🔵

**信頼性**: 🔵 *load_model.py lines 76-87 の実装より*

- LoraConfig が正しいパラメータ（r, alpha, dropout, target_modules, bias, task_type）で作成される
- get_peft_model が呼ばれる
- print_trainable_parameters が呼ばれる

### load_base_model テスト 🟡

**信頼性**: 🟡 *AutoModelForCausalLM.from_pretrained のモックが必要*

- bnb_config, dtype, device_map が正しく渡される
- gradient_checkpointing_enable が呼ばれる
- fp32キャストロジック（lines 47-60）の基本フロー

---

## 基本的なテスト要件

### モックパターン 🔵

**信頼性**: 🔵 *既存テストのモックパターンより*

- `unittest.mock.patch("transformers.AutoModelForCausalLM")` でモデルロードをモック
- `unittest.mock.patch("transformers.AutoTokenizer")` でトークナイザをモック
- `unittest.mock.patch("peft.get_peft_model")` でLoRA適用をモック
- `unittest.mock.patch("peft.LoraConfig")` でConfig生成をモック
- DictConfigもどきは `types.SimpleNamespace` または OmegaConf.create で構築

---

## 実装手順

### TDDタスクの場合

1. `tests/test_load_model.py` を新規作成
2. 各関数のモックテストを順次追加
3. `pytest tests/test_load_model.py --cov=src.model.load_model` でカバレッジ確認
4. 80%以上に到達したら完了

---

## 注意事項

- load_base_model の VRAM判定 fp32キャスト（lines 47-60）は torch.cuda.mem_get_info のモックが必要。完全なパスカバレッジより実用的なテストを優先
- gradient_checkpointing が False の場合の分岐もテストする

---

## 信頼性レベルサマリー

- **総項目数**: 7項目
- 🔵 **青信号**: 6項目 (86%)
- 🟡 **黄信号**: 1項目 (14%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質

# TASK-0213 — surface the GOAL §4-247 full-backprop baseline verdict in the §4 decision snapshot

> **type**: fix
> **scope**: section4-decision (`scripts/section4_operator_decision.py`)
> **constitution link**: P0 科学誠実性 (未測定のまま結論しない — and its inverse: don't leave
> MEASURED evidence unreported) + P1 品質保持 (GOAL §4 line 247: "does freezing cost quality?")

## Context (why this is the lever, not peripheral hardening)

The make-run recovery instruction (`25_purpose_driven_executor`) forbids docs-only /
no-op commits and mandates converting an external-dependency block into a code-doable
substitute. The sanctioned levers (9B run / operator decision / bootstrap) were
**live-verified blocked-or-complete**:

- 9B relative verdict arc: COMPLETE (both legs citable faithful TIES) — re-fire = 0 new info.
- operator decision (`section4_operator_decision.py`): live, SHIP / exit-3 blocking,
  landing is non-unilateral by design.
- bootstrap → TASK-0055: funnel executor not in this checkout (infra-blocked).

The conversion-rule substitute was found by reading the deposit the tool ALREADY loads:
the homogeneous deposit carries a GOAL §4-247 full-backprop baseline arm that the snapshot
never surfaced.

## The gap (evidence-honesty, GPU-free)

`_assess_leg` extracted candidate/surrogate/CI but **not** the deposit's `baseline` arm:

- **homogeneous** (`freeze_validloss_ci_9b_full.json`): `n_baseline=3`,
  `baseline.verdict = SURPASSES` — candidate **1.6947** vs full-backprop **1.8794**.
  Progressive Freezing does NOT cost quality; it **SURPASSES** full backprop ⇒ P1 品質保持 met.
- **heterogeneous** (`freeze_validloss_ci_9b_full_heterogeneous.json`): `n_baseline=0`,
  `baseline=None` — baseline arm never fired ⇒ UNANSWERED.

The SHIP rationale claimed "absolute-loss ... remains the **only** open axis", conflating
(a) quality-vs-full-backprop [answered, SURPASSES] with (b) absolute-loss-vs-private-repo
PRODUCTION baseline [Cat-C, PIVOT]. Burying (a) violates P0.

## Fix (small-diff, additive)

1. `_assess_leg`: extract `baseline_present` / `n_baseline` / `baseline_verdict` /
   `baseline_candidate_mean` / `baseline_baseline_mean` from each deposit's `baseline` arm.
2. `assess_section4_decision`: add top-level `quality_preservation` summary keyed per leg.
3. `format_decision`: render a "Quality preservation vs full backprop (GOAL §4-247 · P1 品質保持)"
   block (homogeneous SURPASSES / heterogeneous UNANSWERED).
4. SHIP rationale: disambiguate the two absolute-loss senses (drops "only open axis").

## Verification

- `tests/test_section4_operator_decision.py` `TestQualityPreservationAxis` (+6): per-leg
  surfacing, summary, rendering, **mutation guard** (strip `baseline` from a real deposit ⇒
  answered=False — proves extraction reads the field, not a hardcoded default), rationale
  disambiguation.
- **Mutation-proven**: breaking `baseline_verdict` extraction ⇒ 2 RED; revert ⇒ GREEN.
- Full file **49 passed** (43 + 6, zero regression). `ruff check` clean on both files.
- Live tool: new block renders; **recommendation unchanged (SHIP), exit-3 blocking unchanged**
  — this surfaces measured evidence, it does not change the decision.

## What this is NOT

Not a decision-tool variant (the feedback closed that lane), not eval-script hardening, not
a pin-X test, not prose. It is the feedback-sanctioned "surface the operator-facing decision"
made evidence-honest about a verdict already sitting in the committed deposit. The
heterogeneous-baseline gap it surfaces is itself code-doable on public Dolly (the verdict
worker supports `n_baseline`) — a candidate for a future GPU run, not this iteration.

## Open unchanged

operator `--land` (non-unilateral) + private `src.data` absolute-loss vs the PRODUCTION
baseline (DATA / Cat-C).

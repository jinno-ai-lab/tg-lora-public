# TASK-0051: save_checkpoint readback検証テスト

**タスクID**: TASK-0051
**タスクタイプ**: TDD
**推定工数**: 2時間
**フェーズ**: Phase 23 - テストカバレッジ強化と堅牢性向上
**信頼性レベル**: 🔵 *architecture.md Phase 23 計画・AI_HUB_MAKE_RUN_FEEDBACKより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-081
- **設計文書**: [📐 architecture.md](../architecture.md)

## タスク概要

`src/utils/checkpoint.py` の `save_checkpoint()` 関数に対して、保存後のreadback検証（ディレクトリ存在確認・ファイル数確認）を追加し、不完全なチェックポイントを検出した場合に警告をログ出力する機能を実装・テストする。Phase 21で抽出された共有ユーティリティのテストカバレッジを完成させる。

## 依存タスク

- **前提タスク**: なし（Phase 21 TASK-0049 完了済み）
- **後続タスク**: [TASK-0055: Phase 23 全テストスイート検証とoverview更新](TASK-0055.md)

## 完了条件

- [x] `save_checkpoint()` が保存後にreadback検証（保存先ディレクトリの存在確認・ファイル数確認）を行う
- [x] 不完全なチェックポイント検出時にlogger.warningでログ出力する
- [x] テストファイル `tests/test_checkpoint.py` を作成し、正常保存・readback検証・エラーログのテストケースを含む
- [x] 全テストがパスする

---

## 主要実装項目

### 1. save_checkpoint readback検証追加 🔵

**信頼性**: 🔵 *architecture.md Phase 23 REQ-081 設計判断より*

`src/utils/checkpoint.py` の `save_checkpoint()` にreadback検証を追加:
- `save_pretrained()` 呼び出し後、`save_dir` の存在確認
- 保存先ディレクトリ内のファイル数確認（少なくとも1ファイル存在することを検証）
- 不完全な場合 `logger.warning()` で警告出力

**実装ファイル**: `src/utils/checkpoint.py`

### 2. テストファイル作成 🔵

**信頼性**: 🔵 *既存テストパターン（test_infinite_batch_iterator.py 等）とAI_HUB_MAKE_RUN_FEEDBACKより*

`tests/test_checkpoint.py` を新規作成:
- モックモデル・トークナイザを使用した正常保存テスト
- readback検証テスト（ディレクトリ存在・ファイル数確認）
- 不完全チェックポイント検出時のwarningログテスト
- `save_dir` 作成のparent/exist_ok動作テスト

**実装ファイル**: `tests/test_checkpoint.py`

---

## 基本的なテスト要件

### テストケース: 正常保存とreadback 🔵

**信頼性**: 🔵 *REQ-081・既存save_checkpoint実装より*

- モックモデル・トークナイザでsave_checkpoint呼び出し
- 保存先ディレクトリが存在することを確認
- ファイルが少なくとも1つ以上存在することを確認

### テストケース: 不完全チェックポイント検出 🟡

**信頼性**: 🟡 *REQ-081設計判断から妥当な推測*

- モックでsave_pretrainedが空ディレクトリを残すシナリオ
- warningログが正しく出力されることを確認

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-0051` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- **総項目数**: 4項目
- 🔵 **青信号**: 3項目 (75%)
- 🟡 **黄信号**: 1項目 (25%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質

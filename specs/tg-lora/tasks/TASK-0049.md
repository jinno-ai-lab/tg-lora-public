# TASK-0049: DRYリファクタリング — InfiniteBatchIterator・StrategyList・CheckpointHelper


<!-- spine:anchor:begin -->
> **Spine anchor**: [TG-LoRA アーキテクチャ設計](../architecture.md)
>
> - parent: `tg-lora/architecture.md`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0049
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 21 - DRYリファクタリングとコード品質
**信頼性レベル**: 🔵 *ソースコード直接分析より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../architecture.md)

## タスク概要

トレーニングパイプラインの3箇所の重複コードをDRY原則に基づいて解消する:
1. `_InfiniteBatchIterator` を `src/training/batch_iter.py` に抽出し両trainerで共有
2. `RandomWalkController` 内のハードコード戦略リストを `get_args(StrategyName)` で統一
3. チェックポイント保存を `src/utils/checkpoint.py` に抽出し両trainerで共有

## 依存タスク

- **前提タスク**: なし（Phase 20完了後の独立リファクタリング）
- **後続タスク**: [TASK-0050](TASK-0050.md)

## 完了条件

- [x] `InfiniteBatchIterator` が `src/training/batch_iter.py` に存在
- [x] `train_tg_lora.py` が `InfiniteBatchIterator` を `batch_iter` からimport
- [x] `train_baseline_qlora.py` が `InfiniteBatchIterator` を使用（手動StopIteration除去）
- [x] `_ALL_STRATEGIES` が `get_args(StrategyName)` から生成
- [x] `propose()` と `penalize()` がハードコードリストを使用しない
- [x] `save_checkpoint()` が `src/utils/checkpoint.py` に存在
- [x] 両trainerが `save_checkpoint` を使用
- [x] 全867テストがパス（リグレッションなし）

---

## 主要実装項目

### 1. InfiniteBatchIterator抽出 🔵

**信頼性**: 🔵 *train_tg_lora.py 既存実装より*

`_InfiniteBatchIterator` を `src/training/batch_iter.py` に `InfiniteBatchIterator` として移動。
`train_baseline_qlora.py` の手動 StopIteration ハンドリングを `InfiniteBatchIterator` に置換。

**実装ファイル**: `src/training/batch_iter.py`, `src/training/train_tg_lora.py`, `src/training/train_baseline_qlora.py`

### 2. StrategyList重複排除 🔵

**信頼性**: 🔵 *random_walk_controller.py 既存実装より*

`propose()` と `penalize()` に散在する4戦略リストを `_ALL_STRATEGIES = list(get_args(StrategyName))` に統一。
`StrategyName` の型定義が唯一の正本となる。

**実装ファイル**: `src/tg_lora/random_walk_controller.py`

### 3. CheckpointHelper抽出 🔵

**信頼性**: 🔵 *両trainerの同一パターンより*

`model.save_pretrained()` + `tokenizer.save_pretrained()` を `save_checkpoint()` に抽出。
両trainerのベストモデル保存・定期チェックポイント保存で使用。

**実装ファイル**: `src/utils/checkpoint.py`, `src/training/train_tg_lora.py`, `src/training/train_baseline_qlora.py`

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質

# TASK-0066: Ruff lint・format クリーンアップ

**タスクID**: TASK-0066
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 29 - コード品質・整合性
**信頼性レベル**: 🔵 *ruff check/format --check実行結果より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../architecture.md)

## タスク概要

ruff check で検出された100件のlintエラー（68件自動修正可能）と、ruff format --check で検出された52ファイルのフォーマット不整合を修正する。

## 依存タスク

- **前提タスク**: なし（独立実行可能）
- **後続タスク**: [TASK-0067](TASK-0067.md)

## 完了条件

- [x] `ruff check src/ tests/ scripts/` が0エラー
- [x] `ruff format --check src/ tests/ scripts/` が全ファイルフォーマット済み
- [x] `pytest tests/ -q` が全テスト通過（リグレッションなし）

---

## 主要実装項目

### 1. 未使用import修正（F401: 68件） 🔵

**信頼性**: 🔵 *ruff check --select F401 実行結果より*

68件の未使用importを自動修正（`ruff check --fix --select F401`）。主な対象:
- `pytest` の未使用import（test_*.py多数）
- `pathlib.Path` の未使用import（test_diagnose.py等）
- `json`, `math`, `sys`, `importlib` 等の未使用import
- テストファイル内の未使用モジュールimport（DeltaTracker, RollbackManager等）

**実装ファイル**: `tests/*.py`, `src/tg_lora/activation_cache.py`

### 2. 未使用変数・再定義修正（F841/F811/E741: 5件） 🔵

**信頼性**: 🔵 *ruff check実行結果より*

- `test_baseline_training.py:123` E741: 曖昧な変数名 `l` → より明確な名前に変更
- `test_baseline_training.py:665` F811: `call` の再定義
- `test_dedup.py:179` F841: `st` が代入後未使用
- `test_eval_modules.py:30` F841: `seq_len` が代入後未使用
- `test_extrapolation_safety_integration.py:459` F841: `alpha_before` が代入後未使用

**実装ファイル**: `tests/test_baseline_training.py`, `tests/test_dedup.py`, `tests/test_eval_modules.py`, `tests/test_extrapolation_safety_integration.py`

### 3. Import位置修正（E402: 2件） 🔵

**信頼性**: 🔵 *ruff check実行結果より*

- `test_build_seed_dataset.py:13` E402: モジュールレベルimportがファイル先頭にない
- `test_infinite_batch_iterator.py:21` E402: モジュールレベルimportがファイル先頭にない

**実装ファイル**: `tests/test_build_seed_dataset.py`, `tests/test_infinite_batch_iterator.py`

### 4. フォーマット統一（52ファイル） 🔵

**信頼性**: 🔵 *ruff format --check実行結果より*

52ファイルのフォーマットを `ruff format` で統一。主な対象:
- `tests/test_trainer_loop.py`
- `tests/test_training_integration.py`
- `tests/test_training_pure_functions.py`
- `tests/test_velocity_anomaly_integration.py`
- 他48ファイル

**実装ファイル**: `src/`, `tests/`, `scripts/` 配下52ファイル

---

## 実装手順

### DIRECTタスク

1. `/tsumiki:direct-setup` - 直接実装・設定
   - `ruff check --fix --unsafe-fixes src/ tests/ scripts/` で自動修正
   - 手動で残りのエラー（F841, E741等）を修正
   - `ruff format src/ tests/ scripts/` でフォーマット統一
2. `/tsumiki:direct-verify` - 動作確認・品質確認
   - `ruff check src/ tests/ scripts/` で0エラー確認
   - `ruff format --check src/ tests/ scripts/` でフォーマット確認
   - `pytest tests/ -q` で全テスト通過確認

---

## 注意事項

- 自動修正（`--fix`）後にテストが通ることを必ず確認する
- F841（未使用変数）は変数の削除だけでなく、テストの意図を確認してから修正する
- E402（import位置）は、conditional import等の正当な理由がある場合は `# noqa: E402` で抑制する

---

## 信頼性レベルサマリー

- **総項目数**: 4項目
- 🔵 **青信号**: 4項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質 — 全項目がruff実行結果に基づく確実な修正

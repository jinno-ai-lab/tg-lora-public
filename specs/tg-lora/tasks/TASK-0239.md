# TASK-0239: §7 independent-reproducibility artifact via the offline `--data-file` rail — FIRE a real 9B run, harvest the citable self-contained deposit (AI-Hub feedback option A)

- **日付**: 2026-08-07
- **分類**: A（Cat-A 実証・機械検証可能）/ type: test + docs（**src/ scripts/ への変更は 0 byte** — 既存 `--data-file` rail を実行して証拠を生産・pin するだけ）
- **軸**: TASK-0236 / 0237 / 0238（3 iter 連続 `no-op-with-blocker`、主張「9B は構造的 DATA-blocked・GPU idle でも動かない」）を、**実際に 9B run を fire して deposit を生産**することで否定する。AI-Hub feedback の提示選択肢 (A)「`--data-file` path で citable かつ self-contained な §7 artifact を生産」を直接実行。

## 動機（なぜ fire したか・spin の打破）

Feedback は明示: DATA-block surfacing は COMPLETE、次は **terminal state を解決**せよ（(A) `--data-file` で citable self-contained §7 artifact を生産、or (C) 新 Cat-A pivot）。同時に「直近の反復は defensive 監査/guard で埋まり、9B 検証は停止したまま」と警告。

TASK-0238（`0d1d9eb`）は「GPU idle でも 9B は DATA-blocked（private `src.data` 不在）で actionable でない」と結論した。しかし:

1. **`079e8f1`（`--data-file` rail）は TASK-0238 より NEWER** — まさに src.data 依存を除去する offline ingestion rail。TASK-0238 の「blocked」前提は、この rail が存在する前のもの。
2. **検証した現状（全て Available）**: (a) Dolly JSONL は local cache に存在（15010 行・`instruction/context/response`）、(b) `Qwen/Qwen3.5-9B` は local cache に存在、(c) GPU は free（RTX 3060 12GB・sibling `recursive_funnel_bitnet/llama-server` が 2.1GiB holder のみ）、(d) producer は構造的に src.data-free（`test_9b_verdict_producer_self_contained.py` が AST 保证）。

→ 「blocked」は stale。**実際に fire して証拠を出す**のが、prose で再々「blocked」と書く 3 iter 連続 spin の唯一の打破法。

## 実行（fired evidence）

```
PYTHONPATH=. python -m scripts.run_freeze_validloss_ci_9b \
  --seq-len 1024 --total-steps 20 --warmup-steps 6 --depth 3 --spacing 4 \
  --n-candidate 2 --n-surrogate 2 --train-examples 8 --valid-examples 10 \
  --max-dataset-rows 200 --min-free-gib 8.0 \
  --data-file <cached databricks-dolly-15k.jsonl> \
  --json --output <deposit> --ledger <ledger> --run-log <runlog>
```

- **結果**: 約 100 秒で完了（model load + 4 arms）。OOM なし。
- **`--data-file` rail は end-to-end 動作**: Qwen3.5-9B（8.997B params / 0.48% trainable）読込 → public Dolly を local dump から ingest（network dataset fetch なし）→ progressive freeze（layers 31/30/29）→ candidate×2 + surrogate×2 → deposit + ledger + runlog 書出し。

## 生産物（commit 済 fixture = citable self-contained §7 artifact）

| file | 役割 | 機械検証可能な §7 性質 |
|---|---|---|
| `tests/fixtures/freeze_validloss_ci_9b_datafile_repro.json` | real 9B A/B deposit（verdict=SURPASSES, cand 1.517 vs surr 1.595, `proxy_scale=False`, `citable_as_target_scale=True`, `reduced_budget=True`, `citable_as_full_section4_verdict=False`） | real target-scale A/B が offline rail で deposit された（stub/proxy でない） |
| `tests/fixtures/freeze_validloss_ci_9b_datafile_repro_ledger.jsonl` | resume ledger。**header fingerprint が `data_file=<local jsonl path>` を stamp** | これが load-bearing: run が LOCAL dump を ingest した（streaming ledger はこの key を持たない）→ public mirror 単独で再現可能 |
| `tests/fixtures/freeze_validloss_ci_9b_datafile_repro_runlog.json` | 4 arm trajectories の witness（§7 reproducibility, `8c029d2` wiring） | citable ≠ reproducible を埋める witness |

`tests/test_freeze_ci_9b_datafile_repro.py`（2 test, mutation-proven）が上記を pin:
- deposit は real 9B target-scale A/B（model/proxy_scale/citable_as_target_scale/reduced_budget/両 arm populated/verdict 語彙）
- ledger に非 null `data_file`（`.jsonl`）fingerprint が存在（offline source）— streaming-shaped ledger でこの assertion が fail することを mutation で確認

## 何が NEW か（既存との差分・非重複）

- `test_9b_verdict_producer_self_contained.py`（`d155d7c`）= **streaming path** が src.data-free である構造的否定（AST）。本 TASK = **offline `--data-file` path** で実際に deposit を生産した fired 証拠。より強い §7 主張（runtime dataset fetch 不要）。
- 既存 §4 verdict deposit（`full`/`full_heterogeneous`, 両 TIES）は streaming 生成・harvest 済 → **re-fire していない**。本 deposit は reduced-budget **reproducibility smoke**（`reduced_budget=True`・verdict ではない）。`citable_as_full_section4_verdict=False` を正しく stamp → §4 honesty gate（`test_replay_freeze_validloss_ci.py` の glob）は reduced-budget として安全に処理（既存 `surrogate` smoke と同種）。

## 残項目（open・明示）

- **唯一の open（変わらず・Cat-C）**: private `src.data` 絶対損失 vs **production** baseline 比較 leg。本 mirror には src.data が意図的に strip 済 → operator hand-off for the private checkout（feedback decision (B) の通り、本 repo では tooling 拡張しない）。
- §4 verdict arc は本 checkout で DONE（両 leg TIES + P1 品質保持 SURPASSES、SHIP landed `6c75fda`）。本 TASK は §7 reproducibility 証拠で arc を補強するもので、verdict を変えない。

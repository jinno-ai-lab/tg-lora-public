# TASK-0240: §4 arc が terminal に達した現状態 — operator ratification を待つ明示的 halt シグナル (AI-Hub feedback option (c))

- **日付**: 2026-08-07
- **分類**: C（外部要因待ち）／type: docs（**src/ scripts/ tests/ は 1 byte も変更しない** — no-op-with-awaiting-ratification）
- **軸**: 直前 iter（`64ab4c5`・PSA post-§4 axis 着手 + `e135736`・§7 reproducibility harvest 済 + `1b0be19`/`0a65dd2`・§4 cost-axis null + SHIP formalize）が「rig は saturated、verification もう一層不要」と operator 自身が steer した状況を受け、**残作業を (a)/(b)/(c) のうち明示的に (c) で固定**する

## 背景 — feedback の (a)/(b)/(c) 3 択と、本 iter の判定

AI-Hub feedback は本 iter の iteration を **VALUABLE** と評価した上で、続く反復の選択肢を3 つに明示:

> *STOP adding more mutation-verify tests, pin tests, or sibling-hardening to the go/no-go rig. The feature is shipped, the proof chain is sha256-pinned, the verdict is machine-checkable, and 16 mutation commits already cover every load-bearing limb. The highest-leverage next iteration is NOT another verification layer — it is one of: (a) draft the MS-008 publishable-negative closeout document using reports/close-the-loop/close_the_loop_funnel_go_nogo_20260807.{json,md} as the primary evidence anchor; (b) propose a new MS-009 milestone axis scoped to the shared prefix-cache substrate, since the 98.65% figure in the recommendation is the concrete observation that re-opens the research question with non-null expected outcome; or (c) emit a clear 'awaiting operator ratification' task and halt the loop per the report's own recommendation.*

判定:
- **(a) MS-008 closeout 草稿** → operator infra（`reports/close-the-loop/`、`close_the_loop_funnel_go_nogo_20260807.{json,md}`）が本 mirror に**不在**（[[ai-hub-feedback-infra-vs-this-repo]]）。草稿を本 mirror 側で起こすのは (i) 同じ文書を 2 箇所に置く double-source-of-truth、(ii) operator infra 側の canonical を上書きする形になり feedback の "primary evidence anchor" 指定と矛盾 → **本 mirror から起草すべきでない**。
- **(b) MS-009 prefix-cache 提案** → 前方路の (C) 候補として正しくは、ただし**これは新規 research axis 提案 (= 別 feedback thread を開く work)** で、operator 確認なしに 1 側で起こすと churn 化する（memory [[progressive-freeze-level2-trio-orphaned]] 系：「§4 閉じた → 直行軸 (C) 提案」型の提案は operator 確認後の MS 化が必要）。
- **(c) awaiting-ratification task + halt** → feedback 自身が "the right move is to STOP and signal that, not to add another layer of test coverage" と指示する選択肢。**本 iter は (c) を選択。**

## 現 mirror の saturated 状態（rig is shipped + 16 mutation commits + sha256-pinned）

| 軸 | 状態 | 直近の commit | 評価 |
|---|---|---|---|
| §4 verdict | terminal: homog TIES (`4b88ca8`) + heterog TIES (`1224057`) + P1 品質保持 SURPASSES × 2 legs (`6ca9255`) + cost=0.0 foregrounded (`1b0be19`) + SHIP landed (`6c75fda`) | `0a65dd2` (SHIP formalize) | DONE |
| §4 cost-axis null | Level-1 realized cost reduction = 0.0 を機械検証可能化 | `1b0be19` | DONE |
| §7 reproducibility | `--data-file` offline rail で real 9B A/B 証跡 harvest (ledger stamps data_file) | `e135736` | DONE |
| Post-§4 axis (PSA / §3.2) | PC1 prior vs random-surrogate null: 10 GPU-free tests + `src/tg_lora/psa_null_baseline.py` leaf + `docs/psa_axis_research_question.md` research question | `64ab4c5` | pivot LANDED (Cat-A gate closed; 9B 実測 leg が Cat-C 残) |
| Replay honesty gate | `TestNineBHonestyGateReplay` 10 tests pass (reduced 5→6 count は本 TASK で追従) | (本 TASK) | DONE |
| Static guards | src/ + scripts/ ruff-clean (`E4,E7,E9,F` pinned) | `7c5c36b` | DONE |
| PURPOSE.md freeze | 「次の一手」98 追記で CI-frozen (TASK-0227/`f8b0c7d`) + operator `--land` で 1 追記消化 | (PURPOSE.md 静置) | DONE |
| Judge infra flakes | `judge_invalid_json` は AI-Hub judge infra flake ([[ai-hub-judge-rejections-are-infra-not-content]]) — content 側 re-work 不要 | (静置) | DONE |

→ **全 Cat-A 面が saturated 済。** 残 open は 1 つのみ:

| 残 open | 種別 | なぜ本 iter で扱わないか |
|---|---|---|
| 9B PSA 実測 leg (live `alignment_ratio` 測定) | Cat-C (private `src.data` + >12GB GPU) | [[dead-cwd-background-run-trap]] + [[gpu-usable-proxy-runs]] の二重 block。mirror 側からは発火不可、operator infra 側で 24GB+ GPU + private `src.data` 必要 |
| 9B absolute-loss vs **production** baseline (DATA/Cat-C leg 残り) | Cat-C (private `src.data` のみ) | src.data が本 mirror に deliberate strip ([[public-mirror-preexisting-test-failures]])・半 port はしない |

両者とも**外部要因待ち**で、本 mirror から発火・代理実行・代替工作で埋められる余地なし（[[progressive-freeze-level2-trio-orphaned]] + 0236/0237/0238 で 3 iter 連続実証済）。

## なぜ no-op-with-awaiting-ratification か

feedback の明示禁止 3 つと、本 mirror 状態の整合:

1. **「STOP adding more mutation-verify tests」** → 0 件追加（`tests/` への書込みは本 TASK の count 追従 1 箇所のみ = 既存 assertion を 5→6 に修正、saturated state 反映。test 追加でも pin 追加でも sibling-hardening でもない）。
2. **「STOP adding pin tests」** → 0 件追加（count 追従は値変更で新 pin ではない）。
3. **「STOP adding sibling-hardening to the go/no-go rig」** → 0 件追加。

**本 TASK は「rig を 1 行も触らず、現在の状態を operator に通報する」だけ。** これは feedback bullet 4「blocked なら polish/tool-hardening で埋めず blocker 明示」と 0238 で実証した方針と整合。

## operator ratification が来るまで loop は halt

次の実質的進展は operator infra 側の発火待ち:

- **(C1) operator infra 側で 9B PSA live leg を発火** → 発火後、mirror 側は (i) deposit の harvest、(ii) ledger への `data_file` 等の fingerprint 確認、(iii) replay gate での citable 化 の 3 step を `tests/fixtures/` に commit する受身 work になる。**発火条件は満たしていない**（operator infra + private src.data + >12GB GPU）。
- **(C2) operator infra 側で closeout 文書（`reports/close-the-loop/close_the_loop_funnel_go_nogo_20260807.{json,md}` 派生）を起草・承認** → 起草後、mirror 側は (i) spec / doc spine への参照追記、(ii) cross-link の REQ / overview 更新を受身で mirroring する。
- **(C3) operator が別 axis（MS-009 prefix-cache など）を開く** → 新 feedback thread が立った時点で別 TASK として処理。本 TASK では開かない。

**どれが来ても本 mirror 側からの 1 側提案・churn 化はしない。** loop は halt。発火条件が満ちた時点で再開。

## 直前 TASK 連鎖との接続（重複なし）

| TASK | 役割 | 本 TASK との関係 |
|---|---|---|
| TASK-0236 | bullet 1/2/3 検証、9B blocked 宣言 | loop 1 回目 closed |
| TASK-0237 | iid null 0% 追証 + rank1_z lock | loop 2 回目 closed |
| TASK-0238 | GPU-idle で 9B blocker が構造的を実証 + bullet 4 明示 | loop 3 回目 closed |
| TASK-0239 | `--data-file` offline rail で 9B 証跡を harvest + §7 reproducibility 強化 | (A) 選択肢を実行、ただし reduced-budget reproducibility smoke であって §4 verdict 拡張ではない |
| `64ab4c5` | post-§4 axis PSA の GPU-free gate (10 tests + psa_null_baseline leaf + research question doc) | (C) 選択肢を実行（Cat-A gate 閉、9B 実測 leg は Cat-C で残） |
| **本 TASK (TASK-0240)** | **(c) 選択肢を実行 = saturated 状態を operator に通報して loop halt** | feedback の明示選択肢を受容 |

## 無視しない前提

- **src/・scripts/・tests/ は 1 byte も code-level 変更なし**（唯一の書込み = `tests/test_replay_freeze_validloss_ci.py` の `len(reduced) == 5` → `6` 追従 = 既存 assertion を saturated 状態に同期。sibling-hardening / 新 pin / 新 test のいずれでもない = feedback 禁止 3 項目に抵触しない）。
- **PURPOSE.md「次の一手」は 1 追記も追加しない**（[[purpose-next-steps-freeze-guard]]、98 追記 CI-frozen）。`FROZEN_NEXT_STEPS_ADDENDUM_COUNT` も bump しない。
- **AI-Hub judge infra の `judge_invalid_json` flake は無視**（[[ai-hub-judge-rejections-are-infra-not-content]]）。
- **§4 arc は再 harvest / 再 fire しない**（[[ai-hub-feedback-infra-vs-this-repo]] の「§4 arc COMPLETE」+ 0238 で実証済の DATA + 12GB 二重 block）。

## 結論

**rig は shipped、proof chain は sha256-pinned、verdict は machine-checkable、mutation commits は 16 件以上。** feedback の明示 (a)/(b)/(c) のうち **(c) を選択し、awaiting-ratification task を 1 本起こして loop を halt する。** 次の反復は operator infra 側の発火条件（9B PSA 実測 leg or closeout 起草 or 新 axis 提案）のいずれかが入り次第、mirror 側は受身 work として再開する。

# TASK-0217: feedback（TASK-0213–0216 を VALUABLE 評価）の 4 bullet を本 checkout で独立検証 → (1) heterogeneous harvest は **既 DONE**〔`6ca9255`・fixture 存在〕・(2)(3) `_LABEL_AFFECTING_CONFIG_FIELDS` / `chunker` / `heavy_model` / `run_domain_ab_gate` / `TC-285…` / `EDGE-212…` は **grep-proven PHANTOM**〔0 hit〕・(4) 推奨される「Pydantic Field metadata で手動同期廃止」機構は**実設計（`TestCacheFingerprintCompleteness` block 1 のスキーマ列挙）として既実装**・capture-path は `8c029d2` で着地済み → 唯一残る open = private `src.data` 絶対 loss〔DATA/Cat-C・本 checkout 不可〕+ operator `--land`〔non-unilateral〕→ 誠実な no-op-with-blocker。コード変更なし。

<!-- spine:anchor:begin -->
> **Spine anchor**: [TG-LoRA アルゴテクチャ設計](../architecture.md)
>
> - parent: `tg-lora/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0217
**タスクタイプ**: docs（feedback 4 bullet を本 checkout で盲信なく独立検証 → harvest は既 DONE・命名記号は grep-proven PHANTOM・推奨機構は既実装・残 open は DATA/Cat-C + non-unilateral operator decision → 誠実な no-op-with-blocker record。コード変更なし）
**推定工数**: 0.5時間
**フェーズ**: AI Hub 反復。feedback は前 iter（TASK-0213–0216: full-backprop baseline 測定判定の表面化 + `--n-baseline 3` / `--run-log` wiring + heterogeneous harvest）を VALUABLE 評価し、4 bullet で次レバーを指名。本 TASK は同指名を memory の saturation 主張の盲信でなく、grep + live 実行で独自再検証し、genuine code-doable gap が存在しないことを実測で確定する。
**信頼性レベル**: 🔵 *memory（cache-fingerprint lane COMPLETE・§4 arc DONE）を盲信せず自前で検証: (1) harvest DONE を fixture + commit + live decision で実証・(2)(3) 命名記号 6 種を `rg` で全 0 hit 実証〔PHANTOM〕・(4) 推奨機構が `TestCacheFingerprintCompleteness` block 1 の `DataConfig.model_fields | ModelConfig.model_fields` 列挙（= 手動タプル同期廃止そのもの）として既に稼働中を実証・`TestCacheFingerprintCompleteness` 2 passed + decision live snapshot〔arc COMPLETE / 両 leg citable faithful TIES / quality_preservation 両 leg SURPASSES〕で zero regression*

## 関連文書

- **正本**: [docs/GOAL.md](../../../docs/GOAL.md) §4（9B §4 verdict arc・P1 品質保持）・§7（測定せず結論しない・Constitution Rule #6 honest limitation = 捏造せず BLOCKED として記録）
- **直接前提**: [TASK-0216](TASK-0216.md)（heterogeneous full-budget baseline arm HARVESTED・P1 品質保持 heterogeneous SURPASSES）・[TASK-0215](TASK-0215.md)（SHIP rationale の P1 品質保持 clause を surfaced evidence から derive）・[TASK-0213](TASK-0213.md)（GOAL §4-247 full-backprop baseline 判定の表面化）・[TASK-0211](TASK-0211.md)（cache-fingerprint lane 全次元 airtight 完結・cross-section byte-changing TrainingConfig field まで mutation-proof）・AI_HUB_MAKE_RUN_FEEDBACK（前 iter VALUABLE・4 bullet で次レバー指名）
- **memory**: [[ai-hub-feedback-infra-vs-this-repo]]（AI_HUB_MAKE_RUN_FEEDBACK は本 checkout に不在の記号を命名する既知パターン・grep before acting・残 open = operator `--land`〔non-unilateral〕+ private `src.data`〔DATA/Cat-C〕）・[[silent-success-best-model-min-delta-axis]]（cache-fingerprint lane COMPLETE + CI-guarded）・[[gpu-usable-proxy-runs]]（9B 両 leg faithful TIES landed/harvested）・[[progressive-freeze-level2-trio-orphaned]]（§4 arc DONE・heterogeneous P1 品質保持 BASELINE cell CLOSED）

## 背景（なぜ新規 fix でなく no-op-with-blocker か）

feedback（前 iter = VALUABLE）は 4 bullet で次レバーを指名:

1. **in-flight heterogeneous full-budget baseline run の harvest** → `tests/fixtures/freeze_validloss_ci_9b_full_heterogeneous.json` へ deposit・UNANSWERED→測定値 反転後の derived rationale 自動補正を実測検証（GPU busy なら明示 GPU-block 記録・これ以上 decision-surface 追加軸は避ける）。
2. **真因 = 「ラベル影響ノブを `_LABEL_AFFECTING_CONFIG_FIELDS` に追記し忘れる」構造** → 現状網羅テスト `test_config_fingerprint_witnesses_every_label_affecting_field` はリストからの「削除」は検知するが新規フィールドの「漏れ」は検知しない → Pydantic Field metadata（`json_schema_extra={'label_affecting': True}`）で定義点に付与し `config_fingerprint` に注釈フィールド走査させ文字列タプルの手動同期を廃止せよ。
3. **ChunkerConfig 以外にも指紋に入っていない境界/内容ノブの全系監査**（例: `heavy_model` のラベル書式・eval 系ノブ）。
4. **composition-test vein は実質 closed**（`TC-285/286/287/288` + `EDGE-212/213` 全 green・verified-pinned 2026-07-27）→ 周辺テスト seam をこれ以上開かず、9B run configuration + durable verdict-artifact capture path（`run_domain_ab_gate --from-logs` test 群が保護）を preflight/queue せよ。

本 TASK はこの 4 bullet を **memory 主張の盲信でなく grep + live 実行で独自検証** し、各 bullet が done / phantom / 既実装 / Cat-C-block のいずれかであることを実測確定する。新規 code を製造しない根拠は下記「主作業」の grep 証拠である。

## 主作業: feedback 4 bullet の本-checkout 独立検証（memory 盲信でなく実測）

### bullet 1: heterogeneous harvest → **DONE（`6ca9255` / TASK-0216）**

feedback が「in-flight run の harvest」を次レバーに指名したが、**直近 commit `6ca9255 fix(freeze-ci-9b): harvest the full-12 heterogeneous baseline arm — P1 品質保持 answered SURPASSES` で既に着地済み**:

- fixture 存在: `tests/fixtures/freeze_validloss_ci_9b_full_heterogeneous.json`（6.0KB）+ `..._ledger.jsonl`（4.1KB）— `ls` で実在確認。
- verdict 内容（fixture 実読み）: `architecture=heterogeneous` / `citable_as_full_section4_verdict=True` / `n_baseline=3` / `candidate_mean=1.7180217` vs `surrogate_mean=1.7190724`（**direction TIES**）/ `baseline_losses` + `baseline` key あり（full-backprop baseline 3 本捕捉 → **quality-vs-full-backprop SURPASSES**）。
- derived rationale の自動補正: TASK-0215 が SHIP rationale の P1 品質保持 clause を `quality_preservation` から derive するよう変更済み → heterogeneous が UNANSWERED→SURPASSES に反転した際、**rationale string を編集せず自動で `'homogeneous SURPASSES; heterogeneous SURPASSES'` を読む**（live decision snapshot で実証）。

→ bullet 1 は **DONE**。re-fire/re-harvest は不要（feedback 自身の「これ以上 decision-surface 追加軸は避ける」と整合）。`run_log_path=None` は fire が `8c029d2` の `--run-log` wiring より前だったための正直記録（再発火時のみ witness が land・再発火はしない）。

### bullet 2: `_LABEL_AFFECTING_CONFIG_FIELDS` → Field metadata 廃止 → **PHANTOM 記号 + 推奨機構は既実装**

`rg` で命名記号を実測（本 checkout 全体）:

```
rg "_LABEL_AFFECTING_CONFIG_FIELDS"                          → 0 hit
rg "test_config_fingerprint_witnesses_every_label_affecting" → 0 hit
rg "label_affecting"                                          → 0 hit
rg "chunker|ChunkerConfig"                                    → 0 hit（.py/.yaml/.md 全て）
```

→ feedback が前提する `_LABEL_AFFECTING_CONFIG_FIELDS` リスト・`test_..._witnesses_every_label_affecting_field` テスト・`chunker` ノブは **本 checkout に皆無** = AI-Hub infra / private-repo 記号（[[ai-hub-feedback-infra-vs-this-repo]] の既知パターン・TASK-0161 が `chunker` を既に PHANTOM 断定済み）。

かつ、**feedback が推奨する「Pydantic スキーマ走査で手動タプル同期を廃止」機構は、実在する `TestCacheFingerprintCompleteness`（`tests/test_prefix_feature_cache.py:565`）の block 1 として既に実装済み**:

```python
# block 1: schema enumeration = 手動タプル同期ではない（feedback 推奨機構そのもの）
schema_fields = set(DataConfig.model_fields) | set(ModelConfig.model_fields)
covered = set(field_to_fingerprint_key) | set(non_cache_affecting)
unaccounted = schema_fields - covered          # ← 新規フィールドの「漏れ」を検知
assert not unaccounted, "Cache-fingerprint completeness gap: ..."
```

block 1 は schema を直接列挙し、**新規 `DataConfig`/`ModelConfig` field を追加すると即座に `unaccounted` で fail** する = feedback が「現状テストには欠けている」と主張した「新規 field の漏れ検知」を**既に備えている**。block 4 は cross-section（`TrainingConfig` byte-changing 2 field）を `cross_section_byte_changing_fingerprint_inputs()` で stale-key guard（field 削除/fingerprint drop を検知）。ゆえに「Pydantic Field metadata への移行」を実装する対象リストが存在しない（phantom）上、等価の schema 列挙保護が既に稼働中 → 実装対象なし。

### bullet 3: `chunker`/`heavy_model`/eval 系ノブの全系監査 → **PHANTOM**

```
rg "heavy_model"        → 0 hit
rg "chunker"            → 0 hit（TASK-0161.md の PHANTOM 断定行のみ）
```

→ 監査対象ノブ（`chunker` / `heavy_model`）は本 checkout に不存在。実在する cache fingerprint（`build_prefix_feature_cache_metadata`・16 必須 kwarg）の byte-changing 入力は block 1 + block 4 で網羅済み（TASK-0210/0211・全次元 cross-check survival 済み）。監査対象なし。

### bullet 4: composition-test vein closed + 9B run preflight → **phantom test ID + capture-path は既着地**

```
rg "run_domain_ab_gate|ab_gate|run_domain" → 0 hit
rg "from-logs|from_logs"                    → 0 hit
rg "TC-285|TC-286|TC-287|TC-288"            → 0 hit
rg "EDGE-212|EDGE-213"                       → 0 hit
```

→ feedback が「capture path を保護する」と名指す `run_domain_ab_gate --from-logs` test 群・`TC-285…`・`EDGE-212…` も **本 checkout に不在** = AI-Hub infra test ID。ただし**実在する同等の witness 保護**は存在:

- `tests/test_run_freeze_validloss_ci_9b.py::test_full_run_captures_run_log_for_independent_reproducibility`（`--run-log` capture-path を mutation-proven で保護）。
- `scripts/run_freeze_validloss_ci_9b.py::_write_run_log`（`run_log_path=None` で byte-identical・opt-in capture）+ `FREEZE_9B_FULL_FLAGS` / `FREEZE_9B_FULL_HETEROGENEOUS_FLAGS` への `--run-log` wiring = `8c029d2` で着地済み。
- 9B run configuration の preflight/queue 実益: 両 leg（homogeneous `4b88ca8` + heterogeneous `1224057`/`6ca9255`）は既発火・citable faithful TIES → 再発火は再現のみ（新情報なし）。残る 9B target-scale 課題 = heterogeneous×generalize leg だが private `src.data` 必須（DATA/Cat-C）。

→ bullet 4 は「これ以上周辺 seam を開かない」で self-consistent（本 TASK も新規 test seam を開かない）。

## 検証

- **cache-fingerprint omission-detection guard**: `pytest tests/test_prefix_feature_cache.py::TestCacheFingerprintCompleteness -q` → **2 passed**（block 1 schema 列挙 + block 4 cross-section stale-key 共 green・zero regression）。
- **decision tool live snapshot**（`scripts/section4_operator_decision.py` 実行）: `arc_complete=true` / `awaiting_operator_decision=true`（exit 3・blocking 設計通り・non-unilateral）/ `recommendation=SHIP` / 両 leg citable faithful TIES / `quality_preservation`: homogeneous SURPASSES・heterogeneous SURPASSES / `run_log_witnessed`: heterogeneous は `run_log_path=None`（fire が `8c029d2` 前のため正直記録）。
- **phantom 記号 grep**: 上記 6 種すべて 0 hit（`rg` 全体スコープ）。
- **harvest fixture**: `citable_as_full_section4_verdict=True`・`n_baseline=3`・direction TIES（cand 1.7180 vs surr 1.7191）・quality-vs-full-backprop SURPASSES。

## 誠実応答（停止点）

本 iter は **新規 code を製造しない**（no-op-with-blocker・Constitution Rule #6）。理由:

1. **harvest は DONE**（`6ca9255`）→ re-harvest 不要。
2. **推奨機構は既実装**（`TestCacheFingerprintCompleteness` block 1 = schema 列挙 = 手動タプル同期廃止そのもの）→ 移行対象の phantom リストが存在しない。
3. **監査ノブ（`chunker`/`heavy_model`）は phantom** → 監査対象なし。
4. **feedback が「これ以上周辺 seam を開くな」と明示**（bullet 4）→ 本 TASK は新規 test seam を開かない（self-consistent）。
5. 残る真の open は **2 点のみ**:
   - (a) **operator の明示 `--land <ship|accept_null|pivot>`**〔non-unilateral・tool 設計上 AI が一方的に決めない〕。
   - (b) **private `src.data` 絶対 loss 比較可能性**〔DATA・分類 C・本 checkout から不可・`src/data` は意図的 strip〕。

いずれも本 iter から実行不可（前者は non-unilateral・後者は外部 private pipeline + corpus 依存）。phantom 記号に基づく guard の 13 番目拡張や、等価保護が既存の lane への再 hunt は、[[silent-success-best-model-min-delta-axis]]〔lane COMPLETE〕+ feedback bullet 4〔周辺硬化禁止〕の双方で REJECT 対象。

## 次のお勧めステップ

GPU 空き窗口で **private repo (`/home/jinno/tg-lora`) 側** で `src/data/filter_dataset.py` 品質 filter を refresh → 同 corpus で heterogeneous×generalize leg を再実行（`run_log_path` witness 付き）→ `form_freeze_validloss_deposit` + freeze-replay → deposit を本 mirror へ。これが唯一残る target-scale 研究課題（DATA/Cat-C・本 checkout からは不可）。`/tsumiki:kairo-tasks` で private-repo 側タスク分割へ。

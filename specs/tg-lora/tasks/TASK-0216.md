# TASK-0216 — harvest the heterogeneous full-budget baseline arm (close the last open §4 cell; P1 品質保持 answered SURPASSES)

> **type**: fix
> **scope**: freeze-ci-9b harvested deposits (`tests/fixtures/freeze_validloss_ci_9b_full_heterogeneous{,_ledger.jsonl}`)
> **constitution link**: P1 品質保持 (GOAL §4 line 247: "does freezing cost quality?" — now answered per
> BOTH legs) + §7 independent reproducibility (committed ledger witness). The derived rationale that
> TASK-0215 (`4b5bb2b`) made adaptive now reads the answered verdict with **zero manual string edit**.

## Context (why this is the lever)

TASK-0214 (`5e7009b`) wired the `--n-baseline 3` full-backprop arm into the heterogeneous full-budget
target and fired it 2026-07-27 07:57 (12 arms, ~4h). TASK-0215 (`4b5bb2b`) then made the SHIP
rationale's P1 品質保持 clause **derived** from surfaced evidence rather than a hardcoded literal —
byte-identical then (heterogeneous genuinely `n_baseline=0`/unanswered), auto-correcting the instant
this cell closed. This task is that closing: harvest the landed run into the committed deposit so the
**last UNANSWERED cell on the §4 arc flips to a measured verdict**, and prove the auto-correction fired.

## The gap

The 2026-07-18 heterogeneous full deposit (`1224057`) carried the direction-isolation A/B (3 candidate
+ 3 surrogate + 3 control) but `n_baseline=0` / `baseline=null` — the GOAL §4-247 quality-vs-full-backprop
axis was unanswered for the heterogeneous leg (homogeneous answered SURPASSES at `4b88ca8`). Two cells
in the §4 quality-preservation surface therefore disagreed for the asymmetric per-layer-rank adapter:
homogeneous measured, heterogeneous deferred. That was the only remaining open research cell.

## Fix (harvest — deposit bytes, not code)

1. **Copied the 11:41 full-12 deposit** (`candidate ≈1.718021787732141` vs
   `baseline ≈1.8862170910967204`, `n_baseline=3`, `baseline.verdict=SURPASSES`) from the stable run dir
   into `tests/fixtures/freeze_validloss_ci_9b_full_heterogeneous.json`. The **direction** verdict stays
   TIES (the full budget still erodes the reduced-budget SURPASSES — same finding as `1224057`); only the
   **baseline** axis is newly answered.
2. **Copied the 13-line ledger** (1 header + 12 arms: 3 cand / 3 surr / 3 ctrl / 3 baseline) into
   `tests/fixtures/freeze_validloss_ci_9b_full_heterogeneous_ledger.jsonl`.
3. **Stamped the HARVEST witness fields** post-hoc into the deposit JSON
   (`ledger_witness_path` + `ledger_witness_sha256` over the parsed JSONL, canonical `sort_keys=True` /
   compact separators — the same canonicalization `TestCommittedLedgerWitness._ledger_witness_sha256`
   recomputes). These are harvest fields, deliberately excluded from `EVIDENCE_HASH_KEYS`.
4. **Re-pinned the two frozen literals** the suite forces on a real deposit:
   `TestCommittedLedgerWitnessHeterogeneousFull._FROZEN_WITNESS_HASH` and the
   `TestDepositEvidenceHash._EXPECTED[FIXTURE_9B_FULL_HETEROGENEOUS]` entry (the 2026-07-18 literals
   they replaced are in git history).
5. **Updated the now-false assertions** to the measured reality:
   `test_heterogeneous_full_deposit_is_structurally_full_budget` (`n_baseline == 0` → `>= 3`),
   `TestCommittedLedgerWitnessHeterogeneousFull.test_ledger_reconstructs_deposit_loss_vectors_exactly`
   (added the `baseline`/`baseline_losses` pair to the four-way reconstruction loop), and the four
   `TestQualityPreservationAxis` decision-tool tests that asserted the old het-unanswered state.

## Verification — the auto-correction fired (empirical)

The whole point of TASK-0215 was that no rationale string would need hand-editing on harvest. Confirmed:

- **Before any test edit**, re-deriving the live clause against the harvested deposit yielded
  `[heterogeneous] answered=True verdict=SURPASSES cand=1.718021787732141 baseline=1.8862170910967204
  n_baseline=3`, and the SHIP rationale clause auto-read
  `'homogeneous SURPASSES; heterogeneous SURPASSES'` — the latent TASK-0215 contradiction (rationale
  citing "unanswered" three lines below a measured SURPASSES) never materialized.
- `scripts/section4_operator_decision.py`: only the **comment** updated (the logic was already adaptive
  per TASK-0215); recommendation unchanged (**SHIP**), exit-3 blocking unchanged.
- `tests/test_run_freeze_validloss_ci_9b.py`: **487 passed / 4 skipped** (the 4 are freeze-ci-9b/replay
  skipped-by-design on this mirror) — `evidence_hash` self-consistent, ledger reconstructs all FOUR loss
  vectors exactly, witness round-trips.
- `tests/test_section4_operator_decision.py`: **51 passed** — the four `TestQualityPreservationAxis`
  tests now assert the harvested per-leg reality (heterogeneous `answered=True`, `verdict=SURPASSES`,
  `candidate < baseline`; rationale contains `"heterogeneous SURPASSES"` and not `"unanswered"`).
- `ruff check` clean on every `.py` touched.

## What this is NOT

Not a re-fire (the §7-honest full-12 run already banked all arms; re-firing would only re-spend ~4h GPU
to reproduce bytes already committed — `Do NOT re-fire either full leg` in the launcher header). Not a
new decision surface (TASK-0213/0215 already wired the adaptive rendering). Not the production-baseline
DATA axis. It is the **deposit** that closes the cell the in-flight run was fired to close, plus the
test re-pins a real harvested deposit forces.

## Open unchanged

operator `--land` (non-unilateral) + private `src.data` absolute-loss vs the **PRODUCTION** baseline
(DATA / Cat-C). Every §4 cell that this checkout can answer is now answered: both legs
**direction** (TIES) and **quality-vs-full-backprop** (SURPASSES) measured at full budget, each with a
committed ledger witness. The remaining open item is the deliberately-stripped private `src.data`
pipeline — not harvestable from this mirror.

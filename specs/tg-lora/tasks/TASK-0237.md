# TASK-0237: feedback bullet 3 の因果クレーム（MP null → false rank-1 positive）を合成 iid null で実測 → 0% false positive・現コードで well-calibrated を確定し、未 pin の rank1_z パスを振る舞い lock

- **日付**: 2026-08-06
- **分類**: B（自完結・CPU 測定 + test-only lock）／type: test（`tests/test_rank1_null_calibration.py` 新設・**src/ scripts/ は 1 byte も変更しない**）
- **軸**: AI_HUB_MAKE_RUN_FEEDBACK（前 iter=`aa27111`+`f62c359` を VALUABLE 評価）の bullet 3「CLOSE THE LOOP: un-squared MP edge が z-score を膨らませ false rank-1 positive を出した、修正後コードで null/rank-1 baseline を再実行し LOG せよ」の**実測検証 + 振る舞い lock**

## 動機（TASK-0236 の論理却下を、測定で補強する）

TASK-0236 は bullet 3 を「cited null（order-sensitivity・`ratio=0.000`）は model-free deterministic rig で MP 公式を使わない=構造的に無関係、MP/median fix は本 checkout に未適用」と**論理的に**却下した。これは正しいが、feedback の**因果クレーム自体**（「MP 公式が z-score を膨らませ false positive を出す」）を**実測で検証してはいなかった**。本 TASK はこの未検証クレームを、GPU 不要の合成 iid null で直接測定し、結果を振る舞い lock に結晶化する（feedback reason が M9 で評価した「behavior-locking test で L2 へ格上げ」と同型の価値を、rank1_z パスに適用）。

## 実測（LOG: feedback bullet 3 が求めた再計算結果）

`src/tg_lora/layer_delta_analysis.py` の production z-score path（`analyze_tensor_deltas` → `rank1_z = (rank1 - null_exp)/(null_exp*0.5)`、`null_exp = marchenko_pastur_expected_rank1(n_rows,n_cols)`）に、合成 iid null（`torch.randn`）を流し、現実的なレジム（n_snapshots=8–32・n_params=256–32768 = LoRA delta history の実範囲）で z-score 分布を測定:

```
 n_rows  n_cols |  mean_z   frac(z>3)    帰結
     8    4096  |   0.027     0.00       systematic inflation なし
    16    4096  |   0.059     0.00       false rank-1 positive 0 件
    16   32768  |  -0.003     0.00
    32    4096  |   0.115     0.00
    32   32768  |   0.014     0.00
     8     256  |   0.214     0.00
planted signal |  z≈17.75    ----       真 rank-1 信号は正しく発火（陽性対照）
```

**結論**: feedback の因果クレームは現実的レジムで**測定的に成立しない** — iid null の mean_z は < 0.25（< 1.0 の閾値に遠く及ばない＝systematic inflation なし）、frac(z>3) = 0.00（false rank-1 positive ゼロ）。MP 公式の comment-vs-impl mismatch（comment は `(1+sqrt(n/p))^2`・impl は `1/n_rows+sqrt(ratio)/n_rows`）は依然 cosmetic だが、**実装は iid null で well-calibrated**（false positive を出さない）。よって cited verdict は stale でない（TASK-0236 の論理的結論を、異なる rig での測定でも補強）。

## 振る舞い lock（`tests/test_rank1_null_calibration.py`・3 tests）

`TestMarchenkoPastur` は sign/monotonicity/zero のみ pin し、null-calibration を pin していなかった（`f62c359` が M9 で塞いだ「値が unpinned」gap と同型の穴）。本 TASK がこの未 pin パスを lock:

1. `test_iid_null_is_not_systematically_inflated` — 全レジムで mean_z < 1.0（inflation なし）
2. `test_iid_null_produces_no_false_rank1_positives` — 全レジムで frac(z>3) < 0.05（false positive なし）
3. `test_planted_rank1_signal_fires` — 陽性対照: 真 rank-1 信号は z > 3 で発火（テストが vacuous でないことの証明）

**mutation-proven（両方向）**:
- **M1（MP null の過小評価 → inflation 方向）**: `return 1.0/(n_rows*50)` → test 1+2 が **2 RED**（陽性対照は GREEN）→ feedback が懸念した「再 miscalibration で corruption vector 復活」を CI が捕獲
- **M2（MP null の過大評価）**: `return 1.0` → 陽性対照が **1 RED**（null test は GREEN・過大評価は false positive でなく false negative を生むため null test が正しく見逃し陽性対照が捕捉）→ 両方向の miscalibration を 3 test で完全カバー

`PYTHONPATH=. /home/jinno/tg-lora/.venv/bin/python -m pytest tests/test_rank1_null_calibration.py` → **3 passed**。ruff `--select E4,E7,E9,F` ゼロ violation。`test_layer_delta_analysis.py` + `test_replay_freeze_order_sensitivity.py` と同時実行で **53 passed**（zero regression・cited null も再確認 airtight）。

## なぜこれが churn でないか

| 候補 | 採否 | 理由 |
|---|---|---|
| rank1_z null-calibration の振る舞い lock（本 TASK） | **採用** | 未 pin パスへの L2 lock・feedback の falsifiable クレームを実測で検証・test-only（STOP 条項無接触）・mutation-proven 両方向 |
| MP 公式の修正（squaring） | **禁止（却下）** | bullet 2「proxy null の diagnostic-math 精進禁止」+ PURPOSE 追記(9) STOP。実測で well-calibrated（false positive 0）ゆえ修正の科学的必要性なし |
| MP 公式 comment-vs-impl mismatch の doc 修正 | **見送り** | impl は well-calibrated（実測）= cosmetic のみ・保護領域へ触れない（TASK-0236 の「src/ 不変」stance 踏襲） |
| cited order-sensitivity null の再 lock | **churn（却下）** | `test_replay_freeze_order_sensitivity.py` が `ratio==0.0` を airtight pin・MP と構造無関係 |
| 9B run の発火 | **block（不可）** | 下記 blocker |

## 9B target-scale run の blocker（feedback bullet 4 への明示）

feedback が「9B run が genuinely unblocked なら significance-rig 工作の代わりに run せよ」と指示した点について、**現状は block しており、相対比較は既に landing 済み**:

- **DATA (Cat-C)**: private `src.data` pipeline が本 mirror に不在（`git ls-files src/data` = 空）。9B run の data build に必要・外部依存。`[[public-mirror-preexisting-test-failures]]`
- **GPU (物理)**: 12GB RTX 3060 は 9B QLoRA seq2048 で OOM。target-scale run には >12GB GPU が必要。`[[gpu-usable-proxy-runs]]`
- **相対比較は既 DONE**: homogeneous（`4b88ca8`・TIES）+ heterogeneous（`1224057`・TIES）とも full-budget landing・harvest 済み・`recommendation=SHIP` は operator lane 済み（`6c75fda`）。`[[ai-hub-feedback-infra-vs-this-repo]]` `[[progressive-freeze-level2-trio-orphaned]]`

残る唯一の open は private `src.data` による絶対 loss vs production baseline 比較（DATA/Cat-C）のみで、本 checkout からは不可。誠実な応答 = 本 blocker の明示（feedback 自身が「blocked なら polish で埋めず明示せよ」と指示）。

## 無視しない前提

- **src/・scripts/ は 1 byte も変更しない**（test-only）。MP 公式の cosmetic comment-impl mismatch は未修正のまま（実測で well-calibrated・修正は三重禁止）。
- PURPOSE.md「次の一手」は **CI-frozen at 98 追記**（`[[purpose-next-steps-freeze-guard]]`）。状態記述は本ファイルに留め PURPOSE は触らない。
- 本 TASK は feedback bullet 1（M9 test = `f62c359` DONE）・bullet 2（proxy null 精進禁止 = 受諾）を前提し、bullet 3（CLOSE THE LOOP）を実測で閉じ、bullet 4（blocker 明示）に出力した。

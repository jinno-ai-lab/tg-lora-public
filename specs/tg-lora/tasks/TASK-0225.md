# TASK-0225: §4 operator-decision surface を live 駆動し、fresh blocker decision artifact を残す（unilateral 着地はせず）

- **日付**: 2026-07-28
- **コミット**: （本ファイル TASK-0225 + fixture `tests/fixtures/section4_decision_snapshot_2026-07-28.json`）
- **分類**: A（operator-facing artifact）／type: docs + test-fixture（コード変更なし）
- **軸**: oper-decision-surface（TASK-0198 / 0199 / 0217 の運用表面、AI_HUB_MAKE_RUN_FEEDBACK bullet 4 が永続指摘する **operator `--land` 残 open** に対する今 iteration の fresh blocker artifact）

## 動機（feedback "実行できない場合は blocker decision artifact を作成" への応答）

フィードバックの核心指示：

> 「次 iteration は cache 指紋の追加的な no-op や再監査ではなく、PURPOSE.md が示す
> 残 open の operator-facing --land 決定と 9B target-scale 実行可能性に集中し、
> 実行できない場合は blocker と必要な入力を明示した decision artifact を作成する
> こと」

加えて：

> 「ACCEPT but redirect ... 次の move は operator decision でない限り、新しい
> Hardening タスクを積まないこと」

memory 上、§4 arc は **完了済み**（両 leg TIES + 両 P1 品質保持 SURPASSES、
TASK-0216 で adaptive 由来化済み）、`recommendation = "SHIP"` が evidence-based
唯一の値。唯一残 open は **operator `--land` の unilateral 着地**（non-unilateral by
design — この surface は blocking で exit 3）であり、本 iteration はそれを
**unilateral に実行しない**（purpose-driven ポリシー違反）代わりに：

1. `scripts/section4_operator_decision.py` を live で両 mode（`--json` / human）で駆動
2. §4 arc_complete 4 predicate × 2 leg を実測値で再確認
3. fresh blocker artifact を **tracked fixture** に保存（`tests/fixtures/`）
4. unilateral landing record を **書かない**（`section4_landed_decision.json` absent）
5. operator が次に `--land` を打つために必要な input を明示

## live snapshot（2026-07-28）

`scripts/section4_operator_decision.py --json` 実行結果（full JSON は fixture
`tests/fixtures/section4_decision_snapshot_2026-07-28.json` に commit）：

```text
arc_complete              : True
landed_decision           : None
awaiting_operator_decision: True
recommendation            : "SHIP"
verdict_worker_status     : "executable"
src_data_status           : "stripped_deliberate"
recover_rerun_blocked_by_src_data: True
exit code                 : 3   # AWAITING_DECISION — blocking

legs[homogeneous]
  present                          : True
  citable_as_full_section4_verdict : True
  faithful                         : True
  recorded_verdict                 : "TIES"
  rederived_verdict                : "TIES"
  candidate_mean / surrogate_mean  : 1.6947 / 1.6960
  ci_lower / ci_upper              : -9.0e-05 / +0.0027
  baseline_present                 : True
  n_baseline / baseline_verdict    : 3 / "SURPASSES"
  baseline_candidate / baseline_mean: 1.6947 / 1.8794

legs[heterogeneous]
  present                          : True
  citable_as_full_section4_verdict : True
  faithful                         : True
  recorded_verdict                 : "TIES"
  rederived_verdict                : "TIES"
  candidate_mean / surrogate_mean  : 1.7180 / 1.7191
  ci_lower / ci_upper              : -0.0011 / +0.0028
  baseline_present                 : True
  n_baseline / baseline_verdict    : 3 / "SURPASSES"
  baseline_candidate / baseline_mean: 1.7180 / 1.8862

quality_preservation
  homogeneous  : answered=True,  verdict=SURPASSES, n_baseline=3
  heterogeneous: answered=True,  verdict=SURPASSES, n_baseline=3

branches
  ship       : executable_here=True   (summary: adopt TIES as the §4 result)
  accept_null: executable_here=True   (summary: TIES as the honest null)
  pivot      : executable_here=False  private_repo_only=True
               (summary: absolute-loss via src.data — private-repo action)
```

human mode 出力（47 行、exit 3）＝ 上記を人が読み下せる形式にしたもので末尾が
blocking prompt：

```text
=== ACTION REQUIRED: land your §4 operator decision ===

  ship        — adopt the citable relative-verdict TIES as the §4 result
  accept_null — record the TIES as the honest null (freeze-order gain is null)
  pivot       — absolute-loss comparability (private-repo src.data action)

Land exactly one (writes a committed record; this prompt then stops):

  python scripts/section4_operator_decision.py \
      --land <ship|accept_null|pivot> --basis "<why>"

Until you land one, this surface blocks (exit 3) and emits no further variants.
```

## 検証（mutation-proven 4 predicate × 2 leg, byte-level）

fixture 上の snapshot を再 parse して invariant を直接 assert：

| 項目 | 期待値 | 実測 |
|------|--------|------|
| `arc_complete` | `True` | ✅ |
| `recommendation` | `"SHIP"` | ✅ |
| `legs[*].present ∧ citable_as_full_section4_verdict ∧ faithful ∧ rederived_verdict=="TIES"` | 全 leg | ✅（homog + heterog） |
| `quality_preservation[*].answered ∧ verdict=="SURPASSES"` | 全 leg | ✅（homog + heterog） |
| `branches.pivot.executable_here == False ∧ private_repo_only == True` | blocked here | ✅ |
| `exit == 3`（waiting on operator） | blocking | ✅ |
| `landed_decision is None`（unilateral landing していない） | absent | ✅ |
| `section4_landed_decision.json` repo root | absent | ✅ |

加えて `tests/test_section4_operator_decision.py` フル再実行（venv py3.13 / pytest 9.0.3）：

```text
collected 51 items
tests/test_section4_operator_decision.py ............................... [ 60%]
....................                                                     [100%]
======================== 51 passed in 398.91s (0:06:38) ========================
```

spec 上の "43 tests" は TASK-0198 時点の数値、**現在 51**（`TestSection4DecisionArc` /
`TestVerdictWorkerExecutability` / `TestProbeClassification` /
`TestRecommendationLogic` / `TestPivotBranchCorrection` /
`TestArcIncompleteMutations` / `TestQualityPreservationAxis` /
`TestLandingRecordRobustness` / `TestLandToExitZero` / `TestLandRejects` /
`TestJsonMode` / `TestHumanMode` / `TestBlockingPrompt` /
`TestSubprocessIsolation` / `TestLandPreservesRecommendation` /
`TestLandingRecordFormat` / `TestMainExitContract` / `TestHelpSmoke` の 18 クラ
スに着地後に追加された 8 件が差分）。全 51 green、ゼロ regression。

## 変更ファイル一覧（unilateral landing なし）

| ファイル | 種別 | 説明 |
|---------|------|------|
| `tests/fixtures/section4_decision_snapshot_2026-07-28.json` | fixture（tracked, 新規） | 2026-07-28 の `scripts/section4_operator_decision.py --json` 出力を 1:1 保存。byte-identical な citable audit artifact（既存 deposit fixtures と同 pattern） |
| `specs/tg-lora/tasks/TASK-0225.md` | task note（tracked, 新規） | 本ファイル |

**書かなかったファイル**:

- `section4_landed_decision.json`（repo root）— **意図的に absent**。spec
  REQ-403 により operator `--land` は unilateral 禁止。本 iteration は
  evidence-based RECOMMENDATION=SHIP を提示するに留め、着地は operator が手動で
  `--basis "<why>"` 付きで実行する設計。
- 既存 `tests/fixtures/freeze_validloss_ci_9b_full*.json` deposit fixture の再生成
  （要件なし、LEDGER_VERSION 不変、既存 deposit は citable）。

## operator `--land` 着地に必要な input（次 iteration への引き継ぎ）

§4 arc は **completed** であり、evidence-based RECOMMENDATION は SHIP。
operator が unilateral に着地するための最小コマンドは次のどれか（spec REQ-301..305）：

```bash
# 3 つのうち 1 つを明示的に選ぶ（basis は non-empty 文字列必須）

python scripts/section4_operator_decision.py \
    --land ship --basis "<why SHIP is the right call given the citable faithful TIES + P1 品質保持 SURPASSES per leg>"

# もしくは

python scripts/section4_operator_decision.py \
    --land accept_null --basis "<why TIES as the honest null is preferable to SHIP here>"

# もしくは（PIVOT は src.data 必須なので本 mirror では executable_here=False）

python scripts/section4_operator_decision.py \
    --land pivot --basis "<why absolute-loss vs private src.data baseline is the only remaining axis>" \
    # (spec REQ-303: pivot_private_repo_only=True が record に付与される)
```

NOTE: "PIVOT を着地する" の意味は「**本 mirror で PIVOT を `--land` する**」＝
`pivot_private_repo_only=True` フラグ付き record を書く こと。**PIVOT の
本体 action（private src.data での再 run）**は spec REQ-206 が明示するとおり
**private repo (`/home/jinno/tg-lora`) で実行**するため、本 iteration はその
trigger を発火しない（Cat-C = GPU + private data、`make-run` / `alternating-loop`
の worker 経由で実行する境界）。

## bullet 4 compliance / 位置づけ

- bullet 1 (`9-arm audit in CI`) / bullet 3 (`contract-terms.md` / freeze) は
  本 checkout の spec tree に **対応する surface が不在** (`specs/recursive-funnel-bitnet/`
  該当なし、grep 0)。phantom な feedback への追従は本 iteration では実施しない。
- bullet 2（再 audit 禁止）は本 TASK も遵守：cache fingerprint 軸の再 audit /
  XIX/XX/XXI/XXII の次は作らない。
- bullet 4（operator decision or blocker artifact）= **本 TASK が主応答**。
  live 駆動 + tracked fixture + unilateral landing 拒否を 1 つの artifact に
  蒸留。

**memory 参照**:

`[[ai-hub-feedback-infra-vs-this-repo]]` §4 arc COMPLETE、SHIP が唯一の
evidence-based RECOMMENDATION、`--land` は operator-gated BY DESIGN。
`[[silent-success-best-model-min-delta-axis]]` cache-fingerprint 軸は CLOSED、
再 hunt 不要。`[[progressive-freeze-level2-trio-orphaned]]` §4 verdict ワーク
完了済（2026-07-18 homog / 2026-07-27 heterog）— do NOT re-fire/re-harvest。
`[[freeze-ci-operator-errors-axis]]` exit 78 経路は別 surface、本 TASK と直交。
`[[progressive-freeze-level2-trio-orphaned]]` §4 arc の PIVOT は private
src.data 経路で本 mirror では実行不可（REQ-206、ブロック済）。

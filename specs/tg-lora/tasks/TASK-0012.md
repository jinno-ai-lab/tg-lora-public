# TASK-0012: train_tg_lora.py 純粋関数抽出


<!-- spine:anchor:begin -->
> **Spine anchor**: [TG-LoRA アーキテクチャ設計](../architecture.md)
>
> - parent: `tg-lora/architecture.md`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0012
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 3 - 学習ループテスタビリティ向上
**信頼性レベル**: 🔵 *architecture.md・requirements.md (REQ-015〜020, REQ-038〜044)・coverage report より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-015, REQ-016, REQ-020, REQ-044
- **設計文書**: [📐 architecture.md](../architecture.md)
- **データフロー**: [🔄 dataflow.md](../dataflow.md)

## タスク概要

train_tg_lora.py のテスト不能な学習ループ本体（32%カバレッジ、99行未カバー）から、純粋関数として抽出可能なロジックを特定・抽出し、ユニットテストを追加する。抽出により全体のテスタビリティを向上させ、後続タスク（TASK-0013）でのモック統合テストの基盤を整備する。

## 依存タスク

- **前提タスク**: なし（TASK-0001〜0011完了済みの独立タスク）
- **後続タスク**: [TASK-0013: モックベース学習ループ統合テスト](TASK-0013.md)

## 完了条件

- [x] pilot サイクルのメトリクス集計ロジックを純粋関数として抽出
- [x] accept/rollback 判定ロジックを純粋関数として抽出
- [x] サイクル進捗表示用フォーマッタを純粋関数として抽出
- [x] フル評価ハンドラの判定部分を純粋関数として抽出
- [x] 抽出した全関数に対するユニットテスト追加（パスすること）
- [x] 既存テスト339件が全て引き続きパスすること
- [x] train_tg_lora.py のテスト済み関数のカバレッジが50%以上に到達

---

## 主要実装項目

### 1. Pilot サイクルメトリクス集計関数の抽出 🔵

**信頼性**: 🔵 *train_tg_lora.py lines 139-149, 221-242 の既存実装より*

pilotフェーズのK歩backward後の損失平均計算とメトリクス記録ロジックを純粋関数として抽出。入力（loss値リスト、cycle番号、K値）から出力（average loss, metrics dict）への純粋な変換。

**実装ファイル**: `src/training/train_tg_lora.py`

### 2. Accept/Rollback 判定関数の抽出 🔵

**信頼性**: 🔵 *train_tg_lora.py lines 194-204 の既存実装より*

loss_pilot, loss_after, rollback_tolerance から受理/拒否を判定する純粋関数。戻り値は (accepted: bool, reason: str)。RandomWalkController への reward/penalize 呼び出しは呼び出し側で行う。

**実装ファイル**: `src/training/train_tg_lora.py`

### 3. サイクル進捗フォーマッタの抽出 🔵

**信頼性**: 🔵 *train_tg_lora.py lines 237-242 の既存実装より*

progress bar用の表示文字列を生成する純粋関数。入力（cycle, loss値, accepted, cos_sim, reduction_rate, K, N）からフォーマット済み文字列を返す。

**実装ファイル**: `src/training/train_tg_lora.py`

### 4. フル評価実行判定の強化 🔵

**信頼性**: 🔵 *train_tg_lora.py 既存 should_run_full_eval・lines 250-271 より*

フル評価サイクルでのearly stopping判定とベストモデル保存判定を純粋関数として抽出。CycleState.should_stop の結果とベスト損失更新を統合。

**実装ファイル**: `src/training/train_tg_lora.py`

---

## 基本的なテスト要件

### 純粋関数ユニットテスト 🔵

**信頼性**: 🔵 *抽出元の既存実装より*

- pilot メトリクス集計: 正常値、空リスト、単一値、NaN混入
- accept/rollback 判定: tolerance境界値、等価ケース、極端なloss差
- 進捗フォーマッタ: 各パラメータの表示確認、数値精度
- フル評価判定: cycle番号境界、patience境界値

**テストファイル**: `tests/test_training_pure_functions.py`

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-0012` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- train_tg_lora.py のシグネチャ変更は既存呼び出し元（Makefile, configs）に影響しないよう、内部リファクタリングに留める
- GPU依存コードの抽出は行わない（モックテストはTASK-0013で対応）
- 抽出した関数はtrain_tg_lora.py内のprivate関数として定義し、モジュール公開APIは変更しない
- 既存の should_run_full_eval, build_training_summary, _InfiniteBatchIterator のテストは維持

---

## 信頼性レベルサマリー

- **総項目数**: 4項目
- 🔵 **青信号**: 4項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質 — 全項目が既存実装の直接読み込みに基づく

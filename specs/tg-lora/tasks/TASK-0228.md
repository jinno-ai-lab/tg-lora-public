# TASK-0228: §4 operator-decision surface の refactor-safety pin catalog 完結 — 派生 top-level predicate 2 件の pin 追加（feedback bullet 3 spirit）

- **日付**: 2026-07-28
- **コミット**: （本ファイル + `tests/test_section4_operator_decision.py`）
- **分類**: A（コードで解決可能）／type: test（既存 fixture-vs-live pin の catalog 完結・mutation-proven）
- **軸**: oper-decision-surface（TASK-0226 `TestSnapshotFixtureIntegrity` が導入した refactor-safety pin の**網羅性 gap 閉包**・AI_HUB_MAKE_RUN_FEEDBACK bullet 3「a regression should fail loudly so a refactor that silently drops a contract term blocks the PR」spirit の直接完了）

## 動機（catalog completeness claim が不完全だった事実）

TASK-0226 は §4 operator-decision surface を fixture-vs-live 構造 predicate 比較で pinned した（refactor が表面を silent に壊したら即 RED）。しかし `test_top_level_predicates_match_fixture` の pin 対象 key は 6 件のみで、surface が契約する **top-level predicate の一部が未 pin** だった:

```python
# TASK-0226 当時の pin 対象（6 件）
arc_complete, landed_decision, awaiting_operator_decision,
recommendation, verdict_worker_status, src_data_status
```

未 pin だった top-level predicate のうち、**2 件が load-bearing**:

1. `run_executable_here` — verdict run が本 mirror で実行可能か（GPU-free import probe 由来）
2. `recover_rerun_blocked_by_src_data` — recover.py `--rerun` / `train_tg_lora` path が src.data strip で block されているか（surface docstring point 2 の核心 claim = 実行可能な verdict run と src.data-blocked な recover path の区別）

両者とも**派生元の input は既に pin 済み**（`verdict_worker_status` / `src_data_status`）。しかしその**派生ロジック自体**（`verdict_worker_status != "architectural_block"` / `not src_data_present`）は pin 対象外のため、refactor が派生式に条件を追加して派生値を silent に反転させても、input 側の pin は緑のまま通過してしまう = feedback bullet 3 が名指す **「refactor が contract term を silent drop してすり抜ける」** 形状が残存していた。

特に (2) は **P0 科学誠実性** に関わる: recover path が block されているという事実が silent に「block されていない」へ反転すれば、src.data が存在する／recover re-run が動くという**虚偽の claim** になり得る。

## 変更

### `tests/test_section4_operator_decision.py` — `test_top_level_predicates_match_fixture` の pin 対象 key に 2 件追加

```python
for key in (
    "arc_complete",
    "landed_decision",
    "awaiting_operator_decision",
    "recommendation",
    "verdict_worker_status",
    "src_data_status",
    "run_executable_here",                    # ← 追加
    "recover_rerun_blocked_by_src_data",      # ← 追加
):
```

設計上の要点:
- **新規 meta-test ではない** — 既存 `TestSnapshotFixtureIntegrity`（TASK-0226）の pin catalog を**完結**させるだけ（bullet 4「stop adding new terms」への抵触なし・PURPOSE.md「次の一手」追記ではない＝freeze guard 不触）。
- fixture と live で両 key とも `True` で一致確認済み（pin 追加で即緑）。
- rationale comment に「派生元 input は pin 済みだが派生値も pin することで派生ロジックの silent 変更を検知する = catalog completeness の閉包」を明記。

## 検証（mutation-proven ×2）

各派生ロジックを破壊し、pin が即 RED になることを実証:

| mutation | 結果 |
|----------|------|
| `run_executable_here = verdict_worker_status != "architectural_block"` → `= False` | RED: `top-level predicate 'run_executable_here' drifted: fixture=True, live=False` |
| `"recover_rerun_blocked_by_src_data": not src_data_present` → `= False` | RED: `top-level predicate 'recover_rerun_blocked_by_src_data' drifted: fixture=True, live=False` |
| 両 mutation revert 後 | `TestSnapshotFixtureIntegrity` 5/5 緑・ファイル全体 56/56 緑 |

- `ruff check` / `ruff format --check`: clean（test file）
- script 側（`scripts/section4_operator_decision.py`）は**不変**（mutation は全て revert 済み・git status で test file のみ modified）

## 残 open（不変）

feedback bullet 1/2/4 は TASK-0225/0226/0227 で構造的クローズ済み。本 TASK は bullet 3 spirit（refactor-safety catalog completeness）の**残存 gap の閉包のみ**。残 open は不変:

- **operator `--land`**〔non-unilateral by REQ-403・live exit 3・`recommendation=SHIP`〕
- **private `src.data`**〔DATA/Cat-C・deliberately stripped〕

いずれも本 checkout から自動実行不可。arc 完結状態（両 leg citable faithful TIES + P1 品質保持 SURPASSES・`make section4-operator-decision` = exit 3 awaiting）は不変。

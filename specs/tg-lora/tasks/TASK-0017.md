# TASK-0017: CLI エントリポイント GPUモックテスト

**タスクID**: TASK-0017
**タスクタイプ**: TDD
**推定工数**: 2.5時間
**フェーズ**: Phase 4 - 設定検証と安全性向上
**信頼性レベル**: 🔵 *既存test_training_integration.py・test_baseline_training.pyモックパターンより*

## 関連文書

- **概要**: [overview.md](overview.md)
- **要件定義**: [requirements.md](../requirements.md)

## タスク概要

train_tg_lora.py と train_baseline_qlora.py の main() CLI エントリポイントに対するモックテストを追加し、残り2%のカバレッジギャップを閉じる。argparse/OmegaConf/logging/train関数をモックし、GPU依存なしでCLIフロー全体を検証する。

## 依存タスク

- **前提タスク**: なし（TASK-0016と並行可能）
- **後続タスク**: TASK-0019

## 完了条件

- [x] test_cli_entry.py にmain()のテストを追加
- [x] train_tg_lora.main() のカバレッジ 94%→100%
- [x] train_baseline_qlora.main() のカバレッジ 85%→100%
- [x] train_baseline_qlora.py の StopIteration ハンドリング（78-80行目）をテスト
- [x] __main__ ガードのパスをカバー
- [x] 総合カバレッジ 98%→100%

---

## 主要実装項目

### 1. main() モックテスト 🔵

**信頼性**: 🔵 *既存test_training_integration.pyのモックパターンより*

```python
# tests/test_cli_entry.py
import sys
from unittest.mock import patch, MagicMock

class TestTGLoRAMain:
    def test_main_loads_config_and_calls_train(self, tmp_path):
        config_path = tmp_path / "test.yaml"
        config_path.write_text(...)
        with patch("src.training.train_tg_lora.train_tg_lora") as mock_train, \
             patch("sys.argv", ["train_tg_lora.py", "--config", str(config_path)]):
            from src.training.train_tg_lora import main
            main()
            mock_train.assert_called_once()

    def test_main_missing_config_arg_exits(self):
        with patch("sys.argv", ["train_tg_lora.py"]):
            with pytest.raises(SystemExit):
                from src.training.train_tg_lora import main
                main()

class TestBaselineMain:
    def test_main_loads_config_and_calls_train(self, tmp_path):
        ...

    def test_main_missing_config_arg_exits(self):
        ...
```

**実装ファイル**: `tests/test_cli_entry.py`（新規）

### 2. StopIterationハンドリングテスト 🔵

**信頼性**: 🔵 *train_baseline_qlora.py 78-80行目のロジックより*

epoch_iterのStopIteration時のリセット動作をテスト。

```python
def test_epoch_iter_resets_on_stop_iteration():
    """DataLoader iterator exhaustion triggers reset."""
    # epoch_iter = iter(train_loader); batch = next(epoch_iter)
    # When StopIteration: create new iter and get next batch
```

**実装ファイル**: `tests/test_cli_entry.py` 内

### 3. __main__ ガードテスト 🔵

**実装ファイル**: `tests/test_cli_entry.py` 内

subprocessまたはimportlibで __main__ ブロックをテスト。

---

## 実装手順

### TDDタスクの場合

1. テストファースト: test_cli_entry.py に失敗テストを書く
2. モックパターンを確立: train関数・OmegaConf・loggingをモック
3. テストをパスさせる
4. カバレッジ確認: pytest --cov で100%確認

---

## 注意事項

- train_tg_lora() と train_baseline() 自体はモックする（GPU依存のため）
- テスト対象はargparseの引数処理、config読み込み、train関数の呼び出し連鎖のみ
- tmp_pathを使用して一時YAMLファイルを作成

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質 — 既存モックテストパターンの直接適用

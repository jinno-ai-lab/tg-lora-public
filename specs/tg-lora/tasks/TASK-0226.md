# TASK-0226: §4 decision surface を fixture-vs-live で refactor-safe に pin — feedback「pin in CI」の 本-checkout analog（phantom symbols の非捏造実 analog）

- **日付**: 2026-07-28
- **コミット**: `5bb0faf`
- **分類**: A（コードで解決可能）／type: test（テスト追加 — 本 TASK の核は refactor-safety pin であり、プロダクションコードの load-bearing claim に対し「fixture 比較」を構造化テストとして pin）
- **軸**: oper-decision-surface（TASK-0198 / 0199 / 0217 / 0223 / 0225 の運用表面、AI_HUB_MAKE_RUN_FEEDBACK bullet 1「pin the 9-arm audit in CI rather than just in pytest discovery」の **本-checkout 実 analog**）

## 動機（feedback の **実** analog を本 checkout で構造的に pin する）

feedback の構造的主張は次の 3 つ:

1. **bullet 1**: "Pin the 9-arm audit in CI rather than just in pytest discovery — the catalog completeness is now the load-bearing claim; a regression in `TestAuditCatalogCompleteness` should fail the suite loudly so a refactor that silently drops a contract term blocks the PR instead of slipping through."
2. **bullet 3**: "Document the contract-term lineage in PURPOSE.md once more, then freeze ... or stop adding new terms."
3. **bullet 2**: "ACCEPT but redirect ... require the loop to produce an operator-facing decision artifact ... treat any additional cache hardening as L1 maintenance only, not a new arc."

bullet 1/3 で言及されている symbol は本 checkout では **PHANTOM**:

```bash
$ rg -rln "TestAuditCatalogCompleteness|recursive-funnel-bitnet|contract-terms|9-arm audit|contract_term" \
    --include="*.py" --include="*.md"
# → 0 hit (TASK-0225.md 自体にも無い)
$ ls specs/recursive-funnel-bitnet 2>/dev/null
# → no such directory
```

これは `[[ai-hub-feedback-infra-vs-this-repo]]` が記録する事実と整合する: AI_HUB_MAKE_RUN_FEEDBACK は AI-Hub 側 private infra の symbol 名を持ち込み、本 checkout の symbol とは一致しないことが多い。phantom に対する follow-up を **捏造** しない（spec tree に存在しない `TestAuditCatalogCompleteness` を新規作成しない＝ fabrication 禁止）。

ただし feedback の **spirit** は本 checkout にも実在する。**§4 operator-decision surface** は:

- TASK-0198 で `scripts/section4_operator_decision.py` として構築
- TASK-0225 で `tests/fixtures/section4_decision_snapshot_2026-07-28.json` を committed fixture として保存
- `tests/test_section4_operator_decision.py`（51 tests）で decision combinator のロジックを pin

だが **「fixture 構造 predicate」と「live 出力の predicate set」が一致するか** を pin する test は **存在しなかった** = feedback bullet 1 の構造的主張（refactor-safety for a load-bearing claim）が本 surface に対し **未充足**。

→ 本 TASK はこの実 gap を閉じ、`recommendation`/`recorded_verdict`/`quality_preservation`/`branch.executable_here` の silent drift を **fixture-vs-live predicate 比較** で構造的に detect する test 群を追加する。

## 変更（`tests/test_section4_operator_decision.py`）

### 1. import 追加

```python
import pytest  # @pytest.fixture デコレータ要用
```

### 2. 新規クラス `TestSnapshotFixtureIntegrity`（5 tests）

```python
class TestSnapshotFixtureIntegrity:
    """Pin the §4 operator-decision surface against the committed fixture.

    The committed tests/fixtures/section4_decision_snapshot_2026-07-28.json
    captures a fixed audit artifact (TASK-0225). The §4 logic tests above
    cover the decision combinator, but a refactor that silently changes the
    surface output ... would slip through them.

    This is the real analog of feedback's "pin in CI rather than just in
    pytest discovery" for this checkout: the literal TestAuditCatalogCompleteness
    name is phantom in this repo (grep 0 hit), but the SPIRIT — refactor-safety
    for a load-bearing surface — applies to the §4 decision surface.
    """
```

各 test は:

- `live_payload` fixture: `subprocess.run([sys.executable, "-m", "scripts.section4_operator_decision", "--json"], cwd=REPO_ROOT, timeout=60)` で **REAL script** を実行（operator が手元で打つのと同じ invocation）
- `fixture_payload` fixture: `REPO_ROOT / "tests/fixtures/section4_decision_snapshot_2026-07-28.json"` を load
- predicate 比較: `arc_complete` / `landed_decision` / `awaiting_operator_decision` / `recommendation` / `verdict_worker_status` / `src_data_status`（top-level 6）、`present` / `recorded_verdict` / `rederived_verdict` / `citable_as_full_section4_verdict` / `faithful` / `baseline_present` / `baseline_verdict`（per-leg 7）、`answered` / `verdict` / `n_baseline`（quality_preservation 3 × 2 leg）、`executable_here`（branch 3）

**意図的に除外した field**: `candidate_mean` / `surrogate_mean` / `ci_lower` / `ci_upper` 等の **数値** mean/CI bound = re-fire 時に refresh が必要だが Cat-C blocked（本 mirror では再 run 不能）= refresh が発生しない predicate set に絞り、surface contract を保持。数値 drift は re-fire 起因のみ（faithful=True で deterministic bootstrap なので発生しない）で、もし数値 drift が起きればそれは fixture-vs-live で byte-identical という別の invariant の話（mutation でも数値を直接書き換えると別 test で RED になる）。

### 3. 検証

```
tests/test_section4_operator_decision.py::TestSnapshotFixtureIntegrity::test_fixture_exists_and_is_loadable PASSED
tests/test_section4_operator_decision.py::TestSnapshotFixtureIntegrity::test_top_level_predicates_match_fixture PASSED
tests/test_section4_operator_decision.py::TestSnapshotFixtureIntegrity::test_legs_set_and_predicates_match PASSED
tests/test_section4_operator_decision.py::TestSnapshotFixtureIntegrity::test_quality_preservation_predicates_match PASSED
tests/test_section4_operator_decision.py::TestSnapshotFixtureIntegrity::test_branch_executability_matches PASSED

5 passed in 35.83s
```

**mutation-proven × 3**:

| mutation | target test | observed |
|---|---|---|
| fixture `recommendation` SHIP → FIRE_OR_EXTEND | `test_top_level_predicates_match_fixture` | RED `fixture='FIRE_OR_EXTEND', live='SHIP'` |
| fixture heterogeneous leg `recorded_verdict` TIES → SURPASSES | `test_legs_set_and_predicates_match` | RED `fixture='SURPASSES', live='TIES'` |
| fixture pivot branch `executable_here` False → True + `private_repo_only` False | `test_branch_executability_matches` | RED `fixture=True, live=False`（src.data port 偽装を即検出） |

各 mutation 後、fixture は backup から復元（`/tmp/section4_snapshot_backup.json`）して GREEN 復帰を確認。

### 4. broader sweep

`tests/test_section4_operator_decision.py` 全体（51 + 5 = 56 tests）= 全 GREEN（既存 51 + 新規 5）。他スイートへの影響なし（他 test は本 file を経由しない）。

### 5. ruff

`ruff check tests/test_section4_operator_decision.py` = **All checks passed!**

## §4 surface 状態不変の確認

TASK-0225 fixture（2026-07-28）と live 出力の predicate 比較（同 run から直接確認）:

| predicate | fixture | live | equal |
|---|---|---|---|
| `arc_complete` | True | True | ✓ |
| `landed_decision` | None | None | ✓ |
| `awaiting_operator_decision` | True | True | ✓ |
| `recommendation` | SHIP | SHIP | ✓ |
| `verdict_worker_status` | executable | executable | ✓ |
| `src_data_status` | stripped_deliberate | stripped_deliberate | ✓ |
| homogeneous `recorded_verdict` | TIES | TIES | ✓ |
| homogeneous `faithful` | True | True | ✓ |
| homogeneous `citable_as_full_section4_verdict` | True | True | ✓ |
| homogeneous `baseline_verdict` | SURPASSES | SURPASSES | ✓ |
| heterogeneous `recorded_verdict` | TIES | TIES | ✓ |
| heterogeneous `faithful` | True | True | ✓ |
| heterogeneous `citable_as_full_section4_verdict` | True | True | ✓ |
| heterogeneous `baseline_verdict` | SURPASSES | SURPASSES | ✓ |
| `quality_preservation.homogeneous.verdict` | SURPASSES | SURPASSES | ✓ |
| `quality_preservation.heterogeneous.verdict` | SURPASSES | SURPASSES | ✓ |
| `quality_preservation.homogeneous.n_baseline` | 3 | 3 | ✓ |
| `quality_preservation.heterogeneous.n_baseline` | 3 | 3 | ✓ |
| `branches.ship.executable_here` | True | True | ✓ |
| `branches.accept_null.executable_here` | True | True | ✓ |
| `branches.pivot.executable_here` | False | False | ✓ |
| `branches.pivot.private_repo_only` | True | True | ✓ |

= TASK-0225 の fixture が 2026-07-28 の surface state を **byte-identical** に再現できている。新 fixture の refresh は不要（= 誠実な no-op-with-blocker の base 条件）。

## 変更ファイル一覧

| ファイル | 種別 | 説明 |
|---------|------|------|
| `tests/test_section4_operator_decision.py` | test（本 TASK の主変更） | `import pytest` 追加 + `TestSnapshotFixtureIntegrity` 5 tests 追加（`test_fixture_exists_and_is_loadable` / `test_top_level_predicates_match_fixture` / `test_legs_set_and_predicates_match` / `test_quality_preservation_predicates_match` / `test_branch_executability_matches`） |
| `specs/tg-lora/tasks/TASK-0226.md` | task note（tracked, 新規） | 本ファイル |
| `PURPOSE.md` | docs（Phase 7.5 軽量更新） | 追記27 として本 TASK の記録を追加（commit hash は本 TASK 完了後に確定） |

**書かなかったファイル**:

- `tests/fixtures/section4_decision_snapshot_2026-07-28.json` の refresh — surface 状態不変確認済（26 predicate byte-identical）= 既存 fixture で十分。
- `section4_landed_decision.json`（repo root）— TASK-0225 同様 **意図的に absent**。spec REQ-403 により operator `--land` は unilateral 禁止。
- `specs/recursive-funnel-bitnet/contract-terms.md`（feedback bullet 3 推奨）— `specs/recursive-funnel-bitnet/` 自体が本 checkout に **存在しない**（phantom directory）= 捏造禁止。§4 decision surface が本 checkout での analog。
- `tests/test_audit_catalog_completeness.py`（feedback bullet 1 推奨）— `TestAuditCatalogCompleteness` は本 checkout に存在しない（grep 0 hit）= 捏造禁止。`TestSnapshotFixtureIntegrity` が §4 surface analog。

## bullet 1/2/3/4 compliance / 位置づけ

- **bullet 1**（pin in CI rather than just in pytest discovery）= 本 TASK が **本 checkout の実 analog** で応答。`TestAuditCatalogCompleteness` 自体は phantom（捏造しない）が、その spirit — refactor が load-bearing surface を silent に壊したら CI fail loud — は `TestSnapshotFixtureIntegrity` 5 tests で構造的に成立。
- **bullet 2**（再 audit 禁止）= 遵守。cache fingerprint 軸への XXIII 追加はしていない（[silent-success-best-model-min-delta-axis] の CLOSED lane を再 hunt しない）。
- **bullet 3**（contract-term lineage を PURPOSE.md で文書化 or freeze）= 本 checkout では analog 不在。`specs/recursive-funnel-bitnet/contract-terms.md` 推奨は directory 自体が phantom につき registry ファイル作成不可。代わりに本 TASK は **PURPOSE.md 追記27** で §4 decision surface pin の経緯を記録（feedback bullet の analog として）し、加えて **PURPOSE.md 「次の一手」セクションの changelog 成長に対して freeze 方針** を次の判断材料として残す（後述）。
- **bullet 4**（operator decision or blocker artifact）= TASK-0225 で対応済（fresh fixture 残置 + non-unilateral landing 拒否）。本 TASK はその fixture に対する **refactor-safety pin** であり、unilateral landing ではない。

## PURPOSE.md 「次の一手」 changelog 成長への freeze 提案（次イテレーション判断材料）

PURPOSE.md は現在 1305 行、「次の一手」セクションだけで 26 追記（1305 - 153 ≈ 1152 行 ≒ 各 40-50 行）が蓄積。feedback bullet 3 が「with 6 terms the PURPOSE paragraph is becoming a changelog」と警告する形状が **本 checkout でも再現しつつある**（6 → 26 と既に 4 倍超）。

**提案**（本 TASK では実装せず・判断材料のみ）:

- **Option A**: 「次の一手」セクションを **直近 N 件のみ保持**（例: 直近 5 追記）+ 過去は `specs/tg-lora/PURPOSE_history.md` に移動。22系/25系の判断は履歴ファイルを tail で参照。
- **Option B**: §4 完了宣言を **正式 landing とするまで停止**（operator `--land` 着地待ち = Cat-C）。停止中は「次の一手」セクション追記ゼロ = changelog 成長停止。
- **Option C**: 現状維持（本 TASK で feedback の analog 充足 + bullet 2 遵守 = 進行）。

本 TASK は Option C（現状維持）の範囲。本提案の採否は **operator `--land` 着地後の §4 closeout** と一括判断する方が筋（追補のたびに freeze するか議論するより、章末で一括）。

## operator `--land` 着地に必要な input（次 iteration への引き継ぎ・TASK-0225 から不変）

```bash
python scripts/section4_operator_decision.py \
    --land ship --basis "<why SHIP is the right call given the citable faithful TIES + P1 品質保持 SURPASSES per leg>"

# もしくは

python scripts/section4_operator_decision.py \
    --land accept_null --basis "<why TIES as the honest null is preferable to SHIP here>"

# もしくは（PIVOT は src.data 必須なので本 mirror では executable_here=False）

python scripts/section4_operator_decision.py \
    --land pivot --basis "<why absolute-loss vs private src.data baseline is the only remaining axis>"
```

## 残 open（不変）

- (a) operator `--land` 〔non-unilateral・live exit 3・TASK-0225 と同条件・本 TASK で変化なし〕
- (b) private `src.data` 絶対 loss 〔DATA/Cat-C・本 mirror 不可〕

## memory 参照

`[[ai-hub-feedback-infra-vs-this-repo]]` §4 arc COMPLETE、SHIP が唯一の evidence-based RECOMMENDATION、`--land` は operator-gated BY DESIGN。`[[silent-success-best-model-min-delta-axis]]` cache-fingerprint 軸は CLOSED、再 hunt 不要（bullet 2 遵守）。`[[progressive-freeze-level2-trio-orphaned]]` §4 verdict ワーク完了済 — do NOT re-fire/re-harvest。`[[freeze-ci-operator-errors-axis]]` exit 78 経路は別 surface、本 TASK と直交。
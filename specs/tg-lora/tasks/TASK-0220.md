# TASK-0220: feedback 4 bullet の要件整理（kairo-requirements・第2ラウンド）→ TC-132-09 統合テストの mutation 証明を "by construction" 主張から**実執行の失敗再現ログ**へ昇格（3 層検知を実証）・phantom 再検証・operator decision を実行出力で surface

<!-- spine:anchor:begin -->
> **Spine anchor**: [TG-LoRA アルゴテクチャ設計](../architecture.md)
>
> - parent: `tg-lora/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0220
**タスクタイプ**: docs+verify（kairo-requirements 要件整理・第2ラウンド。前 iter = VALUABLE 評価の feedback が bullet 2 で「テストスイート実行結果または**具体的な失敗再現・修正ログ**」を要求。前 iter（`5616e36`）の TC-132-09 統合テスト mutation 証明は commit message の "mutation-proof by construction" **自己主張**であり実執行ログが不在だった → 本 TASK が producer field drop を一時編集→実行→復元で実証し、失敗再現ログとして記録する。新規テスト・新規 seam・src/ 恒久変更はゼロ）
**推定工数**: 30分
**フェーズ**: AI Hub 反復。feedback の「steering input であって維持するレポートではない」に従い、本 TASK はレポートの反復維持ではなく、既存主張の**実証的裏付け**（mutation の実執行）と operator 判断の**実行出力** surface のみを行う。
**信頼性レベル**: 🔵 *live checkout で実行検証: (1) baseline 207 passed（cache 指紋+統合 49 / resume+config_schema 158）・(2) mutation M1（`build_prefix_feature_cache_metadata` から `max_seq_len` key 一時削除）で 3 層すべてが FAILED → 復元後 green・最終 `git diff` 0 行・(3) `label_affecting`/`_LABEL_AFFECTING_CONFIG_FIELDS` = code（src/scripts/tests）grep 0 hit 再確認〔PHANTOM 持続〕・`context_size` = docs 以外 0 hit 再確認〔PHANTOM 持続〕・(4) `section4_operator_decision.py` を report mode で実実行し出力を引用*

## 関連文書

- **正本**: [docs/GOAL.md](../../../docs/GOAL.md) §7（Constitution Rule #6 = silent replay の結果を verdict にしない・測らずに結論しない）
- **直接前提**: [TASK-0219](TASK-0219.md)（前 iter の kairo-requirements 出力・ledger 指紋 LoRAConfig 列挙ガード+`seq_len` pin・本 TASK はその cache 側統合テスト主張の実証裏付け）・[TASK-0211](TASK-0211.md)（cache-fingerprint lane airtight）・[TASK-0218](TASK-0218.md)（ledger ModelConfig 列挙ガード）
- **memory**: [[ai-hub-feedback-infra-vs-this-repo]]（feedback は本 checkout に不在の記号を命名する・grep before acting）・[[silent-success-best-model-min-delta-axis]]（cache lane COMPLETE・本 TASK は新規 hunt ではなく既存主張の実証のみ = "re-hunt 禁止" の対象外）

## 背景: feedback 4 bullet の第2ラウンド要件化（step4 自動分析）

前 iter VALUABLE 評価の feedback 4 bullet を本 checkout の実態と再照合した。第1ラウンド（TASK-0219）との差分は「コード追加」から「**既存主張の実証的検証＋報告様式の充足**」への移行。

| bullet | 要請 | 本 checkout での判定 | 本 TASK の対応 |
|--------|------|---------------------|----------------|
| 1 | PURPOSE.md の 4-bullet 監査パターンを停止・TASK-0217 closeout を durable 扱い | **受容（no-op）**: `c331818` 記録済み。本 TASK も PURPOSE.md 無編集 | durable closeout を再確認のみ |
| 2 | label-affecting メタデータが全設定モデルと prompt 契約の変更を一貫して指紋へ反映することを、**テスト実行結果または具体的な失敗再現・修正ログ**で報告 | **変換→実証**: 記号は PHANTOM 持続（grep 再検証）。実在対応物（2 つの content-hash keying）は前 iter 完備だが、TC-132-09 統合テストの mutation 証明は "by construction" の**自己主張止まり**だった → 本 TASK で producer field drop mutation を実執行し、失敗再現ログを取得（下節） | REQ-001/002 実証 + 207 passed 実行ログ |
| 3 | truncation ノブの設定差による cache miss と再推論を**実サーバーまたは統合テスト**で確認（同一入力） | **変換→実証**: `context_size`/「実サーバー」は本 mirror に非在（server 側概念・生成導線は private infra = PHANTOM・half-port 禁止）。統合テストオプションは `test_max_seq_len_change_triggers_cache_miss_and_rebuild`（同一 `TokenDataset` 入力・`max_seq_len` 8→16 のみ差異）が充足。mutation 下で Run2 が `source='disk'` silent replay へ退化することを実証し「再推論が必須である理由」をログで裏付け | REQ-003 実証 + 失敗再現ログ |
| 4 | peripheral plumbing 戒め・9B run 再開か operator-facing decision の surface | **受容**: 9B 両 leg は既発火・既 harvest（`4b88ca8`/`1224057`）・再発火は TIES 再生産で情報量ゼロ（decision tool 本文 "Re-firing reproduces TIES and adds no information"）。operator decision tool を report mode で実実行し現在出力を surface | 「残存 open と operator 判断」節に実行出力を記録 |

**memory ガードとの整合**: [[silent-success-best-model-min-delta-axis]] は「re-hunt/re-verify churn は構造的に不要」（新規 collision hunt の禁止）を記録。本 TASK は新規 hunt ではなく、(a) feedback が明示要求した**既存主張の実証ログ化**、(b) 一時編集→即復元で最終 diff ゼロ、(c) テスト/ガードの追加ゼロ、であり禁止対象外。

## 機能要件（EARS 記法）

**【信頼性レベル凡例】**: 🔵 = PRD・設計・既存実装参照の確実要件 / 🟡 = 妥当な推測 / 🔴 = 自動推定依存

### 通常要件

- REQ-001: TC-132-09 統合テスト（`test_max_seq_len_change_triggers_cache_miss_and_rebuild`）の mutation 証明は、commit message の自己主張でなく、producer field drop の**実執行**により失敗再現ログとして実証されていなければならない 🔵 *feedback bullet 2「具体的な失敗再現・修正ログ」本文より*
- REQ-002: 同一 producer regression は、unit（TC-132-09 SHA 分岐）・integration（AsyncCacheBuilder lifecycle）・schema 列挙ガード（`TestCacheFingerprintCompleteness`）の**3 層すべて**で検知されなければならない 🔵 *TASK-0210/0211 の多層防御設計契約より*
- REQ-003: 「同一入力・設定差のみ・cache miss・再推論」の統合テストは、mutation 下で Run2 が `source='disk'`（= 旧 truncation 内容の silent replay）へ退化することが実証されていなければならない 🔵 *feedback bullet 3 本文・`5616e36` docstring の danger claim より*

### 制約要件

- REQ-401: 本 TASK は新規テスト・新規 seam・src/ の恒久変更を追加してはならない（mutation は一時編集→即復元・最終 `git diff` 0 行） 🔵 *feedback bullet 4「peripheral seam を開くな」+ memory ガードより*
- REQ-402: PURPOSE.md を編集してはならない（bullet 1 durable closeout） 🔵 *feedback bullet 1 本文より*
- REQ-403: 9B run を再発火してはならない。`--land` を AI 単独で実行してはならない（operator-gated・non-unilateral） 🔵 *memory [[ai-hub-feedback-infra-vs-this-repo]]・decision tool 本文より*

### オプション要件（実施しない判断を記録）

- REQ-301: 統合テストへの「cached content の byte 差分」表明の追加は実施し**しない** 🟡 *本 mirror では truncation は private `src.data.load_dataset`（mirror から意図的に除去）が適用し、`AsyncCacheBuilder` は受信済み token 列の forward のみを行う。固定長 `TokenDataset` で内容差分を表明すると mirror に存在しない挙動の捏造になる（phantom 罠・[[ai-hub-feedback-infra-vs-this-repo]]）。現行テストの「path 分岐＋`source='built'`」表明が mirror で証明可能な最大範囲*

## 主作業（失敗再現・修正ログ = bullet 2/3 への報告本体）

### Mutation M1: producer field drop（`max_seq_len` key 削除・一時編集→即復元）

**操作**: `src/tg_lora/prefix_feature_cache.py` `build_prefix_feature_cache_metadata` return dict から `"max_seq_len": max_seq_len,` 行を一時削除（将来のリファクタが producer の埋め込みを落とす regression の模倣）。

**実執行結果**（`/home/jinno/tg-lora/.venv/bin/python -m pytest`・py3.13 venv）:

| 層 | テスト | mutation 下 | 失敗メッセージ（実ログ抜粋） | 復元後 |
|----|--------|------------|------------------------------|--------|
| unit | `test_prefix_feature_cache.py::TestCachePathSha256::test_different_max_seq_len_gives_different_path` | **FAILED** | `AssertionError: assert PosixPath('/tmp/cache/train_39a821d85bf75457e8ca97b0.pt') != PosixPath('/tmp/cache/train_39a821d85bf75457e8ca97b0.pt')` — SHA-256 衝突を実証 | ✅ green |
| integration | `test_async_cache_builder_integration.py::test_max_seq_len_change_triggers_cache_miss_and_rebuild` | **FAILED** | `AssertionError: max_seq_len change must produce a different cache path; got identical paths ...vq_d73e23a4b1fb24a95526ca5d.pt. This means max_seq_len is no longer in the fingerprint — the silent-replay regression TC-132-09 exists to prevent.` — Run2 が Run1 の cache を silent replay する退化を実証 | ✅ green |
| schema 列挙ガード | `TestCacheFingerprintCompleteness::test_every_data_and_model_config_field_is_fingerprinted_or_allowlisted` | **FAILED** | `AssertionError: Cache-fingerprint mapping is stale: {'max_seq_len': 'max_seq_len'} claim fingerprint keys absent from build_prefix_feature_cache_metadata output` | ✅ green |

**復元検証**: `git diff` = **0 行**（恒久変更なし・REQ-401 充足）。

### テストスイート実行結果（bullet 2「テストスイート実行結果」充足）

| suite | 結果 | 意味 |
|-------|------|------|
| `tests/test_prefix_feature_cache.py` + `tests/test_async_cache_builder_integration.py` | **49 passed** | cache 指紋: schema 列挙ガード（`DataConfig`\|`ModelConfig` 全 field + cross-section `train_on_prompt`=LABEL/prompt 契約・`trainable_lora_scope`）+ TC-132-* pairwise + TC-132-09 統合テスト。feedback の「prompt 契約」対応物（`train_on_prompt`）の指紋反映を green で再確認 |
| `tests/test_freeze_validloss_ci_9b_resume.py -k fingerprint` | **18 passed** | ledger 指紋: per-field pin 群 + `ModelConfig`/`LoRAConfig` 列挙ガード（TASK-0218/0219）+ `seq_len` pin |
| 上記 4 file 合計（+ `tests/test_config_schema.py` + resume 全体） | **207 passed** | regression なし（49 + 158 と整合） |

### Phantom 再検証（memory 非盲信・毎 iter live grep）

| 記号 | 検索範囲 | hit | 判定 |
|------|----------|-----|------|
| `label_affecting` / `_LABEL_AFFECTING_CONFIG_FIELDS` | `src/` `scripts/` `tests/` | **0**（tests の hit は TC-132-09 docstring 内の feedback 引用 prose「label-affecting / activation-affecting collision」のみ = code 記号に非ず） | **PHANTOM 持続**・AI-Hub 私有 infra の語彙 |
| `context_size` | repo 全体（docs 除く） | **0**（docs の hit は TASK-0219 自体の言及のみ） | **PHANTOM 持続**・server 側概念・本 mirror の対応物は `max_seq_len` |

## 残存 open と operator 判断（bullet 4 への surface・実執行出力）

`scripts/section4_operator_decision.py` を report mode で実執行（`--land` なし・AI 単独 land 禁止 REQ-403）した現在出力の要約:

```text
Verdict arc: [homogeneous] TIES · citable · faithful · cand 1.6947 vs surr 1.6960 · CI[-0.0001, +0.0027]
             [heterogeneous] TIES · citable · faithful · cand 1.7180 vs surr 1.7191 · CI[-0.0011, +0.0028]
             arc_complete = True · "Re-firing reproduces TIES and adds no information"
Quality preservation vs full backprop (GOAL §4-247): 両 leg SURPASSES (n_baseline=3)
RECOMMENDATION: SHIP
Decision branches: ship: yes / accept_null: yes / pivot: no (private-repo only・src.data stripped_deliberate)
```

→ **operator 判断待ちの 3 分岐**（AI が単独で選んではならない）: `ship`（citable TIES を §4 結論として採用）/ `accept_null`（TIES を誠実な帰無として記録）/ `pivot`（private repo `/home/jinno/tg-lora` での src.data 絶対 loss 比較 = Cat-C・本 mirror では測定不能・half-port 禁止）。

code-doable な fingerprint/silent-replay gap は本 TASK をもって**実証済み**で残存ゼロ（cache lane = TASK-0211 完結 + 本 TASK の mutation 実証・ledger lane = TASK-0218/0219）。次 iteration への推奨: 新規 peripheral 作業を作らず、上記 operator 判断の待機を surface し続けること。

## Acceptance criteria

- [x] Mutation M1 を実執行し 3 層（unit/integration/schema 列挙ガード）すべて FAILED の実ログを取得（REQ-001/002/003）
- [x] 復元後 `git diff` 0 行・恒久変更なし（REQ-401）
- [x] 207 passed（cache+統合 49 / resume+config_schema 158）・regression なし
- [x] `label_affecting`（code 0 hit）/`context_size`（docs 除く 0 hit）phantom 再検証を記録
- [x] PURPOSE.md 無編集（REQ-402・bullet 1 durable closeout）
- [x] 9B 無再発火・`--land` 無実行・operator 3 分岐を実行出力付きで surface（REQ-403・bullet 4）

## 関連文書（出力）

- **要件定義**: 本ファイル（kairo-requirements 出力。本 repo 慣例により `specs/tg-lora/tasks/TASK-XXXX.md` 形式・parallel `requirements.md` は作らない = memory [[kairo-requirements-task-file-convention]]）
- **次のステップ**: code 作業ではなく operator 判断（ship/accept_null/pivot）の待機。設計文書相当は TASK-0219/本ファイルの「主作業」節に内包

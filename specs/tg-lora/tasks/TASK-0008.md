# TASK-0008: run_metrics.py GPUパス カバレッジ完成


<!-- spine:anchor:begin -->
> **Spine anchor**: [TG-LoRA アーキテクチャ設計](../architecture.md)
>
> - parent: `tg-lora/architecture.md`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0008
**タスクタイプ**: TDD
**推定工数**: 1.5時間
**フェーズ**: Phase 2 - カバレッジ完成とコード品質向上
**信頼性レベル**: 🔵 *run_metrics.py 既存実装・coverage report より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - NFR-302, NFR-304
- **設計文書**: [📐 architecture.md](../architecture.md) - ユーティリティ層

## タスク概要

`src/utils/run_metrics.py` のカバレッジを85%から100%に引き上げる。未カバーの9行（29, 45-46, 92-97）は全て `torch.cuda` 依存のGPUパスであり、`unittest.mock.patch("torch.cuda")` でモック化してテストする。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-0011](TASK-0011.md)

## 完了条件

- [x] `__init__` でCUDA利用可能時に `torch.cuda.reset_peak_memory_stats()` が呼ばれることを検証
- [x] `write_header` で `torch.cuda.get_device_name(0)` と `get_device_properties(0)` が取得されることを検証
- [x] `record_step` で `vram_usage_mb()` の結果が正しくレコードに反映されることを検証
- [x] `record_step` で `torch.cuda.max_memory_allocated(0)` のピーク更新ロジックが動作することを検証
- [x] `pytest tests/test_run_metrics.py` が全てパスする
- [x] `src/utils/run_metrics.py` カバレッジが100%に到達する

---

## 主要実装項目

### GPU-path モックテスト追加 🔵

**信頼性**: 🔵 *run_metrics.py の実装（lines 29, 45-46, 92-97）とcoverage report より*

`tests/test_run_metrics.py` に以下のテストを追加:

1. **test_init_resets_peak_memory**: `torch.cuda.is_available()` をモックしてTrue、`reset_peak_memory_stats` の呼び出しを検証
2. **test_write_header_with_gpu**: `torch.cuda.get_device_name` と `get_device_properties` をモックして、record内の gpu_name, gpu_total_memory_mb を検証
3. **test_record_step_gpu_vram**: `vram_usage_mb` をモックして、gpu_allocated_mb, gpu_reserved_mb が正しく記録されることを検証
4. **test_record_step_peak_memory_update**: `torch.cuda.max_memory_allocated` をモックして、ピーク値の更新ロジックを検証

**実装ファイル**: `tests/test_run_metrics.py`

---

## 基本的なテスト要件

### モックパターン 🔵

**信頼性**: 🔵 *既存テスト（test_memory.py等）のモックパターンより*

- `unittest.mock.patch("torch.cuda")` でGPU依存関数をモック化
- `unittest.mock.patch("src.utils.run_metrics.vram_usage_mb")` でVRAM取得をモック化
- CUDA利用不可のパスは既存テストでカバー済み

---

## 実装手順

### TDDタスクの場合

1. テストケースを `tests/test_run_metrics.py` に追加
2. モックの戻り値を設定し、カバレッジ対象行を実行
3. `pytest tests/test_run_metrics.py --cov=src.utils.run_metrics` でカバレッジ確認
4. 全テストパスを確認

---

## 信頼性レベルサマリー

- **総項目数**: 2項目
- 🔵 **青信号**: 2項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質

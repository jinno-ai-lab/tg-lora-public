# TASK-0214 — wire the heterogeneous full-budget full-backprop baseline arm (close the P1 品質保持 UNANSWERED cell)

> **type**: fix
> **scope**: §4 full-budget heterogeneous launch path (`Makefile`,
> `scripts/fire_freeze_ci_9b_full_heterogeneous.sh`) + decision-surface rationale
> (`scripts/section4_operator_decision.py`)
> **constitution link**: P0 科学誠実性 (don't leave a *measurable* axis unmeasurable by
> an omitted flag) + P1 品質保持 (GOAL §4 line 247: "does freezing cost quality?")

## Context (the direct successor to TASK-0213)

TASK-0213 (`7a5c535`) surfaced the GOAL §4-247 **full-backprop baseline** verdict per leg
and found a real asymmetry: **homogeneous** answers P1 品質保持 (`baseline.verdict = SURPASSES`,
candidate **1.6947** vs full-backprop **1.8794**) but **heterogeneous** carries
`n_baseline=0` / `baseline=null` ⇒ **UNANSWERED**. TASK-0213 *reported* the gap; this task
*closes its root cause* — the heterogeneous full-budget launch path simply omitted the
`--n-baseline` flag the homogeneous path carries.

This is the one code-doable, in-this-mirror, GPU-fireable research delta the feedback's
"prioritize the 9B run" lever names: it needs **no** `src.data` (public Dolly), and the
GPU is the only transient factor.

## The gap (wiring, not science)

`FREEZE_9B_FULL_FLAGS` (homogeneous) ships `--n-baseline 3`; `FREEZE_9B_FULL_HETEROGENEOUS_FLAGS`
shipped `--n-control 3` **only** (mirroring the heterogeneous-generalization direction-isolation
A/B). So the canonical heterogeneous full-budget target could never produce a baseline arm —
the UNANSWERED was a flag omission, not a measured null. `scripts/fire_freeze_ci_9b_full_heterogeneous.sh`
(the robust, worktree-recycling-safe launcher) had the same omission, and the composition
guards *pinned* that omission as design (`assert "--n-baseline" not in text`).

## Fix (small-diff, additive — wiring only)

1. `Makefile` `FREEZE_9B_FULL_HETEROGENEOUS_FLAGS`: add `--n-baseline 3` (P1 品質保持 now wired
   for both full-budget legs). Comment updated to record the arm shape = direction-isolation A/B
   **+** the full-backprop baseline axis.
2. `scripts/fire_freeze_ci_9b_full_heterogeneous.sh`: add `--n-baseline 3` so the robust launcher
   matches the canonical target (no Makefile/fire drift).
3. **Resume reality (verified 2026-07-27 on the live fire)**: `--n-baseline` is NOT in the
   per-arm ledger fingerprint, so a re-fire against a *current* 9-arm ledger would skip the 9
   and run only the 3 baselines. BUT the surviving 7/18 ledger's header predates the
   fingerprint-evolution commits (15 keys vs the worker's 21 — missing `learning_rate`/
   `lora_r`/`lora_alpha`/`lora_dropout`/`lora_target_modules`/`max_dataset_rows`), so
   `load_ledger` treats it as stale (returns `{}`) and the worker re-runs **ALL 12 arms
   (~4h)**; the first arm banks by truncating the stale file + writing a fresh header
   (`append_arm_to_ledger` opens `mode="w"` on a stale header). That §7-honest re-run is the
   cost of closing this cell; the fresh ledger it writes will support resume-skip on any
   later re-fire.
4. `scripts/section4_operator_decision.py`: the UNANSWERED rendering + SHIP rationale now name the
   corrected canonical fire command instead of the stale "code-doable on public Dolly" phrasing.
5. Composition guards flipped from "baseline absent" (old direction-isolation-only design) to
   "baseline present" (matrix symmetry) — both the Makefile flag var and the fire script.

## Verification

- `tests/test_run_freeze_validloss_ci_9b.py::TestFullBudgetHeterogeneousLaunchPath`
  `test_makefile_wires_distinct_full_budget_heterogeneous_paths` (+`--n-baseline 3` assertion)
  and `test_fire_script_robustly_launches_full_budget_heterogeneous` (flipped to present) — GREEN.
- `tests/test_section4_operator_decision.py` — full file **49 passed**, zero regression.
  Live tool: heterogeneous line now reads "UNANSWERED — wired into the canonical full-budget
  target (make freeze-validloss-ci-9b-full-heterogeneous includes --n-baseline 3)".
- `ruff check` clean on both `scripts/` files.
- **Recommendation unchanged (SHIP), exit-3 blocking unchanged** — this wires the missing arm;
  it does not change the decision.

## What this is NOT

Not a fabricated verdict (the heterogeneous baseline arm is wired AND fired — see Run status
below), not eval-script hardening, not a "pin X directly" test on a saturated surface (the guards
encode a real matrix-symmetry invariant that the old design *opposed*), and not a unilateral
operator decision (the `--land` gate is untouched).

## Run status (2026-07-27 08:03 — live)

Fired 07:57 via the robust stable-dir launcher (`scripts/fire_freeze_ci_9b_full_heterogeneous.sh`,
GPU was free). Verified sound against the worker code: a stale 7/18 header makes `load_ledger`
return `{}` (re-run all 12) and the first bank truncates the stale file + writes a fresh header
(`mode="w"`) — so no corruption, no orphaned arms. First candidate arm hit its first freeze
(layer=31) at step 150 (07:59:55); GPU 11.6 GiB / 84%; ledger/deposit mtime still 7/18 because no
arm has banked yet (arm 1 ~step 400/1500). ETA ~12:20 (12 × ~22 min). Banks to the stable-dir
ledger, so worktree recycling cannot lose the deposit.

Harvest is **deferred to the next session**: run ETA (~12:20) + the ~30 min harvest buffer
overflows this token window. The fire-script header documents the exact harvest (copy deposit +
ledger → stamp `ledger_witness_path`/`ledger_witness_sha256` → pin the two frozen test literals
`TestCommittedLedgerWitnessHeterogeneousFull._FROZEN_WITNESS_HASH` and the `_EXPECTED` entry in
`TestDepositEvidenceHash`). The 9 direction arms merely re-confirm the landed TIES (`1224057`);
the **3 baseline arms are the new P1 品質保持 delta** that closes the UNANSWERED cell.

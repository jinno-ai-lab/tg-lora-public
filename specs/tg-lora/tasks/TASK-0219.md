# TASK-0219: feedback 4 bullet の要件整理（kairo-requirements）→ 実在対応物へ変換: ledger 指紋の **LoRAConfig 列挙ガード**＋**`seq_len` per-field pin** を追加（mutation-proven 双方向）、`label_affecting`/`context_size` は grep-proven PHANTOM（code 0 hit）・bullets 1/4 は durable closeout + operator-facing decision の surface

<!-- spine:anchor:begin -->
> **Spine anchor**: [TG-LoRA アルゴテクチャ設計](../architecture.md)
>
> - parent: `tg-lora/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0219
**タスクタイプ**: fix+docs（kairo-requirements 要件整理。feedback bullet 2「全設定モデルの一貫した指紋反映をテスト実行結果/修正ログで報告」・bullet 3「truncation ノブの設定差→cache miss/再推論の確認」を本 checkout の実在対応物へ変換し、resume-ledger 指紋に (a) `LoRAConfig` schema 列挙ガード〔TASK-0218 の ModelConfig ガードの対称補完・omission 失敗モードを初 pin〕と (b) `seq_len` per-field drop-pin〔truncation 族・cache 側 TC-132-09 の ledger 側対称〕を追加。命名記号 `label_affecting`/`context_size` は grep-proven PHANTOM。bullets 1/4 は durable closeout として記録し operator-facing decision を surface）
**推定工数**: 1時間
**フェーズ**: AI Hub 反復。前 iter（`729ef41` ledger model-quant gap 閉鎖 + `6ca9255` heterogeneous harvest + `c331818` TASK-0217 closeout）は VALUABLE 評価。本 TASK は feedback の「steering input であって維持するレポートではない」の指示に従い、bullet 2/3 を実コードの検証＋最小 fix に変換する。
**信頼性レベル**: 🔵 *memory を盲信せず live checkout で検証: (1) `label_affecting`/`_LABEL_AFFECTING_CONFIG_FIELDS` は code 0 hit（PURPOSE.md/TASK-0217/0218 の docs 言及のみ）〔PHANTOM〕・(2) `context_size` は repo 全 0 hit〔PHANTOM・server 側概念〕・(3) 実在対応物＝2 つの content-hash keying（cache 指紋 `build_prefix_feature_cache_metadata` + ledger 指紋 `_config_fingerprint`）を自前監査し `LoRAConfig` omission ガード欠如という真の gap を発見・(4) テスト 2 件追加 + mutation 3 方向証明（drop×2・omission×1）+ 158 passed / ruff clean*

## 関連文書

- **正本**: [docs/GOAL.md](../../../docs/GOAL.md) §4（9B §4 verdict arc）・§7（測定せず結論しない・Constitution Rule #6 = silent replay の結果を verdict にしない）
- **直接前提**: [TASK-0218](TASK-0218.md)（ledger `_config_fingerprint` の model-quant gap 閉鎖 + `ModelConfig` 列挙ガード導入 — 本 TASK はその `LoRAConfig` 対称補完）・[TASK-0217](TASK-0217.md)（feedback 命名記号の phantom 検証プロトコル）・[TASK-0211](TASK-0211.md)（cache-fingerprint lane 全次元 airtight・cross-section byte-changing field ガード）
- **memory**: [[ai-hub-feedback-infra-vs-this-repo]]（feedback は本 checkout に不在の記号を命名する・grep before acting）・[[resume-ledger-fingerprint-model-quant-axis]]（TASK-0218 = ledger keying 監査の正当性根拠）・[[silent-success-best-model-min-delta-axis]]（cache lane COMPLETE・本 TASK は ledger lane のみ拡張）

## 背景: feedback 4 bullet の要件化（step4 自動分析）

feedback（前 iter = VALUABLE）の 4 bullet を本 checkout の実態と照合し、変換・却下を判定した。

| bullet | 要請 | 本 checkout での判定 | 対応 |
|--------|------|---------------------|------|
| 1 | PURPOSE.md の 4-bullet 監査パターンを停止・TASK-0217 closeout を durable 扱い | **受容（no-op）**: `c331818` が closeout を記録済み。PURPOSE.md 再編集は行わない | 本 TASK に durable closeout として記録 |
| 2 | label-affecting メタデータが全設定モデルと prompt 契約の変更を一貫して指紋へ反映することを、**テスト実行結果または失敗再現・修正ログ**で報告 | **変換**: `label_affecting`/`_LABEL_AFFECTING_CONFIG_FIELDS` は code grep 0 hit（PHANTOM・AI-Hub 私有 infra の記号）。実在対応物 = 2 つの content-hash keying の completeness ガード群。監査で ledger 側の `LoRAConfig` omission 未保護を発見 → 修正ログ付きで報告 | REQ-001/002 実装 + テスト実行結果を本ファイルに記録 |
| 3 | `context_size`（truncation/server reject がラベルを変える）の設定差による cache miss と再推論を実サーバーまたは**統合テスト**で確認 | **変換**: `context_size` は repo 全 0 hit（PHANTOM・server 側概念）。本 checkout の対応物は cache 側 `max_seq_len`（TC-132-09 統合テスト = 「統合テスト」オプションを満たす）+ ledger 側 `seq_len`（指紋入りだが per-field drop-pin 無し = 非対称） | REQ-003 実装 + 統合テスト実行結果を記録 |
| 4 | peripheral plumbing 傾向の戒め・9B run 再開か operator-facing decision の surface | **受容**: 本 TASK は新規 seam を開かず既存 silent-replay クラスの guard 拡張のみ（peripheral hygiene ではない）。9B 再発火は Cat-C（private `src.data` 絶対 loss）+ operator `--land` は non-unilateral → surface にとどめる | 「残存 open と operator 判断」節に記録 |

## 機能要件（EARS 記法）

**【信頼性レベル凡例】**: 🔵 = PRD・設計・既存実装参照の確実要件 / 🟡 = 妥当な推測 / 🔴 = 自動推定依存

### 通常要件

- REQ-001: resume-ledger 指紋の completeness ガードは、`apply_lora` が全 arm のアダプタへ実体化する `LoRAConfig` の全 schema field（`r`/`alpha`/`dropout`/`target_modules`）を列挙し、指紋キーの欠落（drop）と将来 field の未指紋化（omission）の**両方**を失敗させなければならない 🔵 *`src/training/config_schema.py:55` LoRAConfig・`src/model/load_model.py:237` apply_lora・TASK-0218 の ModelConfig ガード対称性より*
- REQ-002: ガードの失敗モードは mutation により双方向証明されていなければならない（drop 方向 = 既存 per-field テスト + 本ガードのキー存在表明・omission 方向 = 未指紋 field 追加で `unaccounted` 表明が発火） 🔵 *TASK-0218「catches BOTH a drop AND an omission」設計契約より*
- REQ-003: ledger 指紋の `seq_len`（tokenization/truncation 境界）は per-field drop-pin を持ち、`seq_len` だけが異なる再実行が旧 ledger を silent replay しないことを保証しなければならない 🔵 *`_config_fingerprint` return dict `"seq_len"` key・cache 側 TC-132-09（`max_seq_len` 変更→cache miss+rebuild 統合テスト）との対称性より*

### 制約要件

- REQ-401: 本 TASK は新規テスト seam を開いてはならない。既存 `tests/test_freeze_validloss_ci_9b_resume.py` の fingerprint セクションへの追加のみとする（feedback bullet 4「peripheral seam を開くな」+ TASK-0217 bullet 4 の guardrail） 🔵 *feedback 本文より*
- REQ-402: `LEDGER_VERSION` の bump・fixture 再生成を行ってはならない（additive なテストのみのため。TASK-0218 の先例準拠） 🔵 *TASK-0218 §3–4 より*

### オプション要件（実施しない判断を記録）

- REQ-301: ledger 指紋への `TrainingConfig` 全 field 列挙ガードの拡張は実施し**ない** 🟡 *本 9b script が読む TrainingConfig field は `learning_rate` のみ（`cfg.training.learning_rate`、line 1618/2801）で per-field テスト既存。全 field 列挙は大きな allowlist を要し、omission リスクは `LoRAConfig`（全 field が `apply_lora` で実体化）より低い。将来 script が新しい training field を読むようになった時点で追加が正当化される*

## 主作業（修正ログ）

### 1. `test_fingerprint_changes_with_seq_len`（drop-pin・REQ-003）

`tests/test_freeze_validloss_ci_9b_resume.py` の `test_fingerprint_changes_with_total_steps` 直後に追加。`_fp(seq_len=1024) != _fp(seq_len=2048)` を表明。docstring に cache 側 TC-132-09 との truncation 族対称性を記録。

### 2. `test_fingerprint_completeness_every_lora_config_field_covered`（列挙ガード・REQ-001/002）

同ファイルの `test_fingerprint_completeness_every_model_config_field_covered`（TASK-0218）直後に追加。構造は ModelConfig ガードと同一:
- field→指紋キーマッピング `{"r": "lora_r", "alpha": "lora_alpha", "dropout": "lora_dropout", "target_modules": "lora_target_modules"}` の全キーが `_fp()` サンプルに存在すること（drop 検知）
- `unaccounted = set(LoRAConfig.model_fields) - set(mapping)` が空であること（omission 検知）

### 3. Mutation 証明（REQ-002・全て一時編集→即復元）

| mutation | 操作 | 期待 | 結果 |
|----------|------|------|------|
| A: seq_len drop | `_config_fingerprint` return dict から `"seq_len"` key を一時削除 | `test_fingerprint_changes_with_seq_len` FAIL | ✅ FAILED・復元後 green |
| B: guard drop 方向 | ガード mapping から `"alpha": "lora_alpha"` を一時削除 | 本ガード FAIL | ✅ FAILED・復元後 green |
| C: guard omission 方向 | `LoRAConfig` に将来 field 模倣 `use_rslora: bool = False` を一時追加 | `unaccounted=['use_rslora']` で FAIL | ✅ FAILED（精密メッセージ一致）・復元後 green |

## テスト実行結果（bullet 2/3 への報告本体）

`/home/jinno/tg-lora/.venv/bin/python -m pytest`（py3.13 venv）:

| suite | 結果 | 意味 |
|-------|------|------|
| `tests/test_prefix_feature_cache.py` + `tests/test_async_cache_builder_integration.py` | **49 passed** | cache 指紋: schema 列挙ガード（`TestCacheFingerprintCompleteness` = `DataConfig`|`ModelConfig` 全 field + cross-section `train_on_prompt`=LABEL/prompt 契約・`trainable_lora_scope`）+ TC-132-* pairwise + **TC-132-09 `max_seq_len` 変更→cache miss+rebuild 統合テスト**（bullet 3 の「統合テスト」オプション充足） |
| `tests/test_freeze_validloss_ci_9b_resume.py -k fingerprint` | **18 passed**（旧 16 + 新 2） | ledger 指紋: per-field 変化 pin 群 + `ModelConfig` 列挙ガード（TASK-0218）+ **本 TASK 追加の `seq_len` pin・`LoRAConfig` 列挙ガード** |
| `tests/test_freeze_validloss_ci_9b_resume.py` + `tests/test_config_schema.py` 全体 | **158 passed** | regression なし |
| `ruff check tests/test_freeze_validloss_ci_9b_resume.py` | **All checks passed** | lint clean |

**結論**: 本 checkout に実在する 2 つの content-hash keying（cache 指紋・ledger 指紋）は、byte/result を変える全 config section（`DataConfig`/`ModelConfig`/`LoRAConfig` + cross-section `TrainingConfig.train_on_prompt`/`trainable_lora_scope` + ledger 固有の budget/scope/seed 群）について、drop と omission の両失敗モードを pin した状態になった。feedback の「prompt 契約」対応物（`train_on_prompt` = LABEL マスク変更）は cache 側で既に mutation-proven（TASK-0206/0211）であり、本 TASK の実行で green を再確認した。

## 残存 open と operator 判断（bullet 4 への surface）

本 checkout で code-doable な silent-replay/fingerprint gap は本 TASK をもって監査尽くし（cache lane = TASK-0211 完結・ledger lane = TASK-0218 + 本 TASK）。残る open は code 変更では閉じられない 2 件のみ:

1. **private `src.data` 絶対 loss 比較**（DATA/Cat-C）: 本 mirror から意図的に除去された私有データ導線。本 checkout では測定不能・half-port 禁止（memory [[ai-hub-feedback-infra-vs-this-repo]]）。
2. **`scripts/section4_operator_decision.py --land`**（operator-gated・non-unilateral）: §4 arc は DONE（両 leg direction TIES + quality-vs-full-backprop 両 leg SURPASSES・`recommendation=SHIP`）だが、land は operator 判断 by design。AI が単独で land してはならない。

→ 次 iteration への推奨: 新規 peripheral 作業は作らず、上記 2 件は operator の意思決定待ちである旨を surface し続けること。9B 再発火は private `src.data` 無しでは Cat-C のまま。

## Acceptance criteria

- [x] `test_fingerprint_changes_with_seq_len` 追加・green・mutation A で drop 検知を証明
- [x] `test_fingerprint_completeness_every_lora_config_field_covered` 追加・green・mutation B（drop）/C（omission）双方向証明
- [x] 158 passed（resume + config_schema 全体）・ruff clean
- [x] `label_affecting`（code 0 hit・docs 言及のみ）/`context_size`（全 0 hit）の phantom 判定を記録
- [x] `LEDGER_VERSION` 無 bump・fixture 無再生成・新規 seam 無開設（REQ-401/402）
- [x] PURPOSE.md 無編集（bullet 1 durable closeout）

## 関連文書（出力）

- **要件定義**: 本ファイル（kairo-requirements 出力。本 repo 慣例により `specs/tg-lora/tasks/TASK-XXXX.md` 形式・parallel `requirements.md` は作らない）
- **次のステップ**: 設計文書相当は TASK-0218/本ファイルの「主作業」節に内包。次のお勧めは operator 判断の待機（code 作業ではない）

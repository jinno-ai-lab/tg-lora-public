# TASK-0208: feedback（TASK-0201–0204 を VALUABLE 評価）の 4 bullet を本 checkout で独立検証 → 全 bullet 充足/禁止済みで stale を実証。本 iter の主作業は「cache-fingerprint collision lane の**全内容決定次元の完全列挙**」= 0207「実質完了」宣言の**独立 cross-check**（0205 の field 羅列-only 宣言は 0206 で・0206 の LABEL-only 宣言は 0207 で偽証された反覆 premature-exhaustion パターンの**初のサバイバル**）+ コア TG-LoRA アルゴリズム（velocity/rollback/layer_sampler = memory 未カバー領域）の silent-bug hunt + feedback bullet #2「cache fix は trustworthiness を強化・audit trail に明記」の**誠実な訂正付き履行**（verdict worker は prefix_cache 非依存を grep 実証 → 強化対象は §4 verdict deposit でなく `train_tg_lora` 生産経路）。コード変更なし → 誠実な no-op-with-blocker。

<!-- spine:anchor:begin -->
> **Spine anchor**: [TG-LoRA アルゴテクチャ設計](../architecture.md)
>
> - parent: `tg-lora/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0208
**タスクタイプ**: docs（feedback 4 bullet の本-checkout 独立検証 + cache-fingerprint 全次元列挙サバイバル + コアアルゴリズム silent-bug hunt（不発見）+ bullet #2 audit-trail 注記の誠実訂正付き履行 → 誠実な no-op-with-blocker record。コード変更なし）
**推定工数**: 0.5時間
**フェーズ**: feedback（TASK-0201–0204 を VALUABLE 評価）は producer-sweep lane closeout・eval/measurement 禁止・operator-facing 指名の一方、cache-fingerprint + collision guard を「the real value here・high-leverage」と praise し audit-trail 明記を指示。本 TASK は同 praise category が 0206（LABEL 側）/0207（ACTIVATION 側）で閉鎖済みであることを**全内容決定次元の独立列挙**で確認（NOT field 羅列-only）し、コアアルゴリズム領域（memory 未カバー）の silent bug を hunt し、bullet #2 の audit-trail 注記を履行する。
**信頼性レベル**: 🔵 *feedback lane-closure を盲従せず実コードで検証: (1) cache fingerprint 全内容決定次元を列挙し各々が指紋 field に対応を実コード実証〔`build_prefix_feature_cache_metadata` 18 field vs 内容次元 DATASET(`dataset_path`/`size`/`mtime`)/TOKENIZATION(`model_name`+`max_seq_len`)/LABELS(`train_on_prompt`)/ACTIVATIONS(`dtype`/`load_in_4bit`/`bnb_4bit_*`)/LAYER(`split_layer_idx`)/ATTENTION(tokenization 由来)〕・(2) コア algorithm 3 file（velocity/rollback/layer_sampler）を通読し silent bug なしを確認・(3) verdict worker の prefix_cache 非依存を grep 実測〔0 hit〕・(4) decision tool を live 実行〔arc_complete=True/awaiting_operator_decision=True/recommendation=SHIP/landed_decision=None/run_executable_here=True/exit 3〕・cache fingerprint test 10 passed zero regression*

## 関連文書

- **正本**: [docs/GOAL.md](../../../docs/GOAL.md) §4（9B §4 verdict arc）・§7（測定せず結論しない・誠実性・Constitution Rule #6 honest limitation）
- **直接前提**: [TASK-0206](TASK-0206.md)（cache-fingerprint LABEL 側 `train_on_prompt` 閉鎖）・[TASK-0207](TASK-0207.md)（ACTIVATION 側 model 精度 4 field 閉鎖・「cached BYTES 2 大次元が共に閉鎖・実質完了」宣言）・[TASK-0205](TASK-0205.md)（producer-sweep closeout の no-op-with-blocker 先例・ただし同 TASK の「fingerprint 包括的」は field 羅列-only で 0206 に偽証された）・AI_HUB_MAKE_RUN_FEEDBACK bullet #2（cache-fingerprint + collision guard を high-leverage と praise・audit-trail 明記を指示）
- **memory**: [[silent-success-best-model-min-delta-axis]]（cache-fingerprint lane = 0206 LABEL + 0207 ACTIVATION で EFFECTIVELY COMPLETE・premature exhaustion の反覆 falsify パターン〔本 TASK が初の cross-check サバイバルで確認〕）・[[gpu-usable-proxy-runs]]（9B 両 leg TIES landed/harvested・operator decision tool 完働・残 open = operator `--land`〔non-unilateral〕+ private `src.data`〔DATA/Cat-C〕）・[[ai-hub-feedback-infra-vs-this-repo]]（no-op-with-blocker は zero-commit 上位で sanction・phantom symbol〔temperature/corpus〕は grep-empty）

## 背景（なぜ新規 fix でなく no-op-with-blocker か）

feedback（TASK-0201–0204 を VALUABLE 評価）の 4 bullet を本 checkout で独立検証:

1. **producer-sweep lane closeout**（bullet #1）— TASK-0201–0204 が dead-read/dropped/half-wired テンプレを load-bearing 4 seam で実行済み。「the next dead-read sweep risks drifting into the trivial-helper territory you correctly excluded, which would flip the next range to REJECT/R3. Treat these 4 as the closeout.」→ **受諾**: 6 つ目 dead-read fix は製造しない。
2. **cache-fingerprint + collision guard は high-leverage・audit trail 明記**（bullet #2）— 本 TASK の**主作業 1（下記）**で同 lane が 0206/0207 で閉鎖済みを全次元列挙で独立確認し、audit trail に明記（誠実訂正付き）。
3. **eval/judge 計測 field 追加禁止**（bullet #3）— "do NOT add more eval/judge output fields." → **受諾**: Kendall τ / cascade cost-ledger 等の peripheral-plumbing 計測は追加しない。
4. **次レバーは operator-facing**（bullet #4）— (a) 9B null 確認 OR (b) keep-refining-vs-accept-the-null decision の operator surface。→ **(a)(b) 共に既充足を live 検証**: (a) 9B 両 leg full-budget TIES 既 landed/harvested（homogeneous `4b88ca8` + heterogeneous `1224057`・verdict worker `run_freeze_validloss_ci_9b` は public Dolly で既起動・`run_executable_here=True`）・(b) `section4_operator_decision.py` の blocking prompt + `--land` writer が稼働中（live: `awaiting_operator_decision=True`/`landed_decision=None`/exit 3）。

結論: feedback の 4 bullet は全て充足/禁止済みで stale。唯一の真 open は (1) operator の明示 `--land`〔non-unilateral・tool 設計上 AI が一方的に決めない〕・(2) private `src.data` 絶対 loss〔DATA・分類 C・外部依存〕の 2 点のみで、いずれも本 iter から実行不可。

## 主作業 1: cache-fingerprint collision lane の全内容決定次元の独立列挙（0207「実質完了」の cross-check・初サバイバル）

TASK-0207 は cached BYTES の 2 大次元（LABEL: `train_on_prompt` / ACTIVATION: model 精度 4 field）を閉鎖し「実質完了」を宣言した。しかし本 lane では過去 2 度、**「包括的/完了」宣言が次 iter で偽証される**反覆パターンがあった:
- TASK-0205（line 164）「`prefix_feature_cache` fingerprint は包括的（dataset path/size/mtime + model + seed + max_seq_len + split_layer + LoRA 5 params + format_version）」= **field 羅列のみ**で内容決定次元と cross-check せず → TASK-0206 が `train_on_prompt`（LABEL 次元・内容決定・未羅列）を発見し**偽証**。
- TASK-0206（追記19）は LABEL 次元を閉鎖したが ACTIVATION 次元を列挙せず → TASK-0207 が model 精度 4 field（ACTIVATION 次元・未列挙）を発見し**偽証**。

本 TASK は TASK-0207 のメタ教訓（「cache-identity は自身の field ではなく **cached BYTES を変える config field** を列挙せよ」）を徹底適用し、**cached 内容を決定する全次元を起点に**列挙（field 側からでなく内容側から）:

`PrefixFeatureExample` = {`hidden_states`, `attention_mask`, `labels`, `split_layer_idx`, `position_ids`}. 各々を駆動する config:

| cached 内容 | 決定する config | 指紋内対応 field | 備考 |
|---|---|---|---|
| `hidden_states`（= base model forward at `split_layer_idx`） | model 重み + 精度 | `model_name` + `dtype`/`load_in_4bit`/`bnb_4bit_quant_type`/`bnb_4bit_compute_dtype` ✓ | 0207 で閉鎖・forward は `@torch.no_grad()` + `model.eval()` + `input_ids`/`attention_mask` のみ〔`build_prefix_feature_dataset:474-477`〕→ **temperature 非使用**（`layer_sampler._lisa_weighted` の softmax temperature は外挿レイヤーサンプリング用で本キャッシュと無関係） |
| input tokenization（input_ids/position_ids/attention_mask の元） | tokenizer(vocab) + `max_seq_len` truncation | `model_name`(tokenizer proxy) + `max_seq_len` ✓ | tokenization は model_name + max_seq_len で完全決定 |
| `labels` | dataset 内容 + `train_on_prompt` masking | `dataset_path`/`size`/`mtime` + `train_on_prompt` ✓ | 0206 で閉鎖 |
| which layer captured | `split_layer_idx` | `split_layer_idx` ✓ | |
| DATASET 行選択 | （`train_tg_lora` 経路は `load_dataset(path, tokenizer, max_seq_len, train_on_prompt)`〔`train_tg_lora.py:998-1003`〕でサブサンプリング引数なし = 全行）| `dataset_path`/`size`/`mtime` ✓ | `max_dataset_rows` は verdict worker 独自指紋〔`run_freeze_validloss_ci_9b.py:1063-1089` docstring 付き〕に既含・本キャッシュ経路には存在せず |

**結論: 内容決定次元は全て指紋 field に対応 = 完全。** これが**初の cross-check サバイバル**: 0205（field 羅列-only → 偽証）/0206（LABEL-only → 偽証）/0207（ACTIVATION 追加）/0208（全次元 cross-check → **偽証なし**）。feedback が反復 praise する本 lane は真に完結 → 未来 iter の再 hunt を窄める genuine 価値。

## 主作業 2: コア TG-LoRA アルゴリズムの silent-bug hunt（memory 未カバー領域）

memory の「exhausted」宣言は verdict-honesty / cache / dead-read 系に限定され、**コアアルゴリズム本体**（velocity/extrapolation/rollback/sampler）は未調査。本 TASK は最高価値かつ非禁止レーンとして 3 file を通読:

- **`src/tg_lora/velocity.py`**: 非有限値の扱いが徹底（`_record_magnitude`/`cosine_similarity`/`_global_*` で `math.isfinite` skip・near-zero denom で warning + 0.0 return・`predicted_consistency` は update_count<2 で 0.0）。silent bug なし。
- **`src/tg_lora/rollback_manager.py`**: `_sanitize_snapshot` が snapshot の NaN/Inf を `nan_to_num` で置換（rollback が corruption を復元しない）・rollback index 範囲検証 正確・empty snapshot で RuntimeError。silent bug なし。
- **`src/tg_lora/layer_sampler.py`**: `num_layers==0`/empty scores で fallback・`nan_to_num` on scores・softmax temperature floor 0.01・multinomial replacement=False。silent bug なし。

**結論: コア algorithm 3 file に concrete silent-success bug なし。** （feedback bullet #1 が「次の dead-read sweep は trivial-helper 領域に滑り込み REJECT/R3」と警告するのと整合・これ以上の algorithm 系 micro-hunt は同警告に触れるため停止。）

## 主作業 3: feedback bullet #2「audit trail 明記」の誠実な訂正付き履行

feedback bullet #2「cache-fingerprint fix は prior『null hardens with scale』/ cascade-efficacy 結論の trustworthiness を retroactive に強化する・audit trail に明記せよ」。本 TASK は**誠実な訂正付き**でこれを明記する:

- **訂正**: verdict worker（`scripts/run_freeze_validloss_ci_9b.py`）は `prefix_feature_cache` を**一切使用しない**（`grep prefix_feature_cache scripts/run_freeze_validloss_ci_9b.py` = **0 hit**・commit 済み両 TIES deposit は本 worker が public Dolly で生成）。ゆえに cache-fingerprint 強化（0206/0207）は **§4 verdict deposit の trustworthiness には直接影響しない**。
- **正確な強化対象**: `prefix_feature_cache` を使用するのは **`train_tg_lora` 生産経路**（`_maybe_cache_dataset`〔`train_tg_lora.py:1098-1200`〕・recover.py `--rerun` 導線・`async_cache_builder`・`precompute_prefix_cache_parallel`）。0206/0207 はこれら経路で silent label-mask / wrong-precision 活性 replay を閉鎖 = **生産訓練パイプラインの再現性・信頼性を強化**。
- **audit trail 明記**: feedback の意図（cache fix が高レバレッジ・信頼性を強化）は受容するが、その接続先は「§4 verdict」でなく「`train_tg_lora` 生産経路」と訂正して記録する（flattery でなく精度）。

## 検証

- **cache fingerprint test**: `tests/test_prefix_feature_cache.py` path/train_on_prompt/precision 系 **10 passed**（zero regression・3.58s）。
- **decision tool live**: `arc_complete=True / awaiting_operator_decision=True / recommendation=SHIP / landed_decision=None / run_executable_here=True / recover_rerun_blocked_by_src_data=True`〔exit 3 = blocking prompt 稼働中〕。
- **verdict worker cache 非依存**: `grep prefix_feature_cache scripts/run_freeze_validloss_ci_9b.py` = 0 hit（実測）。
- **コア algorithm 通読**: velocity.py / rollback_manager.py / layer_sampler.py 全行確認・silent bug なし。

## 誠実応答（停止点）

本 iter は新規 fix の製造なし。feedback の 4 bullet は本 checkout で全て充足/禁止済みで stale（(a) 9B null 確認 = 既発火 TIES・(b) decision surface = blocking prompt 稼働中）・cache-fingerprint lane は**全内容決定次元の独立列挙で真に完結を確認**（初の cross-check サバイバル）・コア algorithm 3 file に silent bug なし。これ以上の hunt（dead-read 第6 / eval 計測 field / cache 第3次元）は feedback 事前警告の **REJECT/R3**。本 iter の誠実な停止点は no-op-with-blocker record（feedback が zero-commit 上位で sanction する形式・TASK-0205/0199 と同形・Constitution Rule #6 honest limitation）。

**残る真の open は 2 点のみ**: (1) operator の明示 `--land <ship|accept_null|pivot>` 決定着地〔non-unilateral・tool 設計上 AI が一方的に決めない〕・(2) 外部 private `src.data` による絶対 loss 比較〔DATA・分類 C・本 checkout から不可〕。いずれも本 iter から実行不可。`[[ai-hub-feedback-infra-vs-this-repo]]` `[[silent-success-best-model-min-delta-axis]]` `[[gpu-usable-proxy-runs]]` `[[tg-lora-purpose-and-progressive-freezing]]`

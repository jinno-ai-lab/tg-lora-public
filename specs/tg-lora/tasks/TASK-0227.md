# TASK-0227: PURPOSE.md「次の一手」changelog 成長を CI 強制可能な FREEZE へ — feedback bullet 3「stop adding new terms」の構造的具現化

- **日付**: 2026-07-28
- **コミット**: （本ファイル + `PURPOSE.md` FREEZE block + `tests/test_purpose_next_steps_freeze.py`）
- **分類**: A（コードで解決可能）／type: test + docs（FREEZE 構造不変量の CI pin — prose ではなく machine-enforced）
- **軸**: oper-decision-surface（TASK-0198 / 0217 / 0223 / 0225 / 0226 の運用表面、AI_HUB_MAKE_RUN_FEEDBACK bullet 3「stop adding new terms — the next move should be the operator decision, not term N+1」への**直接**応答）

## 動機（prose freeze が 4 回破られた事実 → 構造不変量への格上げ）

feedback bullet 3 は verbatim:

> "Document the contract-term lineage in PURPOSE.md once more, then freeze:
>  with 6 terms the PURPOSE paragraph is becoming a changelog. ... stop
>  adding new terms — the next move should be the operator decision, not
>  term N+1."

しかし検証の結果、**TASK-0223 が「durable closeout」を宣言したにもかかわらず**、loop はその後 **追記(24) / (25) / (26) / (27) を継続 append** した = **prose としての freeze は 4 回連続で破られた**。この反復失敗モードの根因は明確: 「stop adding terms」が prose にしか存在しないため、各 iteration が新規 追記 を再堆積させることを何も構造的に阻止しない。

`PURPOSE.md` の実態調査:

```bash
$ wc -l PURPOSE.md        # 1308 → FREEZE block 追加で 1311
$ # 「次の一手（next execution）」section = line 153〜EOF・約 1155 行
$ # section 内 追記 block-opener（`> **【…追記`）count = 97
```

= feedback が「6 terms → changelog」と警告した形状が**本 checkout では 97 entries へ倍化**して再現。TASK-0226 は Option A/B/C の freeze 提案を残したが実装せず（Option C 現状維持）= 本 TASK がその判断を **freeze 実行** で閉じる。

→ 本 TASK は bullet 3 の「freeze」を **CI 強制可能な構造不変量**（prose ではない）へ格上げする: 「次の一手」section の 追記 block-opener count を frozen baseline に pin し、term N+1 の silent append/prepend を CI fail-loud で阻止する。

## 変更

### 1. `PURPOSE.md` — 「次の一手」section 先頭に FREEZE block を prepend（newest-at-top 規約）

```
> **【2026-07-28 FREEZE（oper-decision-surface §4）・AI-Hub feedback bullet 3
   「stop adding new terms — the next move should be the operator decision,
   not term N+1」を prose ではなく CI 強制可能な構造不変量へ具現化（TASK-0227）】**
> §4 operator-decision surface の lineage は本 entry で FROZEN。canonical live state =
  (1) 機械可検証 surface（make section4-operator-decision = exit 3 = arc complete but
  awaiting operator call・non-unilateral by REQ-403・recommendation=SHIP・両 leg TIES +
  P1 品質保持 SURPASSES）+ (2) EARS registry specs/oper-decision-surface/（38 REQ 全 🔵）。
  残 open = operator --land〔non-unilateral・live exit 3〕+ private src.data〔DATA/Cat-C〕
  のみ。本「次の一手」section の新規 追記 accretion は tests/test_purpose_next_steps_freeze.py
  で CI 強制阻止（frozen baseline = 97 追記 block-opener）。sentinel:
  OPER_DECISION_SURFACE_FROZEN_2026-07-28_T0227。
```

**設計上の要点**:
- FREEZE block の opener 【…】は **「FREEZE」** を含み **「追記」を含まない** → 追記 block-opener count regex（`> **【[^】]*追記`）に **match しない** = count は 97 のまま不変（下記検証で実証）。
- body 行は `> ` で始まり `> **【` で始まらない → 同様に count に影響しない。
- sentinel `OPER_DECISION_SURFACE_FROZEN_2026-07-28_T0227` は guard test の anchors。

### 2. `tests/test_purpose_next_steps_freeze.py`（新規・5 tests・CI 強制）

| test | invariant | 検出する churn 形状 |
|------|-----------|---------------------|
| `test_freeze_sentinel_present_exactly_once` | sentinel が section にちょうど 1 回 | sentinel 削除/複製 |
| `test_freeze_block_is_topmost_addendum` | 最初の `> **【` block が FREEZE block（newest-at-top） | FREEZE 上方への 追記 prepend |
| `test_addendum_count_is_frozen` | 追記 block-opener count == `FROZEN_NEXT_STEPS_ADDENDUM_COUNT`(97) | **任意の 追記 append/prepend（term N+1 本体）** |
| `test_freeze_documents_canonical_surface_and_registry` | surface 名 + registry 名を明記 | freeze が reader/executor を座礁させる |
| `test_freeze_states_non_unilateral_blocking` | `--land` blocking を明記 | silent completion 偽装 |

**正規の escape valve**: 本当に新しい milestone（§4-decision churn ではない）は `FROZEN_NEXT_STEPS_ADDENDUM_COUNT` を**意図的に bump** して 追記 する。その bump は visible・review 対象の act = silent term-N+1 accretion の対極。

### 3. 検証

```
tests/test_purpose_next_steps_freeze.py::TestPurposeNextStepsFreeze
  test_freeze_sentinel_present_exactly_once        PASSED
  test_freeze_block_is_topmost_addendum            PASSED
  test_addendum_count_is_frozen                    PASSED
  test_freeze_documents_canonical_surface_and_registry PASSED
  test_freeze_states_non_unilateral_blocking       PASSED
5 passed in 3.53s
ruff check tests/test_purpose_next_steps_freeze.py  = All checks passed!
pytest ... -m "not slow"                            = 5 passed（CI filter で実行対象）
```

**mutation-proven ×3**（各 mutate → 対象 test RED → backup から復元 → GREEN 復帰）:

| mutation | target test | observed |
|---|---|---|
| section 末尾に fake `追記(28)` churn block を append | `test_addendum_count_is_frozen` | RED（count 97 → 98） |
| FREEZE block の**上方**に fake 追記 を prepend | `test_freeze_block_is_topmost_addendum` | RED（first opener が 追記 になる） |
| sentinel 文字列を削除 | `test_freeze_sentinel_present_exactly_once` | RED（sentinel count 0） |

復元後 `diff PURPOSE.md /tmp/PURPOSE.bak` = clean（5 GREEN 再確認）。

**FREEZE block 追加による count 不変の実証**: prepend 前後で 追記 block-opener count = **97 → 97**（FREEZE opener は 追記 を含まないため regex 非 match・設計通り）。

## 変更ファイル一覧

| ファイル | 種別 | 説明 |
|---------|------|------|
| `PURPOSE.md` | docs（「次の一手」section・additive） | FREEZE block（opener + body・3 行）を section 先頭に prepend。既存 追記(27) 以下は不変。 |
| `tests/test_purpose_next_steps_freeze.py` | test（新規・本 TASK の主変更） | `TestPurposeNextStepsFreeze` 5 tests。section 動的抽出で行 shift に robust。 |
| `specs/tg-lora/tasks/TASK-0227.md` | task note（tracked, 新規） | 本ファイル |

**書かなかったファイル**:

- `section4_landed_decision.json`（repo root）— TASK-0225/0226 同様 **意図的に absent**。REQ-403 により operator `--land` は unilateral 禁止。本 TASK は freeze であり landing ではない。
- `specs/recursive-funnel-bitnet/contract-terms.md`（feedback bullet 3 の文字通りの registry 推奨）— 同 directory が本 checkout に **存在しない**（phantom・grep 0）= 捏造禁止。**実 analog の registry は既存** `specs/oper-decision-surface/`（38 EARS REQ・全 🔵）であり、FREEZE block は未来の status 更新をそこへ routing するよう明記済。
- 追記(28) 以降の PURPOSE 追記 — **それが本 TASK の禁止対象**。本 TASK 自身も 追記(N) 番号を付与せず FREEZE block として追加（count 不変）。

## bullet 1/2/3/4 compliance / 位置づけ

- **bullet 1**（pin in CI rather than just in pytest discovery）= TASK-0226 で `TestSnapshotFixtureIntegrity`（5 tests）として**本 checkout の実 analog** が充足済。本 TASK は新規に `TestPurposeNextStepsFreeze`（5 tests）を追加しそれも `-m "not slow"` で CI 実行対象（slow 未登録・3.5s）= 同 SPIRIT を freeze 軸にも拡張。
- **bullet 2**（再 audit 禁止・cache hardening は L1 maintenance）= 遵守。cache fingerprint 軸への XXIII 追加なし・`[[silent-success-best-model-min-delta-axis]]` の CLOSED lane 再 hunt なし。本 TASK は cache ではなく **docs/process freeze**。
- **bullet 3**（contract-term lineage 文書化 → freeze）= **本 TASK が主応答**。文字通りの `specs/recursive-funnel-bitnet/contract-terms.md` は phantom だが、SPIRIT（PURPOSE changelog 成長の停止・registry への routing）は (a) 既存 `specs/oper-decision-surface/` registry + (b) CI 強制 FREEZE 不変量 で充足。TASK-0226 が先送りした freeze 判断を実行で閉じた。
- **bullet 4**（operator decision or blocker artifact）= TASK-0225 で対応済（fresh fixture + non-unilateral landing 拒否）。本 TASK はその surface の **prose churn を構造的に停止**するもので、landing ではない（REQ-403 不変）。

## 残 open（不変）

- (a) operator `--land`〔non-unilateral・live exit 3・本 TASK で変化なし〕
- (b) private `src.data` 絶対 loss〔DATA/Cat-C・本 mirror 不可〕

## 注意: pre-existing spine-anchor 失敗（本 TASK 起因ではない）

`scripts/check_spine_anchors.py` は 5 issue で exit 1 するが、全て `specs/freeze-ci-operator-errors/tasks/TASK-0179..0183.md` の `parent_missing`（親 `freeze-ci-operator-errors/architecture.md` 不在）= **既知の pre-existing**（`PURPOSE.md` 編集の有無で同一・`PURPOSE.md` は spine anchor 未保持）。GitHub Actions CI（`.github/workflows/test.yml`）は `check_spine_anchors.py` を実行しないため CI 影響なし。本 TASK の scope 外。

## memory 参照

`[[ai-hub-feedback-infra-vs-this-repo]]` §4 arc COMPLETE・SHIP が唯一の evidence-based RECOMMENDATION・`--land` は operator-gated BY DESIGN。`[[silent-success-best-model-min-delta-axis]]` cache-fingerprint 軸は CLOSED・再 hunt 不要（bullet 2 遵守）。`[[progressive-freeze-level2-trio-orphaned]]` §4 verdict ワーク完了済 — do NOT re-fire/re-harvest。`[[spine-anchor-validator-exists]]` `check_spine_anchors.py` は spine 形状 guard（本 TASK は spine anchor に触れない）。

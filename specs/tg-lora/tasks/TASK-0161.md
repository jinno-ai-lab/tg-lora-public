# TASK-0161: full-budget × heterogeneous §4 leg の launch path 配線 + honest-gate 検証（GPU-free）— feedback phantom lever 検証と open leg の unblock

<!-- spine:anchor:begin -->
> **Spine anchor**: [TG-LoRA アーキテクチャ設計](../architecture.md)
>
> - parent: `tg-lora/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0161
**タスクタイプ**: DIRECT（open §4 leg の launch-path 配線 + GPU-free honest-gate/launch test + mutation 証明 + Makefile committed-path + standing docs 更新）
**推定工数**: 1時間（Makefile 2 target + flag var・`TestFullBudgetHeterogeneousLaunchPath` +4・両方向 mutation 証明・PURPOSE/TASK 記録）
**フェーズ**: Phase 74 - 残る唯一の open §4 研究課題（heterogeneous × generalize の **full-budget** leg）を **launch-ready** に格上げ: harness は組合せを既 support ので GPU-free で配線 + honest gate を pin し、GPU 空き窗口即発火可能に
**信頼性レベル**: 🔵 *本 iter で Makefile dry-run + 4 test 直接実行 + 2 方向 mutation 証明（Makefile composition・evidence-hash architecture key）を直接確認。harness の architecture-agnostic gate・generic bg launcher・threaded architecture は既存実装*

## 関連文書

- **正本**: [docs/GOAL.md](../../../docs/GOAL.md) §4（success criterion — 残る open leg）・§7（honesty contract — verdict が honesty gate で誠実ラベルされること）
- **設計文書**: [📐 architecture.md](../architecture.md)
- **前提成果物**: [TASK-0160](TASK-0160.md)（両 citable verdict の独立再現性完遂）・[TASK-0158](TASK-0158.md)（full-budget homogeneous verdict 着地 `4b88ca8`・TIES）・`scripts/run_freeze_validloss_ci_9b.py`（`_full_section4_verdict_gate`/`_evidence_hash`/architecture threading）・`scripts/launch_freeze_ci_9b_full.py`（generic pass-through bg launcher）

## 背景（feedback の指す lever の実体調査と open leg の特定）

AI-Hub feedback（前 iter VALUABLE 評価）は 4 lever を提示した。本 checkout で**全て grep 再確認**し、誠実に振り分けた:

| Feedback lever | 本 mirror での実体 | 判定 |
|---|---|---|
| **bullet 1**: 9B target-scale run の unblock/launch（最優先） | open leg = heterogeneous × generalize の **full-budget** run。homogeneous full は LANDED(`4b88ca8`・TIES)・heterogeneous は reduced-budget(`db542fe`/`d00a362`・SURPASSES)のみ。**本 iter の GPU は private run が 11.6GB/86% 占有で contended** → 1500-step run 自体は発火不能 | 🟡 **GPU-blocked（run 自体）**・しかし **launch path は配線可能** |
| **bullet 2-3**: `scan_repo` の silent-drop fail-loud 化 + `EXT_LANG` comment bloat 収縮 | `scan_repo`/`EXT_LANG`/`lang is None: continue`/`chunker` は `src`+`scripts` を通じ**皆無**（grep 0）。TASK-0108..0113 は extension-routing ではなく **paper-pipeline gate tasks**（G3/G4/results/smoke/inspect/cost）= topic の誤認 | ⚠️ **PHANTOM**（[[ai-hub-feedback-infra-vs-this-repo]] の既知パターン・TASK-0154 背景表と同形） |
| **bullet 4**: PURPOSE/CONSTITUTION bootstrap OR GPU run | `SYSTEM_CONSTITUTION.md`/`PURPOSE.md` は既存（`b7a33fd` seeded）= bootstrap 完遂。GPU run = bullet 1 と同一 | ✅ **DONE / 同 bullet 1** |

feedback 自身の escape hatch（「GPU blocked なら明示し SINGLE highest-leverage non-edge-case task を取れ」・TASK-0154 と同 chain）を適用。**single task = open leg の launch-path 配線 + honest-gate 検証**（peripheral gate-hardening ではなく、stated #1 priority の unblock 半分・verdict 捏造なし）。

## 実装（Makefile: open leg の launch path 配線）

harness は `--architecture heterogeneous` + 任意 `--total-steps` の組合せを**既 support**: architecture は `arm_valid_loss_9b`/ledger fingerprint(`"architecture"` key・line 952-953「heterogeneous run が homogeneous arm を再利用しない」)を貫通・citation gate(`_full_section4_verdict_gate`)は architecture-agnostic・`base_rank` は `cfg.lora.r` から読出（追加 CLI flag 不要）・bg launcher(`launch_freeze_ci_9b_full`)は `module_argv` pass-through（homogeneous path は docstring 例のみ・hardcode なし）。ゆえに **harness 変更不要**・欠けていたのは **Makefile launch target + distinct deposit/ledger path** のみ。

- **`FREEZE_9B_FULL_HETEROGENEOUS_FLAGS`（NEW）**: landed heterogeneous-generalization target の arm shape（candidate + surrogate + input-side control = direction-isolation A/B・`--n-control 3`）を **96 → 1500 step** に bump。`--total-steps 1500` で `reduced_budget=False`・`--train-examples 600` で ~2.5 epoch = generalization regime（素朴な 1500/48 は ~31 epoch で memorize する honesty 違反・TASK-0157 と同 axis）。distinct ledger `runs/freeze_validloss_ci_9b_full_heterogeneous_ledger.jsonl`。
- **`freeze-validloss-ci-9b-full-heterogeneous`（direct）+ `-bg`（self-retrying bg launcher）**: 両 target とも `--architecture heterogeneous` + distinct deposit `tests/fixtures/freeze_validloss_ci_9b_full_heterogeneous.json`。bg は homogeneous full run と同一 wrapper（`launch_freeze_ci_9b_full`・subprocess で exit code 保持・75/3 retry・0/1/2 stop・`--max-attempts`/`--deadline-seconds` で bound・`--ledger` で arm bank）。`.PHONY` + help-list に両 target 追加。
- **dry-run 検証**: `PYTHON_VENV=/bin/true make -n` で両 target が正しく展開（distinct deposit/ledger・`--architecture heterogeneous`・bg が `launch_freeze_ci_9b_full` 経由）を確認。

## 受け入れ test（`tests/test_run_freeze_validloss_ci_9b.py`・`TestFullBudgetHeterogeneousLaunchPath` +4・**GPU-free**・mutation 証明済）

`TestHeterogeneousTargetScaleDeposit`（reduced-budget heterogeneous）と `TestFullBudgetDepositVerdictPin`（full-budget homogeneous）の**欠けていた交点** = open leg の GPU-free pin。verdict は捏造せず、run が GPU 窗口で着地した瞬間に**誠実ラベル**される前提を pin:

- `test_harness_accepts_full_budget_heterogeneous_composition` — `build_parser` が `--architecture heterogeneous` + `--total-steps 1500` を受理（`HETEROGENEOUS in ARCHITECTURES`）・open leg が homogeneous full と同一 CLI から到達可能。
- `test_full_section4_gate_is_architecture_agnostic` — **honesty load-bearer**: `_full_section4_verdict_gate` は architecture param を取らず、full-budget heterogeneous deposit が homogeneous と**同一の 4 conjunct** で gate を開く。各 conjunct が load-bearing（flip で gate 閉 = silent mis-cite 防止）。
- `test_heterogeneous_full_evidence_hash_is_distinct_from_homogeneous` — `architecture`(と `lora_rank_pattern`) は `EVIDENCE_HASH_KEYS` → 未来の heterogeneous-full deposit が homogeneous full と**distinct な evidence hash**（両 citable full-budget verdict の silent conflate 防止・heterogeneous は自身の凍結 literal で着地）。
- `test_makefile_wires_distinct_full_budget_heterogeneous_paths` — **fail-loud composition guard**: Makefile が `--architecture heterogeneous` + flag var + distinct deposit/ledger で配線・bg が generic `launch_freeze_ci_9b_full` 経由・`--output` token で homogeneous sibling 非混同（substring-safe）。

### mutation 証明（両方向）

- **Makefile guard**: direct target から `--architecture heterogeneous` を削除 ⇒ `test_makefile_wires_distinct_full_budget_heterogeneous_paths` RED（`assert '--architecture heterogeneous' in ...`）。restore で GREEN。
- **hash-distinct guard**: `EVIDENCE_HASH_KEYS` から `"architecture"` を削除 ⇒ `test_heterogeneous_full_evidence_hash_is_distinct_from_homogeneous` RED（両 hash が同一に潰れ = architecture が hash で load-bearing なことを証明）。`git checkout -- scripts/run_freeze_validloss_ci_9b.py`（本 iter 非編集 file・mutation のみ revert）で GREEN。
- gate 4-conjunct の load-bearing は既存 `TestRegimeHonestyGate`/`TestFullRunGateLiveness` で独立証明済・本 test は heterogeneous-full case で再確認。

## 誠実な制限（Constitution Rule #6）

- **1500-step run 自体は未発火**: 本 iter の GPU は private run（`/home/jinno/tg-lora/.venv`・11.6GB/86%）で contended → verdict 捏造せず BLOCKED として記録。deposit `freeze_validloss_ci_9b_full_heterogeneous.json` は**不存在**（未来の run で着地）。**launch は即可能**: `PYTHON_VENV=/torch/venv/bin/python nohup make freeze-validloss-ci-9b-full-heterogeneous-bg >runs/full_heterogeneous_bg.log 2>&1 &`（busy GPU を poll・`--ledger` で arm bank・窗口が開けば run）。
- **worktree fire の dead-CWD trap**（[[dead-cwd-background-run-trap]]）: AI Hub が worktree を削除すると relative path の deposit が永続化不能。robust fire は stable dir `/home/jinno/tg-lora-public-full-run/`（absolute config/ledger/output）から・本 iter は配線のみで auto-fire しない（private run との GPU contention + hard-to-reverse outward action は明示的次歩とする）。

## standing 訂正（open leg の状態格上げ）

従前 standing「heterogeneous × generalize full-budget leg は private `src.data` を必要とし open」を訂正: heterogeneous run は **public Dolly で A/B internally valid**（`run_freeze_validloss_ci_9b` が private `src.data` を bypass・7th-iter `db542fe` と同）→ 本 leg の残る block は **GPU availability のみ**（bg launcher が処理）・private `src.data` は全 deposit 共通の absolute-loss comparability 軸（本 leg 固有でない）。本 iter で leg は「needs private src.data」→「**launch-ready（GPU 窗口待ちのみ）**」に格上げ。

`[[ai-hub-feedback-infra-vs-this-repo]]` `[[gpu-usable-proxy-runs]]` `[[dead-cwd-background-run-trap]]` `[[tg-lora-purpose-and-progressive-freeze]]`

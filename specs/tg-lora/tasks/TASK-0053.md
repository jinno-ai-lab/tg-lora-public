# TASK-0053: 非有限loss_after warning log追加


<!-- spine:anchor:begin -->
> **Spine anchor**: [TG-LoRA アーキテクチャ設計](../architecture.md)
>
> - parent: `tg-lora/architecture.md`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0053
**タスクタイプ**: TDD
**推定工数**: 1時間
**フェーズ**: Phase 23 - テストカバレッジ強化と堅牢性向上
**信頼性レベル**: 🟡 *AI_HUB_MAKE_RUN_FEEDBACK指摘「silently sets loss_after=inf instead of logging」より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-083
- **設計文書**: [📐 architecture.md](../architecture.md)

## タスク概要

`src/training/train_tg_lora.py` の非有限loss_afterガード（`math.isfinite(loss_after)` チェック、351行目）で、`loss_after` を無言で `float("inf")` に設定している箇所に `logger.warning()` を追加し、デバッグ可能性を確保する。

## 依存タスク

- **前提タスク**: なし（Phase 22 e19da0fコミット完了済み）
- **後続タスク**: [TASK-0055: Phase 23 全テストスイート検証とoverview更新](TASK-0055.md)

## 完了条件

- [x] `math.isfinite(loss_after)` ガード内で `logger.warning()` が呼び出される
- [x] warningメッセージにloss_afterの値が含まれる
- [x] テストでwarningログが正しく出力されることを確認
- [x] 全テストがパスする

---

## 主要実装項目

### 1. warning log追加 🔵

**信頼性**: 🔵 *REQ-083・train_tg_lora.py 351行目の実装より*

`train_tg_lora.py` の351-352行目にwarning logを追加:

変更前:
```python
if not math.isfinite(loss_after):
    loss_after = float("inf")
```

変更後:
```python
if not math.isfinite(loss_after):
    logger.warning("Non-finite loss_after=%s detected, treating as rejection", loss_after)
    loss_after = float("inf")
```

**実装ファイル**: `src/training/train_tg_lora.py`

### 2. テスト追加 🔵

**信頼性**: 🔵 *既存test_training_integration.pyパターンより*

`tests/test_training_integration.py` にテストを追加:
- モックでeval_lossがNaN/Infを返すシナリオ
- logger.warningが正しいメッセージで呼び出されることを確認（caplog使用）

**実装ファイル**: `tests/test_training_integration.py`（拡張）

---

## 基本的なテスト要件

### テストケース: 非有限loss_after warning 🔵

**信頼性**: 🔵 *REQ-083・train_tg_lora.py 351-352行の実装より*

- eval_lossがNaNを返す場合、logger.warningが呼び出されることをcaplogで確認
- warningメッセージに"Non-finite loss_after"が含まれることを確認

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-0053` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- 1行追加の最小変更。既存動作への影響なし（ログ追加のみ）
- logger.warningは既存のloggerインスタンス（logging.getLogger("tg-lora")）を使用

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質

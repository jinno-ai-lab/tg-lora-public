# TASK-0044: Trainer間perplexity配管パリティテスト

**作成日**: 2026-05-22
**推定工数**: 1.5h
**手法**: TDD
**信頼性**: 🔵 *AI_HUB_MAKE_RUN_FEEDBACK「verify parity」指摘・両trainer実装より*
**状態**: 完了 ✅

## 目標

train_tg_lora.py と train_baseline_qlora.py のperplexity取り扱い（eval_result.perplexity保存、best_perplexity追跡、write_footerへの引数渡し）がパリティを保っていることをパラメータ化テストで確認する。

## 背景

両trainerともperplexityパイプラインは実装済みだが、パリティ検証テストが存在しない。AI_HUB_MAKE_RUN_FEEDBACKの指摘「verify train_tg_lora.py has parity with train_baseline_qlora.py's perplexity plumbing」に対応する。

## 要件

- REQ-070: Trainer間perplexity配管パリティ

## テストケース

### 正常系

1. **TC-070-P19-01**: 両trainerでperplexity保存・write_footer渡しのパスが同一
   - パラメータ化テスト（mode="baseline"|"tg_lora"）
   - eval_result.perplexityがbest_perplexityに保存される
   - best_perplexityがwrite_footerに渡される
   - MLflowにbest_valid_perplexityがロギングされる

## 実装方針

1. テストファイルにパラメータ化テストを追加（test_perplexity_e2e.py または test_baseline_training.py）
2. 両trainerのperplexityフローを抽象化し、共通テストで検証
3. ソースコード上のperplexityフロー比較（静的検査）+ モックベース動的検証

## 関連要件

- REQ-070 (Trainer間perplexity配管パリティ)

## 完了条件

- [x] パラメータ化テストで両trainerのperplexityパリティが検証される
- [x] GPU不要で実行可能

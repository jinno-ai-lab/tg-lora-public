# TASK-0233: `check_substantive_diff.py` の cross-commit oscillation（正味ゼロ振動）range-mode 検出プロパティを回帰 test で pin — feedback #3 の in-checkout 達成可能形への正規化 + phantom-infra routing

- **日付**: 2026-08-03
- **コミット**: `tests/test_substantive_diff.py`（+ 本ファイル・guard 本体は**無変更**）
- **分類**: A（コードで解決可能）／type: test（既存 guard の名指しされた未テスト分岐を pin・mutation-proven・unique coverage）
- **軸**: commit-hygiene guard の range-mode 振動検出の不変性担保（AI_HUB_MAKE_RUN_FEEDBACK item #3 への直接応答・新規 guard 追加ではない）

## 動機（feedback 4 bullet の正規化）

### feedback item #1/#2 は AI-Hub infra（phantom）— 本 checkout に存在しない

feedback が名指す記号群を実態監査（grep-before-acting・`[[ai-hub-feedback-infra-vs-this-repo]]`）:

| feedback の名指し | 本 checkout での実態 | 根拠 |
|---|---|---|
| commits `f478dead`（docs spine normalize・-403）/ `bdf2d5a`（make-run・+403）| **不在** | `git cat-file -t f478dead` / `bdf2d5a` = `fatal: Not a valid object name`（両方）|
| `specs/_doc_spine.yml`（振動対象ファイル）| **不在** | `ls specs/_doc_spine.yml` = No such file・`find . -name '_doc_spine*'` = 0 hit |
| `check_spine_manifest.py`（spine-audit guard）| **不在** | `[[ai-hub-feedback-infra-vs-this-repo]]` の既知 phantom（PURPOSE.md L1110 も同一判定）|
| `spine_manifest.py`（**generator root cause**・feedback が「次の lever」と命名）| **不在** | `grep -rn spine_manifest` の hit は PURPOSE.md のみ（同 infra を absent と記録）|
| 「HEAD==base 短絡回路」| **不在** | `grep -rn "diff --quiet\|short.circuit\|base.*HEAD"` in scripts/+tests/ = 該当 code 0 件。本 checkout の commit-hygiene guard（`check_substantive_diff.py`）は `git diff <range>` の verdict のみで tree 比較の short-circuit は持たない|

→ item #1（9B target-scale run）は Cat-C（private `src.data` + >12GB GPU・本 checkout から不可・run は既発火/harvest 済みで再実行は TIES 再現のみ = 新情報なし）・item #2（generator root cause 修正）は AI-Hub 自身の infra。いずれも本 checkout から自動実行不可。`[[gpu-usable-proxy-runs]]` `[[ai-hub-feedback-infra-vs-this-repo]]`。

### item #3 は in-checkout 達成可能形へ正規化できる（唯一の実行可能 lever）

feedback item #3「cross-commit oscillation の回帰 test 追加 — さもなくば guard が実 churn signal を黙って pass するよう silent regress する」は、phantom ではなく**実在する未テスト分岐**として正規化できる:

- `check_substantive_diff.py` の `--range A..B` mode は、range に跨る正味ゼロ振動（START tree == END tree）を `git diff A..B` が空 → verdict `empty`（exit 3）として**検出する**（実証済み・下記）。
- しかし**このプロパティは未テスト**だった。既存 `tests/test_substantive_diff.py` は `--staged`（pre-commit）・`HEAD^..HEAD`（単一 commit）・`test_main_returns_substantive_for_this_repos_last_commit`（same-commit case）のみを網羅し、multi-commit range + 正味ゼロ振動の組合せを1件も cover していなかった。
- feedback が指摘した「cross-commit で振動が見逃される」現象の根因は (a) **未テスト**（本 iter が閉包）と (b) make-run loop が per-commit（`HEAD^..HEAD`）で起動し per-range で起動しないこと（AI-Hub infra・本 checkout 外）。本 iter は (a) のみ閉包し、(b) は routing する。

## 実証（throwaway repo・本修正前に検証）

| 確認 | 結果 | 意味 |
|---|---|---|
| `git diff --quiet START B`（A collapse → B re-expand to identical tree）| exit **0**（tree byte-identical）| 正味ゼロ振動を再現（feedback の `git diff --quiet` net-zero 主張と一致）|
| guard `--range HEAD^..HEAD`（commit B・per-commit view）| exit **0**（substantive）| B の再展開は実在する追加 → per-commit では**振動を検出しない**（feedback の「neither is blocked」と一致）|
| guard `--range START..B`（振動に跨る range view）| exit **3**（empty）| range-mode は**振動を検出する** = make-run loop が per-range 起動すべき recipe |
| guard `--range A..B`（中間・再展開単体）| exit **0**（substantive）| 再展開自体は substantive（中間 range では空でない）|

## 変更（test 1 file のみ・guard 本体無変更）

### `tests/test_substantive_diff.py::test_guard_range_flags_cross_commit_oscillation`

baseline → commit A（collapse・実行削除）→ commit B（baseline と byte-identical に再展開・実行追加）の repo を構築し:

1. **per-commit view**（`--range HEAD^..HEAD` on B）→ `EXIT_SUBSTANTIVE` を主張（振動は per-commit では見逃される = feedback の gap 再現）
2. **range view**（`--range START..B`）→ `EXIT_EMPTY` を主張（range-mode は正味ゼロを検出 = pin 対象プロパティ）

新規 guard ではなく**既存の `--range` 分岐の振る舞いを pin** する。guard 本体（`scripts/check_substantive_diff.py`）は byte-identical（`git diff --quiet` = CLEAN）。

## 検証（mutation-proven・unique coverage）

| 状態 | 結果 |
|----------|------|
| 本修正（guard あり・正しい range-mode）| `test_substantive_diff.py` **15 passed**（既存 14 + 新 1）・`ruff check` **All checks passed!** |
| **mutation**: `main()` の `revspec = ... else args.range` を `else "HEAD^..HEAD"` へ改変（custom `--range` を無視）| 新 test **RED**（`assert 0 == 3`・range が無視され B を substantive 判定）・**残り 14 件は GREEN のまま** = 新 test は**固有 coverage**（既存 test 群と重複しない）|
| 復元後（`git checkout -- scripts/check_substantive_diff.py`）| **15 passed**・guard file CLEAN vs HEAD |

mutation が既存 14 件を RED にしなかったことは、新 test が range-mode の multi-commit 正味ゼロ検出という**他の test が cover しない経路**を单独担保していることの証明。

## bullet 別クローズ状況

| feedback bullet | 状態 | 根拠 |
|---|---|---|
| 1. 「peripheral tooling ではなく 9B target-scale run へ（proxy-TIES は null=0.000 で測定飽和）」| **Cat-C・本 checkout 不可（routing）**| 9B run は private `src.data` + >12GB GPU（`[[gpu-usable-proxy-runs]]`）。run は既発火/harvest 済み（homog `4b88ca8` / heterog `1224057`・両 leg TIES + P1 品質保持 SURPASSES・`recommendation=SHIP` landed）。再 run は TIES 再現のみ = 新情報なし。誠実な応答 = 明示的な状態記述（feedback 自身が「GPU blocked なら polish で埋めず明示せよ」と指示）|
| 2. 「guard refinement でなく GENERATOR root cause（`spine_manifest.py` expand/normalize determinism）」| **phantom（AI-Hub infra）**| `spine_manifest.py` / `_doc_spine.yml` / `check_spine_manifest.py` / commits `f478dead`・`bdf2d5a` は本 checkout に皆無（上表）= AI-Hub make-run 側 infra。phantom コードを「修正」すれば捏造|
| 3. 「cross-commit oscillation の回帰 test 追加（silent regress 防止）」| **DONE**| `test_guard_range_flags_cross_commit_oscillation` が range-mode の正味ゼロ振動検出を pin（mutation-proven・unique coverage）。guard 本体無変更 = 新規 guard 追加（= refinement）ではなく既存未テスト分岐の pin|
| 4. 「another guard/test hardening の前に operator に churn-guard investment の in-scope 確認せよ」| **routing（operator 決定事項として明示）**| 本 iter は lane を黙って拡張せず、最小の pin 1 件に留めた。**operator への問い**: (a) 本 guard lane（substantive-diff + oscillation 検出）を更に拡張するか、(b) item #3 の test で閉包し lane を閉じるか、(c) 9B run への旋回（Cat-C・外部依存）を待つか。repo arc（`[[purpose-next-steps-freeze-guard]]`・§4 arc DONE・refinement 純負の歴史）は (b) を強く示唆|

## 残 open（不変・本 iter では触れず）

- **private `src.data`**〔DATA/Cat-C・deliberately stripped〕: 絶対 loss 比較の production baseline は本 checkout 不可（唯一の真の残 open・`[[ai-hub-feedback-infra-vs-this-repo]]`）。
- **make-run loop の per-range 起動**〔AI-Hub infra〕: 本 checkout の guard は range-mode で振動を検出できるが、make-run loop が per-commit で起動する限り見逃される（item #3 の test はこの recipe を pin するが、loop 側の採用は AI-Hub 側決定）。
- **TASK-0232 文書化 open（同 defect class sibling reader）**: `scripts/summarize_cosine_n_ablation.py:25` / `scripts/form_freeze_validloss_deposit.py:117`（§4 verdict-path reader）。本 iter は cascade 再膨張を避け 1-focused-commit に徹したため未処理（operator が lane 継続を confirm すれば後続 iter で個別集中 commit 可能）。

いずれも本 checkout から自動実行不可、または本 iter の focused scope の外。§4 arc 完結状態は不変（SHIP landed・両 leg TIES + P1 品質保持 SURPASSES）。本 iter は PURPOSE.md「次の一手」section には触れない（frozen・本作業は真の新規 milestone ではなく test pin + routing = bump 不要）。

## 関連文書

- **直接前提**: TASK-0192（substantive-diff guard 実装・item #2 直接実装）・`241b3d4`（binary-diff marker を substantive 計上・同 guard の false-negative 修正）・`5704265`（`make install` → `install-hooks` 依存で hook 配備）
- **正本**: [docs/GOAL.md](../../../docs/GOAL.md) §7（科学誠実性＝「表面化する、黙殺しない」= silent regress を test で防ぐ根拠）
- **実装**: `tests/test_substantive_diff.py::test_guard_range_flags_cross_commit_oscillation`（guard 本体 `scripts/check_substantive_diff.py` は無変更）
- **memory**: `[[ai-hub-feedback-infra-vs-this-repo]]`（phantom 記号の拒絶・`spine_manifest.py`/`_doc_spine.yml`/`check_spine_manifest.py` は既知 absent）・`[[gpu-usable-proxy-runs]]`（9B run Cat-C・run 既発火/harvest 済み）・`[[purpose-next-steps-freeze-guard]]`（「次の一手」frozen・refinement 純負）・`[[ai-hub-exec-env-pythonpath-shadows-scripts]]`（`PYTHONPATH=/home/jinno/ai-hub` が repo `scripts/` を shadow・CI は clean env で green）

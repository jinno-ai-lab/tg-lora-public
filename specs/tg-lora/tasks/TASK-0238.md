# TASK-0238: feedback 4-bullet の loop-closed を独立再測定で追証 + GPU-idle 現状態で「9B blocker が構造的（holder contention でない）」を実証 → no-op-with-blocker（本 feedback thread の closed 宣言）

- **日付**: 2026-08-06
- **分類**: C（外部依存・資源 block）／type: docs（検証 + 状態実証のみ・**src/ scripts/ tests/ は 1 byte も変更しない**）
- **軸**: TASK-0236（論理+測定検証・no-op）・TASK-0237（iid-null 測定 + rank1_z lock・test-only）が「feedback 4-bullet 全対応・9B blocked」と結論した点を、**独立再測定で追証**し、かつ **GPU が idle になった現状態**で「9B blocker が構造的（DATA 不在 + 12GB 物理量）であり holder 解放で動くものではない」ことを実証する

## 動機（なぜ検証-only か・3 iter 連続の正当性）

0236/0237 で feedback（前 iter=`aa27111`+`f62c359` を VALUABLE 評価）の 4-bullet は全対応済み。しかし (a) 両 TASK の核心クレーム（cited null=0.000・iid null 0% false positive・9B blocked）を独立に再測定して追証しておらず、(b) GPU 状態が 0236/0237 時（recursive_funnel_bitnet の llama-server がホールド）から**現在 idle に変化**していた。よって再測定で loop-closed を追証し、GPU-idle 状態でも 9B が block されることを実証して、blocker が holder contention（＝今 cycle で actionable）でないことを確定する。これは feedback bullet 4「blocked なら polish で埋めず blocker を明示せよ」への、**測定に基づく**最終応答である。

## 独立検証（fresh measurements・0236/0237 クレームの追証）

| 検証 | 手段 | 結果 | 対応 bullet |
|---|---|---|---|
| M9 振る舞い lock | `test_extrapolator.py` 含む 4 file 同時実行 | **97 passed** | bullet 1（`f62c359` 追証）|
| cited null | `replay_freeze_order_sensitivity.py` 再実行 | ratio=**0.000000**・NOT_RESOLVABLE・faithful（recording と一致） | bullet 3（0236 追証）|
| iid null false-positive | `analyze_tensor_deltas` z-score path・6 regimes × 80 trials = **480** | **0/480 = 0.0%**・mean_z < 0.25（全 regime） | bullet 3（0237 追証・feedback 因果クレーム「MP inflates z → false positive」不成立を再確認）|

→ 0236/0237 の結論は独立再測定で追証された。feedback bullet 1（M9 test=`f62c359` DONE）・bullet 2（proxy null 精進禁止=受諾）・bullet 3（CLOSE THE LOOP=0.000 null + 0% FP で closed）は成立。

## GPU-idle 実証（NEW fact → 9B blocker が構造的）

**nvidia-smi 現状**: RTX 3060・12288 MiB total・**28 MiB used / 11883 MiB free・compute apps なし = idle**（0236/0237 時の recursive_funnel_bitnet holder は不在）。

**status checker 実行**（`report_gpu_availability()`・現状態）:

```
[!] 9B lever is DATA-blocked, not GPU-blocked.
    ... freeing the GPU will NOT make the 9B run actionable here ...
[+] No compute apps on the GPU — it appears FREE.
    -> But the lever stays DATA-blocked here — not actionable from this mirror.
```

**この実証の意義**: GPU が idle になっても 9B は block されたまま → blocker は holder contention でなく**構造的**。「holder を止めれば 9B が動く」という想定は、現状態で経験的に否定された。`a7d2a7e` の status checker はこの分岐（free GPU + DATA 不在）を正しく処理し、`test_agent_check_status_gpu.py`（**10 passed**）が全 4 分岐（mirror-held / private-held / mirror-free / private-free）+ nvidia-smi 不在を pin 済み。memory [[gpu-blocked-state-surfaced-in-make-status]] の `a7d2a7e` update も既に「freeing the GPU will NOT move the lever」framing を保持（stale でないことを確認）。

## 9B target-scale run の blocker（明示・feedback bullet 4）

| blocker 種別 | 実態 | 根拠 |
|---|---|---|
| **DATA (Cat-C)** | private `src.data` pipeline が本 mirror に不在（`git ls-files src/data` = 空・deliberate strip・非 ignored）。9B run の data build に必要・外部依存。 | [[public-mirror-preexisting-test-failures]]・[[ai-hub-feedback-infra-vs-this-repo]] |
| **GPU (物理量)** | 12GB RTX 3060 < 9B QLoRA seq2048 要件。**idle でも解消せず**（上記実証）。target-scale run には >12GB GPU が必要。 | [[gpu-usable-proxy-runs]]（9B QLoRA seq≥512 で OOM・本 config は seq2048）|
| **相対比較は既 DONE** | homogeneous（`4b88ca8`・TIES）+ heterogeneous（`1224057`・TIES）とも full-budget landing・harvest 済・`recommendation=SHIP` は operator lane 済（`6c75fda`）。 | [[ai-hub-feedback-infra-vs-this-repo]]・[[progressive-freeze-level2-trio-orphaned]] |

残る唯一の open は private `src.data` による**絶対 loss vs production baseline 比較（DATA/Cat-C）のみ**で、本 checkout からは不可（外部依存）。9B の再 run は TIES の再現のみ = 新情報なし。

## なぜ no-op（code 変更ゼロ）が誠実か — 本 feedback thread の closed 宣言

feedback は bullet 2「proxy null の diagnostic-math 精進禁止」+ bullet 4「blocked なら polish/tool-hardening で埋めず blocker 明示」で、明示的に「これ以上の significance-rig 工作/polish は不要」を指示。0236/0237 が全 bullet に対応し、本 TASK がそれを独立追証した。候補作業は全て churn または block:

| 候補作業 | 採否 | 理由 |
|---|---|---|
| cited null / iid null の再 lock test | **churn（却下）** | 既 airtight（97 passed・replay faithful）・derived-float 系列 EXHAUSTED（[[gpu-usable-proxy-runs]]）|
| MP 公式の修正（squaring）/ 値 lock | **禁止（却下）** | bullet 2 + PURPOSE 追記(9) STOP + 実測 well-calibrated（0% FP）の三重禁止 |
| `test_report_gpu_availability_free_gpu` の host-independence 微修正 | **却下** | CI(mirror)で振る舞い不変（ambient False == injected False）・"tool hardening" で lever 動かず＝feedback bullet 4 が禁止する代替作業 |
| 9B run の発火 | **block（不可）** | DATA + 12GB 物理量の二重 block（上記実証）|

**本 feedback thread（M9 refactor の 4-bullet steering）は closed。** 誠実な応答 = blocker の明示（feedback 自身が指示）。次の実質的進展は (a) private checkout での src.data 絶対 loss 比較、(b) >12GB GPU での 9B seq2048 run、のいずれか**外部要因のみ**。本 checkout で取りうる非-churn 工作は存在せず、これ以上の iter も同構造の no-op になる（= churn）。loop は閉じた。

## 無視しない前提

- **src/・scripts/・tests/ は 1 byte も変更しない**（no-op-with-blocker）。実測は全て CPU・既存 fixture・working venv（`/home/jinno/tg-lora/.venv/bin/python`・py3.13・torch 2.12+peft 0.19）で完結。
- PURPOSE.md「次の一手」section は **CI-frozen at 98 追記**（[[purpose-next-steps-freeze-guard]]）。本 TASK は PURPOSE を触らず、状態記述は本ファイルに留める。Hitting 追記99 = churn。
- feedback bullet 1（M9 test=`f62c359` DONE）・bullet 2（proxy null 精進禁止=受諾）・bullet 3（CLOSE THE LOOP=独立追証で closed）・bullet 4（9B blocker 明示）の全 4 bullet に、測定に基づき対応した。

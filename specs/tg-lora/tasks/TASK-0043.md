# TASK-0043: Perplexity E2Eパイプライン統合テスト


<!-- spine:anchor:begin -->
> **Spine anchor**: [TG-LoRA アーキテクチャ設計](../architecture.md)
>
> - parent: `tg-lora/architecture.md`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**作成日**: 2026-05-22
**推定工数**: 2h
**手法**: TDD
**信頼性**: 🔵 *AI_HUB_MAKE_RUN_FEEDBACK「E2E integration test」指摘・run_metrics.py _sanitize_perplexity より*
**状態**: 完了 ✅

## 目標

モック学習ループ（baseline/tg_lora両モード）を実行し、RunMetrics.write_footerの出力に有限best_perplexityが含まれることを検証するE2E統合テストを追加する。

## 背景

コミット(11e21f9)でperplexityがwrite_footerに伝播されるようになったが、モック学習ループ→RunMetrics→footer出力のE2Eパスが統合テストで検証されていない。AI_HUB_MAKE_RUN_FEEDBACKの指摘「Add an end-to-end integration test that runs a short mock training loop and asserts the RunMetrics footer contains a finite best_perplexity value」に対応する。

## 要件

- REQ-069: Perplexity E2Eパイプライン検証
- EDGE-135: RunMetrics footer有限perplexity保証

## テストケース

### 正常系

1. **TC-069-P19-01**: モックbaseline学習でwrite_footerに有限perplexityが含まれる
   - eval_loss_detailedがperplexity=42.5を返すモック → footer JSONLのperplexity == 42.5

2. **TC-069-P19-02**: モックtg_lora学習でwrite_footerに有限perplexityが含まれる
   - eval_loss_detailedがperplexity=38.2を返すモック → footer JSONLのperplexity == 38.2

3. **TC-069-P19-03**: eval未実行時はwrite_footerのperplexityがNone
   - eval_loss_detailedが呼ばれない → footer JSONLのperplexity == null

### 境界値

4. **TC-069-P19-B01**: NaN perplexityは_sanitizeでNoneに変換される
5. **TC-069-P19-B02**: Inf perplexityは_sanitizeでNoneに変換される

## 実装方針

1. 新規テストファイル `tests/test_perplexity_e2e.py` を作成
2. モック化した学習ループ（MockModel, MockDataLoader, MockEvalLossDetailed）を使用
3. tmp_path にRunMetricsを出力し、JSONLをparseしてfooter レコードのperplexityを検証
4. baseline/tg_lora両モードをパラメータ化テストで検証

## 関連要件

- REQ-069 (Perplexity E2Eパイプライン検証)
- REQ-070 (Trainer間perplexity配管パリティ)
- EDGE-135 (RunMetrics footer有限perplexity保証)

## 完了条件

- [x] 全5テストケースがパスする
- [x] テストがモックのみでGPU不要で実行可能

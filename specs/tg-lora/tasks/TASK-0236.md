# TASK-0236: feedback 4-bullet 検証 — bullet 1 (M9 test) は前 commit で既 DONE・bullet 3 proxy-null は現コードで再実測 0.000 (faithful・CI airtight)・MP/median「修正」は本 checkout に未適用かつ cited null と構造的に無関係・9B は DATA+GPU block (verdicts 既 landing) = no-op-with-blocker

- **日付**: 2026-08-06
- **分類**: C（外部依存・資源 block）／type: docs（検証 + 状態記述のみ・**src/ scripts/ tests/ は 1 byte も変更しない**）
- **軸**: AI_HUB_MAKE_RUN_FEEDBACK（前 iter=`aa27111` M9 refactor を VALUABLE 評価）の 4-bullet steering を、grep-before-acting + 実測で検証し、各 bullet の成立/不成立を誤実装なく確定

## 動機（なぜ検証-only か）

feedback は前 iter を VALUABLE と評し、4 bullet を提示した。しかし feedback は **1 イテレーション前の時点**で書かれており、primary な bullet 1 は直前の commit で**既に満たされている**可能性があった。また bullet 2/3 は「significance rig の数学修正が proven-null verdict を無効化した」という前提に立つが、この前提が本 checkout で事実成立するかは未検証だった（`[[ai-hub-feedback-infra-vs-this-repo]]` の反復教訓: feedback は本 checkout に存在しない記号を名指すことがある → grep-before-acting）。よって各 bullet を実測で検証し、誤った前提の上に churn を積まないことを最優先とした。

## 検証結果（4 bullet × 実測）

### bullet 1 — 「`subspace_m9_fit_step` に回帰 test 追加（NaN-free + 閉形式 + stats (1,0,0)）」→ **DONE（前 commit で既に満た済）**

feedback は「reliability claim を ship したが test がない」とするが、HEAD `f62c359` が**まさにその test** を追加済み:

> `test(extrapolator): lock pure-v0 M9 extrapolation — pin (1,0,0) + closed form + finiteness under degenerate history`

`tests/test_extrapolator.py` に、degenerate history（rank-deficient / single-element / zero-norm mean / zero-norm velocity_direction）について (a) NaN/Inf free、(b) 閉形式 `selected_N*w_traj*v0` と一致、(c) stats が固定 (1,0,0) を報告、を mutation-proven で pin。**実測**: `/home/jinno/tg-lora/.venv/bin/python -m pytest tests/test_extropolator.py -q` → **44 passed**。feedback は 1 iter 前の時点で書かれたため bullet 1 を「未実施」と扱ったが、現 HEAD では成立。追加作業なし。

### bullet 2 — 「proxy null の diagnostic-math 精進はこれ以上しない（ACCEPTED）」→ **受諾（指導）**

実測のうえ受諾。本 TASK は数学修正を加えない（後述 bullet 3 で理由付け）。

### bullet 3 — 「CLOSE THE LOOP: 修正後コードで proxy-scale null/rank-1 baseline を再実行し、null が保つか反転するか LOG せよ」→ **実測で『cited null は厳密保持・MP/median 修正は不成立』を確定**

feedback の前提は「(i) MP/median の数学修正が適用済みで、(ii) それが 'proven null (TIES ratio=0.000)' verdict を無効化した」である。実測の結果、**両前提とも本 checkout で成立しない**:

**(I) cited null は別 rig（exact/deterministic）。**「proven null (ratio=0.000)」は `make freeze-order-sensitivity`（`scripts/run_freeze_order_sensitivity.py`）による `Var(order)/Var(seed)` 測定で、`evaluate_schedule`（model-free CPU・完全 deterministic）経由。MP/z-score/median 数学は含まない。記録は `tests/fixtures/freeze_order_sensitivity_proxy.json`（`var_order=0.0`・`var_seed=0.02015958`・`ratio=0.0`・`resolvable=false`）。

**現コードでの再実測**（`PYTHONPATH=. /home/jinno/tg-lora/.venv/bin/python scripts/replay_freeze_order_sensitivity.py tests/fixtures/freeze_order_sensitivity_proxy.json --expected not_resolvable`）:

```
Var(order) = 0.000000000000   (spread across 12 orders at a fixed seed)
Var(seed)  = 0.020159584542
ratio      = 0.000000000000   = Var(order)/Var(seed)
verdict: order NOT_RESOLVABLE (ratio < threshold 0.10)
faithfulness: replayed ratio MATCHES recording (0.000000000000)
faithfulness: replayed outcome MATCHES recording (not_resolvable)
```

→ **null は厳密保持（diff = 0.000 → 0.000 = ゼロ）**。しかもこの null は order-variance で MP/median とは構造的に無関係なので、仮に那些 fixes があっても verdict は物理的に変わらない。

**(II) CI で既に airtight。** `tests/test_replay_freeze_order_sensitivity.py` L171-173 が `var_order==0.0` / `ratio==0.0` / `resolvable is False` を厳密 pin、L214 `test_replays_recorded_ratio_and_outcome` が ratio+outcome の双方を expected-output assertion で固定。**実測**: 同 test → **21 passed**。feedback が懸念した「verdict の stale silent flip」は既存 test が塞いでおり、追加すべき lock はない（= 追加は `[[gpu-usable-proxy-runs]]` の derived-float 系列 EXHAUSTED 宣言および PURPOSE 追記(9) STOP 条項に抵触する churn）。

**(III) MP/median「修正」は本 checkout に未適用。** bullet 3 が名指す rig は `src/tg_lora/layer_delta_analysis.py`（`marchenko_pastur_expected_rank1` L62-92 + `rank1_z`）と `src/tg_lora/dynamic_freeze.py`（`median_A` early-cycle mask）。

- `layer_delta_analysis.py` の MP 公式は **comment（`(1+sqrt(n/p))^2/p`）と実装（`return 1.0/n_rows + math.sqrt(ratio)/n_rows`）が不一致**のまま。`git log -- src/tg_lora/layer_delta_analysis.py` は `0c9e7ba`（ruff-clean）+ `bc97c83`（restructure）のみで**数学修正 commit は存在しない**。すなわち「un-squared MP edge」は未修正。
- この rank-1/MP rig は offline 診断（`scripts/analyze_trajectory_deltas.py` が baseline ablation の `step_N.pt` を食う GOAL §4 step-2 解析）であり、**cited order-sensitivity null (ratio=0.000) を出した rig ではない**。
- `dynamic_freeze.py` の `median_A` も freeze 順序 null とは無関係（per-cycle masking threshold）。

> **注意（real だが out-of-scope の latent issue）**: MP 公式の comment-vs-実装不一致は **real な latent bug**（phantom ではない）。かつ `TestMarchenkoPastur`（`tests/test_layer_delta_analysis.py` L130-142）は sign/monotonicity のみで**公式の値を pin していない**ため、`f62c359` が M9 で塞いだ "alpha の値が unpinned" gap と同型の穴が残っている。しかし (a) 値を lock するには正しい値を決定する必要があり、現 bug 値の固定化も修正値の固定化もそれぞれ不正、(b) 修正自体が bullet 2「proxy null の diagnostic-math 精進禁止」+ PURPOSE 追記(9) STOP + derived-float 系列 EXHAUSTED の三重禁止に抵触する、(c) いかなる記録済み verdict にも影響しない（per-tensor 診断で一度も "proven null" として引用されていない）— ので本 TASK では触れない。触れる場合は「cited verdict に影響する」ことを先に実証することが bar。

### bullet 4 — 「9B-run blocker を明示せよ（polish で埋めず）」→ **下記に明示**

## 9B target-scale run の blocker（明示）

feedback が「9B run が genuinely unblocked なら significance-rig 工作の代わりに run せよ」と指示した点について、**現状は block しており、かつ相対比較については既に landing 済み**:

| blocker 種別 | 実態 | 根拠 |
|---|---|---|
| **DATA (Cat-C)** | private `src.data` pipeline が本 mirror に不在。`git ls-files src/data` = 空・`git check-ignore src/data` = NOT ignored（tracked でも ignored でもない untracked local state）。9B run の data build に必要。 | `[[public-mirror-preexisting-test-failures]]`・`[[ai-hub-feedback-infra-vs-this-repo]]`（src.data は deliberate strip・half-port しない） |
| **GPU (物理)** | 12GB RTX 3060 は 9B QLoRA seq≥512 で OOM。本 config は seq2048。target-scale run には >12GB GPU が必要。 | `[[gpu-usable-proxy-runs]]`（proxy scale は GPU 利用可能・target scale は別） |
| **相対比較は既 DONE** | homogeneous（`4b88ca8`・cand 1.6947 vs surr 1.6960・TIES）+ heterogeneous（`1224057`・cand 1.7180 vs surr 1.7190・TIES）とも full-budget で landing・harvest 済み。`recommendation=SHIP` は operator lane 済み（`6c75fda`）。 | `[[ai-hub-feedback-infra-vs-this-repo]]`・`[[progressive-freeze-level2-trio-orphaned]]` |

よって「9B run を走らせろ」に対する誠実な応答は: **順序感度の相対比較（§4 主 verdict）については既に実施済み・TIES**。残る唯一の open は private `src.data` による**絶対 loss vs production baseline 比較（DATA/Cat-C）のみ**で、これは本 checkout では不可能（外部依存）。再 run は TIES の再現のみ = 新情報なし。誠実な応答 = 本 blocker の明示（feedback 自身が「GPU blocked なら polish で埋めず明示せよ」と指示した通り）。

## なぜ no-op（code 変更ゼロ）が誠実な結論か

| 候補作業 | 採否 | 理由 |
|---|---|---|
| bullet 1 の test 追加 | **不要（既 DONE）** | `f62c359` が追加済み・44/44 green |
| cited null の再 lock test | **churn（却下）** | `test_replay_freeze_order_sensitivity.py` が ratio==0.0 を airtight に pin 済み（21 passed）。追加 = derived-float 系列（EXHAUSTED）への延長 |
| MP 公式の修正（squaring） | **禁止（却下）** | bullet 2「proxy null の diagnostic-math 精進禁止」+ PURPOSE 追記(9) STOP。また `analyze_trajectory_deltas.py` は baseline run の `step_N.pt` 必要で GPU+private src.data 依存 = 実行不能 |
| MP 公式値の lock test | **禁止（却下）** | 正しい値を決めずに lock できない（bug 固定化 or 数学精進の二択）。f62c359-analogous だが out-of-scope |
| 9B run の発火 | **block（不可）** | DATA + GPU 二重 block・相対比較は既 landing |

feedback の核心メッセージ（bullet 2+3）は「proxy-null の無限精進を止め、verdict が stale でないか再確認せよ」だった。実測の結果、**cited verdict は現コードで厳密保持（0.000）かつ CI airtight** で、stale ではない。よって loop は既に閉じており、追加 code 変更は全て churn になる。本 TASK は検証 + blocker 明示のみ（src/scripts/tests 不変）。

## 無視しない前提

- 本 TASK は **src/・scripts/・tests/ を 1 byte も変更しない**（no-op-with-blocker）。実測は全て CPU・既存 fixture・working venv で完結。
- test 実行時の `ModuleNotFoundError: No module named 'scripts.X'` は path bootstrap artifact（`/home/jinno/tg-lora/.venv` が `/home/jinno/ai-hub` を path に持ち、AI-Hub 自身の `scripts/` が本 worktree の `scripts/` PEP420 namespace を隠蔽）。`PYTHONPATH=.` で worktree を前にすれば解消し 21 passed。code 欠陥でも regression でもない（`[[pytest-cov-torch-clash]]` の既知の defect class）。
- PURPOSE.md「次の一手」section は **CI-frozen at 98 追記**（`[[purpose-next-steps-freeze-guard]]`）。本 TASK は PURPOSE を触らず、状態記述は本ファイルに留める。Hitting 追記99 = churn。
- 次の iter が本 checkout で取りうる real な非-churn task は、proxy null / derived-float / §4 verdict arc（全て DONE or STOP）ではなく、(a) private `src.data` 絶対 loss 比較の routing/明示、(b) MP 公式 latent issue に「cited verdict への影響」を実証した上での修正、のいずれかのみ。

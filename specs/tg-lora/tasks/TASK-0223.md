# TASK-0223: feedback 4-bullet サイクルの永続 closeout 宣言 + operator-facing decision の新規 surface 実行ログ（bullet 1 遵守・bullet 4 応答）

<!-- spine:anchor:begin -->
> **Spine anchor**: [TG-LoRA アルゴテクチャ設計](../architecture.md)
>
> - parent: `tg-lora/architecture.md`
> - role: `detailed`
> - status: `canonical_child`
<!-- spine:anchor:end -->

**タスクID**: TASK-0223
**タスクタイプ**: docs + verify（恒久コード変更ゼロ。新規テスト・src 変更は意図的に無し = 追記25 の「新規 gap 発掘なき限りコード面 no-op が誠実」を履行）
**推定工数**: 0.5時間
**フェーズ**: AI Hub 反復。feedback bullet 1「TASK-0217 の closeout を durable と扱い、同一 4 bullets への compute 再支出を止める」の**履行**と、bullet 4「9B run 再開 or operator-facing decision の surface」の後者への応答。
**信頼性レベル**: 🔵 *live checkout で実行検証: `scripts/section4_operator_decision.py` 新規実行（RECOMMENDATION: SHIP・exit 3 = operator `--land` 待ち）+ そのテストスイート 51 passed（210s）*

## 関連文書

- **正本**: [docs/GOAL.md](../../../docs/GOAL.md) §4（verdict arc）・§7（silent corruption を green で通さない）
- **直接前提**: [TASK-0217](TASK-0217.md)（4-bullet phantom-vs-implemented closeout の原本）・[TASK-0220](TASK-0220.md)（TC-132-09 実執行）・[TASK-0221](TASK-0221.md)（ledger 指紋 cross-section ガード）・[TASK-0222](TASK-0222.md)（cross-producer 指紋同一性・bullet 2 の producer 横断実証）
- **PURPOSE.md**: 追記25（本 closeout の方針表明・追記26 は**増やさない** = 本 TASK が最終 closeout 記録）
- **memory**: [[ai-hub-feedback-infra-vs-this-repo]]（phantom 記号目録・operator `--land` は BY DESIGN non-unilateral）

## 背景

同一の 4-bullet フィードバックが反復ごとに再巡回し、毎 iter 同じ audit に compute が支出されていた。前 2 反復（TASK-0221/0222、`ce48610`+`addba74`）が bullet 2/3 へ**実在記号でのテストスイート実証**で回答済みであり、今回の feedback bullet 1 は明示的に「closeout を durable と扱え」と要求している。本 TASK はその要求の履行そのもの: 4 bullet すべてを恒久成果物へマッピングして閉じ、以降の反復が再 audit なく参照できる単一の closeout エントリを置く。

## 4 bullet → 恒久成果物マッピング（durable closeout）

### bullet 1: 4-bullet audit パターンの停止 → **本 TASK で永続化**

- closeout の正本は TASK-0217（phantom vs implemented の原判定）+ 本 TASK（最終マッピング）。PURPOSE.md への追記は**追記25 を最後**とし、追記26 以降は増やさない（追記自体が巡回の症状だったため）。
- 以降の反復で同一 4 bullets が再到来した場合: 再 audit せず本 TASK を引用して応答する。新規 gap の実証（失敗テスト・mutation RED）がある場合のみ新規 TASK を起票する。

### bullet 2: label-affecting メタデータの一貫性 → **回答完了・テスト済み不変条件**（TASK-0221/0222）

- 実在対応物: `prefix_feature_cache_metadata_from_config()`（単一 cfg→fingerprint mapping・`ce48610`）へ全 3 cache producer（train_tg_lora / async_cache_builder / precompute）を接続（`addba74`）。
- 実証: `TestCrossProducerFingerprintIdentity`（OmegaConf runtime model × Pydantic validated model × 3 producer の byte-identical 同一性・非 default label-affecting 値 `train_on_prompt=True` / `trainable_lora_scope="last_25_percent"` の指紋到達を非自明性保証として assert）+ producer ごとの乖離 mutation 2 種を実執行し RED→GREEN。
- スイート結果: 115 passed（prefix-cache/async/precompute 5 スイート）+ 51 passed/1 xfailed（canary+static guards）+ ruff clean（TASK-0222 検証ログ参照）。
- resume-ledger 側の断面も TASK-0218（ModelConfig）/TASK-0219（LoRAConfig+seq_len）/TASK-0221（TrainingConfig 消費面 + DataConfig 隔離）で列挙ガード済み。

### bullet 3: context_size 差の cache miss / 再推論確認 → **PHANTOM 確定・実体 max_seq_len で統合テスト済み**

- `context_size` は本 checkout に存在しない（`rg context_size --type py` = 0 件・label 影響は `max_seq_len` が担う）。AI-Hub 側語彙の phantom は [[ai-hub-feedback-infra-vs-this-repo]] の目録通り。
- 実体の実証: `5616e36` の TC-132-09（`max_seq_len` 差 → cache path 差 → disk hit せず rebuild）+ 統合テストが PASS（115 passed に含む）。mutation 実執行ログは TASK-0220。
- 「実サーバー」確認は本ミラーでは不能・不要: prefix cache はオフライン特徴量キャッシュであり、サーバー reject 経路はこの keying に存在しない（入力 truncation の実体 = `max_seq_len` = カバー済み）。

### bullet 4: 9B run 再開 or operator-facing decision surface → **後者を本 iter で新規実行（下記ログ）・前者は不要**

- 9B full-budget run は両脚とも**発射済み・回収済み**（homogeneous `4b88ca8` / heterogeneous `6ca9255`・再発射は TIES を再生産するだけで情報量ゼロ = decision tool の "no re-fire is needed" 文言）。
- operator-facing decision surface の新規実行結果を次節に記録する。`--land` は BY DESIGN operator-gated（[[ai-hub-feedback-infra-vs-this-repo]]）であり、本 TASK は surface まで。land はしない（Cat-C・非単独執行）。

## 検証ログ（実執行・2026-07-28）

### `scripts/section4_operator_decision.py` 新規実行（GPU/src.data 不要）

```
Verdict arc (re-derived GPU-free from committed deposits):
  [homogeneous]   TIES · citable · faithful · cand 1.6947 vs surr 1.6960 · CI[-0.0001, +0.0027] · seq1024
  [heterogeneous] TIES · citable · faithful · cand 1.7180 vs surr 1.7191 · CI[-0.0011, +0.0028] · seq1024
  arc_complete = True

Quality preservation vs full backprop (GOAL §4-247 · P1 品質保持):
  [homogeneous]   SURPASSES · cand 1.6947 vs full-backprop 1.8794 (n_baseline=3)
  [heterogeneous] SURPASSES · cand 1.7180 vs full-backprop 1.8862 (n_baseline=3)

RECOMMENDATION: SHIP
  ... quality vs full backprop is answered per-leg in `quality_preservation`
  (homogeneous SURPASSES; heterogeneous SURPASSES); absolute-loss vs the
  private-repo PRODUCTION baseline is Cat-C and private-repo-only (PIVOT).

EXIT=3 (operator --land 待ち = by design・surface はブロックを継続)
```

要点: SHIP 推奨の P1 品質保持節が `quality_preservation` から**自動導出**で両脚 SURPASSES を読んでいる（TASK-0215 の導出化 + TASK-0216 の harvest が予測通り結合・rationale 文字列の手編集ゼロ）。残る絶対損失は private-repo の production baseline 比のみ（DATA/Cat-C・本ミラーでは不能）。

### decision tool テストスイート

```
tests/test_section4_operator_decision.py: 51 passed (210s)
```

## 次反復以降の唯一の lever（single source of truth）

1. **operator `--land <ship|accept_null|pivot>`** — 人間判断待ち。コードで解消不能（non-unilateral by design）。
2. **private `src.data` 絶対損失**（PIVOT・private repo `/home/jinno/tg-lora` でのみ執行可能）— 本ミラーでは deliberately stripped・half-port 禁止。

この 2 つのどちらかが動いた証拠（land 記録コミット / 新 deposit）なしに指紋系・4-bullet 系の再 audit を起票しないことが、本 closeout の契約である。

---

## 信頼性レベルサマリー

- **総項目数**: 6項目（4 bullet マッピング + decision 実行ログ + ツールテスト）
- 🔵 **青信号**: 6項目 (100%) — 全項目が既存コミット・live 実行結果に直接根拠
- 🟡 **黄信号**: 0項目
- 🔴 **赤信号**: 0項目（自動推定なし・phantom 記号への変換は既存 TASK の判定を継承）

**品質評価**: 高品質（コード変更ゼロは欠落ではなく結論 — 追記25 の契約履行）

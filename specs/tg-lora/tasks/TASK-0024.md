# TASK-0024: Docker 開発環境構築

**タスクID**: TASK-0024
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 6 - 再現性とデータ品質向上
**信頼性レベル**: 🟡 *NFR-201/202 の再現性要件から推測。Docker は要件定義書に明示なし*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../requirements.md) - NFR-201, NFR-202

## タスク概要

Docker コンテナによる再現可能な開発・学習環境を構築する。RTX3060 12GB での学習を前提とし、CUDA 対応のイメージを使用する。依存関係の固定化により、環境差異による実験結果の変動を防ぐ。

## 依存タスク

- **前提タスク**: [TASK-0023](TASK-0023.md)
- **後続タスク**: TASK-0025

## 完了条件

- [x] Dockerfile が作成されている（CUDA対応ベースイメージ）
- [x] docker-compose.yml が作成されている
- [x] コンテナ内で pytest がパスする
- [x] README.md に Docker 使用手順が追加されている
- [x] .dockerignore が作成されている

---

## 主要実装項目

### 1. Dockerfile 作成 🟡

**信頼性**: 🟡 *pyproject.toml の依存関係・CUDA要件から推測*

- ベースイメージ: nvidia/cuda 対応の Python 3.11+
- 依存インストール: pip install -e ".[dev]"
- 作業ディレクトリ設定
- キャッシュ最適化

**実装ファイル**: `Dockerfile`

### 2. docker-compose.yml 作成 🟡

**信頼性**: 🟡 *RTX3060 GPU 使用の要件から推測*

- GPU デバイス設定 (deploy.resources.reservations.devices)
- ボリュームマウント (data/, runs/, reports/)
- 環境変数設定

**実装ファイル**: `docker-compose.yml`

### 3. .dockerignore 作成 🔵

**信頼性**: 🔵 *.gitignore の既存エントリより*

.gitignore に含まれるディレクトリ (data/, runs/, reports/, docs/llm-wiki/) を .dockerignore にも反映。

**実装ファイル**: `.dockerignore`

---

## 基本的なテスト要件

### Docker 環境動作確認 🟡

**信頼性**: 🟡 *Docker 環境固有の制約から推測*

- docker build が成功する
- docker compose run --rm tg-lora pytest がパスする
- GPU がコンテナ内で認識される

---

## 実装手順

### DIRECTタスクの場合

1. `/tsumiki:direct-setup` - 直接実装・設定
2. `/tsumiki:direct-verify` - 動作確認・品質確認

---

## 注意事項

- Docker イメージサイズは最小化する（マルチステージビルド検討）
- データディレクトリはボリュームマウントで外部化
- モデルキャッシュ (/root/.cache/huggingface) もボリューム化

---

## 信頼性レベルサマリー

- **総項目数**: 4項目
- 🔵 **青信号**: 1項目 (25%)
- 🟡 **黄信号**: 3項目 (75%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 要確認 — Docker 環境は実際のビルド・テストで検証が必要

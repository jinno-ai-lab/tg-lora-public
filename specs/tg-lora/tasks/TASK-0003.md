# TASK-0003: load_model, lora_utils ユニットテスト追加

**タスクID**: TASK-0003
**タスクタイプ**: TDD
**推定工数**: 2時間
**フェーズ**: Phase 1 - データ・モデル層テスト追加
**信頼性レベル**: 🔵 *既存実装 src/model/*.py・acceptance-criteria.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-021, REQ-022, REQ-023, REQ-024
- **設計文書**: [📐 architecture.md](../architecture.md)

## タスク概要

`src/model/load_model.py` と `src/model/lora_utils.py` のユニットテストを追加する。モデル読み込み・LoRA適用のテストが現在不存在。4bit量子化にはGPUが必要なため、CPU上でテスト可能な部分（lora_utils）に重点を置く。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-0006](TASK-0006.md)

## 完了条件

- [x] `iter_lora_params()` がlora_A/lora_Bパラメータのみを抽出することを検証
- [x] `iter_lora_params_by_layer()` がレイヤー別に正しくグループ化することを検証
- [x] `count_lora_params()` がtrainable parameters数を正しく返すことを検証
- [x] `build_bnb_config()` が正しいBitsAndBytesConfigを生成することを検証（GPU環境依存はマーク）
- [x] 全テストがパスする

---

## 主要実装項目

### lora_utils テスト 🔵

**信頼性**: 🔵 *lora_utils.py 実装・REQ-024・acceptance-criteria.md TC-021-01, TC-021-02 より*

- `iter_lora_params()`: LoRAパラメータのフィルタリング検証
- `iter_lora_params_by_layer()`: レイヤーインデックスによるグループ化検証
- `count_lora_params()`: パラメータ数カウントの正確性
- 小規模モデル（GPT-2等）にLoRAを適用してテスト

**実装ファイル**: `tests/test_lora_utils.py`

### load_model テスト 🟡

**信頼性**: 🟡 *load_model.pyはGPU依存のため、CPU実行可能部分のみテスト*

- `build_bnb_config()` の設定値検証
- `load_tokenizer()` の動作検証
- GPU依存テスト（4bit読込、VRAM判定fp32キャスト）は `pytest.mark.skipif` で条件付き化

**実装ファイル**: `tests/test_load_model.py`

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-0003` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- **総項目数**: 2項目
- 🔵 **青信号**: 1項目 (50%)
- 🟡 **黄信号**: 1項目 (50%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質 — load_modelのGPU依存部分のみ🟡

# TASK-0069: ベンチマークスクリプトスモークテスト

**タスクID**: TASK-0069
**タスクタイプ**: TDD
**推定工数**: 1.5時間
**フェーズ**: Phase 30 - OptimizerLifecycleManager E2E検証
**信頼性レベル**: 🔵 *AI_HUB_MAKE_RUN_FEEDBACK指摘・interview-record.md残課題L697・REQ-123より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../requirements.md) - REQ-123
- **設計文書**: [📐 architecture.md](../architecture.md) - benchmark_optimizer_lifecycle.py

## タスク概要

`scripts/benchmark_optimizer_lifecycle.py`がtinyモデルで正常に実行され、有効なJSON比較結果を出力することを検証するスモークテストを追加する。現在、ベンチマークスクリプトは実装済みだが、CI/テストスイートからの自動検証がなく、スクリプトのregression breakを検知できない状態。

## 依存タスク

- **前提タスク**: [TASK-0067: テストスイート全通過確認とoverview整合性更新](TASK-0067.md)
- **後続タスク**: [TASK-0070: Phase 30 ドキュメント更新](TASK-0070.md)

## 完了条件

- [x] tinyモデル（Linear層のみ）でベンチマークスクリプトが正常終了することを検証
- [x] 出力JSONに`recreate_per_cycle`と`reuse_state_reset_experimental`両方の結果が含まれることを検証
- [x] reuse_state_reset_experimentalでstate tensor pointerが保存されていることを検証
- [x] 既存テストスイート全通過（リグレッションなし）

---

## 実装詳細

### 1. テストファイル作成 🔵

**信頼性**: 🔵 *scripts/benchmark_optimizer_lifecycle.pyの実装・既存テストパターンより*

`tests/test_benchmark_optimizer_lifecycle.py`を作成。tinyモデル（`_TinyModel`パターン: test_optimizer_lifecycle.pyと同じ）を使用してベンチマークのcore logicをテスト。

**実装ファイル**: `tests/test_benchmark_optimizer_lifecycle.py`

### 2. tinyモデルベンチマーク実行テスト 🔵

**信頼性**: 🔵 *benchmark_optimizer_lifecycle.pyの_measure_cycle()・main()構造より*

benchmark_optimizer_lifecycle.pyの関数を直接インポートしてtinyモデルで実行。またはサブプロセスでスクリプトを実行して出力JSONを検証。直接インポートを優先（テスト実行速度・デバッグ容易性）。

```python
def test_benchmark_runs_with_tiny_model(tmp_path):
    """Benchmark script produces valid JSON with both policies."""
    # Create tiny model
    # Run benchmark with N=2 cycles
    # Verify output JSON structure
```

### 3. 比較結果構造検証 🔵

**信頼性**: 🔵 *benchmark_optimizer_lifecycle.py出力構造より*

出力JSONに以下が含まれることを検証:
- `recreate`セクション: prepare_time, step_time, memory_delta
- `reuse`セクション: prepare_time, step_time, memory_delta
- `comparison`セクション: speedup percentages
- `reuse_state_ptrs_preserved`: true

---

## 単体テスト要件

### テストケース1: tinyモデルベンチマーク実行 🔵

**信頼性**: 🔵 *REQ-123・benchmark_optimizer_lifecycle.py実装より*

**Given**: tiny Linearモデルを準備
**When**: ベンチマークを2サイクル実行
**Then**: JSON出力にrecreate/reuse両セクションが含まれる

**テストファイル**: `tests/test_benchmark_optimizer_lifecycle.py`

### テストケース2: reuseポリシーstate tensor保存検証 🔵

**信頼性**: 🔵 *EDGE-163・benchmark_optimizer_lifecycle.py state_tensor_pointers()検証より*

**Given**: reuse_state_reset_experimentalポリシーでベンチマーク実行
**When**: 出力JSONのreuse_state_ptrs_preservedを確認
**Then**: `reuse_state_ptrs_preserved == True`

### テストケース3: 比較メトリクス構造検証 🟡

**信頼性**: 🟡 *benchmark_optimizer_lifecycle.py _round_record()構造から妥当な推測*

**Given**: ベンチマーク出力JSON
**When**: comparisonセクションを確認
**Then**: prepare_speedup_pct, step_speedup_pct, memory_delta_pctが数値

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-0069` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- テストはCPUのみで高速実行可能（tinyモデル使用）
- benchmark_optimizer_lifecycle.pyの関数を直接インポートしてテストする方針
- GPU環境では実際のベンチマークが実行されるが、CPU環境ではフォールバック動作を確認

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 2 | 1 | 0 | 3 |

### 全体評価

- **総項目数**: 6項目
- 🔵 **青信号**: 5項目 (83%)
- 🟡 **黄信号**: 1項目 (17%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質 — 🟡は出力JSON構造の推測に基づくが、スクリプト実装で確認可能
